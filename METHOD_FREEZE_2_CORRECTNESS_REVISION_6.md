# RevealNav Method-Freeze-2 正确性修订 6

**修订编号：** `MF2-CR6`  
**日期：** 2026-08-26  
**状态：** 经用户批准的多分支正确性修订；多分支数据关卡通过前仅允许
实现接口、构建监督和运行 smoke，不允许报告训练或 benchmark 结论。  
**替代范围：** 替代 `MF2-CR5` 工程实现中把一个真实决策区压缩为“目标分支 +
单个备选分支”的隐含二元假设。`MF2-CR5` 的事件发现、三维定向、执行验证、
严格因果回放、数据边界和困难负例要求保持不变。  
**规范边界：** `FROZEN_SPEC.md` 与 `PHASE0_PROTOCOL.md` 保持逐字节不变；
REE/ECOG/OPP 仍是仅有的三个在线方法模块。

## 1. 触发证据

在封存的 525 个 RxR-train 严格因果候选中，事件级 MLLM 原始提议包含：

- 332 个二分支事件；
- 191 个三分支事件；
- 2 个四分支事件。

其中 158 个事件具有至少两个被提议为可通行的非目标分支。现有 Phase-1
grounder、因果语言门和资源条件化 `T_X` 工厂只保留搜索分数最低的一个备选，
因此它们验证的是一个可复现 pairwise 投影，而不是完整决策区。该投影继续保留为
`pairwise-v1` 消融和工程基线，但不能代表正式多分支方法。

## 2. 完整候选集合与动态 Top-2 活动宽度

每个 ECOG 决策节点保存完整的、经过三维与控制器验证的候选集合

\[
\mathcal B_t(v)=\{b_1,\ldots,b_{m_t}\},\quad m_t\ge2,
\]

以及每个分支的 `UNTRIED/ACTIVE/EXHAUSTED/COMMITTED` 状态。候选不会因为
未进入当前活动对而被删除。

在线 OPP 对全部尚可行候选预测统一任务损失 `Q`，但每一步只激活损失最低的两个：

\[
\mathcal A_t^{(2)}(v)=
\operatorname*{arg\,Top2}_{b\in\mathcal B_t^{\rm viable}(v)}[-Q(h_t,G_t,a_b)].
\]

活动宽度固定为 2，因为它是同时保留一个当前选项和一个明确备选的最小非平凡宽度，
而不是因为环境被假设为二分叉。每次获得新因果观测、更新分支状态或完成回退后，必须
对完整尚可行集合重新排序；禁止把初始顺序固定到 episode 结束。

典型状态更新为：

```text
完整集合 {b1,b2,b3,b4}，活动对 (b1,b2)
b1 失败并安全回退 -> b1=EXHAUSTED -> 重新排序 -> (b2,b3)
新证据改变 Q       -> 重新排序可得到 (b3,b2) 或其他顺序
```

只对活动对执行昂贵的在线探索/比较，完整集合仍保留在 ECOG 中供晋升。该调度器属于
既有 OPP 的动作裁剪与排序，不新增第四个方法模块。

## 3. 多分支 Reveal、成本与监督

目标 Reveal 必须相对所有仍可行竞争分支闭合，而不是只相对一个人工选出的负分支：

\[
T_R=\min\{t:b^*\in\widehat{\mathcal B}_t,
b^*\text{ 与所有可行 }b_j\ne b^*\text{ 可区分，且决定性证据闭合}\}.
\]

对每个分支和固定预算分别生成可复现的资源条件化成本前沿：

\[
C_{t,i}^*=\min(C_{t,i}^{\rm direct},C_{t,i}^{\rm save}),\qquad
T_X^{(i)}(B)=\max\{t:C_{t,i}^*\le B\text{ 且控制序列安全}\}.
\]

在线模型只接收因果历史、剩余预算及预测成本；未来后缀只用于离线
`T_X^{(i)}` last-passage 标签。训练包含：

1. 全集合 target-in-set 与相对所有竞争者的 separation/constraint closure；
2. 每候选归一化 cost-to-go Huber loss；
3. 同一节点内候选动作的 margin ranking；
4. 固定预算下当前可行性和删失感知 cost-frontier loss；
5. Top-2 排序/晋升一致性，不监督固定绝对分支序号。

不枚举分支访问顺序的全排列。标签工厂只执行每候选的一步有界反事实及共享 checkpoint
返回，复杂度随候选数近似线性。

## 4. 数据与审计边界

现有 300 条单人审核验证的是 `pairwise-v1` 中被选择的一对分支。由于其中 9 个反例已被
用于诊断备选真实性，它们转为 development audit，不得再作为多分支 v2 的无偏 Gold
或阈值选择依据。

多分支 v2 必须：

- 从封存的原始 proposal 恢复全部语义分支，不修改原始响应；
- 对每个非目标分支独立执行三维定向、非来路/非重合和控制器验证；
- 保留失败分支及类型化原因，不以找到一个有效备选即提前停止；
- 以 scene 为单位划分 train/development/Gold，禁止同一场景跨集合；
- 重新冻结未查看的独立人工审核样本，审核“是否完整列出所有真实可执行出口”以及目标
  是否相对完整竞争集合唯一；
- 最终三人 Gold 和一致性门槛仍以 `FROZEN_SPEC.md` 为准。

## 5. 训练前关卡

正式方法训练前必须同时满足：

1. 所有 v2 事件保存完整候选集合，候选 ID 唯一且至少两个控制器验证分支；
2. 每个分支都有三维路径、执行状态、因果候选历史和资源成本状态，缺项失败关闭；
3. Top-2 在初始排序、候选耗尽、证据更新和回退后晋升四类测试中确定性通过；
4. hidden/future frame 扰动不改变暴露前的集合、排序或动作；
5. `pairwise-v1`、Top-1、Top-2、Top-3 和 full-set 使用相同候选前端与控制预算；
6. 新鲜独立人工集通过后才允许把 v2 自动标签加入正式训练；
7. 原 525 条 pairwise 证据、冻结文档、公开数据 payload 和 checkpoint 均无覆盖或漂移。

## 6. 非结论

本修订不证明 Top-2 优于 full-set、Top-1 或既有 VLN 方法，不授权把单人审核称为 Gold，
也不保证 A 会录用。其当前作用是修复真实多分支被错误二值化的问题，并冻结一个计算可行、
可消融、与 REE/ECOG/OPP 一致的正式训练对象。
