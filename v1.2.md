# RevealNav：动态不完整候选集下的语言证据闭合与序贯承诺

> 版本说明：本文保留为早期问题定义稿。当前实施与投稿方案只以 [FROZEN_SPEC.md](FROZEN_SPEC.md) 为准。

这个工作的核心观察是：现有 VLN 通常研究“在当前候选动作中选择哪一个”，但真实前向有限视场中可能同时存在两类更前置的问题。

第一类是，正确分支本身还没有进入当前候选集合。

第二类是，正确分支虽然已经可见，但确认它满足“第二个入口”“跳过第一个”“白墙之后”这类语言描述所需的历史证据还没有出现。

所以，我们真正研究的不是简单的“看不清就等”，而是：

> **正确分支及其语言身份现在是否已经被充分显现；如果还没有，机器人能否在保留选择权的情况下继续运动，并在证据充分后再作出不可逆承诺？**

---

## 一、研究背景

视觉语言导航，也就是 VLN，希望机器人能够根据自然语言路线，在陌生环境中完成连续导航。

例如：

> 沿主路前进，经过黄色货车后继续走，在白墙后的第二个右侧入口右转，然后走到红色铁门前停下。

这条指令包含：

- 长路线理解；
- 静态地标 grounding；
- 路线阶段和历史记忆；
- “第二个入口”这样的序数关系；
- 中途分支选择；
- 终点目标定位和 STOP。

近年来，VLN 已经从离散全景节点，逐渐发展到连续环境、流式 RGB、VLM 推理和真实机器人部署。

但是，大部分方法在作出分支决策时，仍然隐含假设：

\[
b^*\in\widehat{\mathcal B}_t
\]

其中 \(b^*\) 是指令要求的正确分支，\(\widehat{\mathcal B}_t\) 是当前时刻可见或被模型提出的候选集合。

也就是说，现有方法主要研究：

> 正确答案已经在选项中时，怎样把它选出来？

但真实前向相机只能看到有限视野。侧路、窄入口和连续路口会随着机器人接近逐渐出现，因此当前候选集合可能并不完整。

更进一步，即使正确入口已经出现，它的语言身份也可能仍然无法验证。

---

## 二、两个关键反例

### 反例一：正确入口尚未出现

指令要求：

> 在第二个右侧入口右转。

机器人当前只看到：

- 继续直行；
- 第一个右侧入口。

第二个右侧入口仍被建筑、植被或车辆遮挡。

此时：

\[
b^*\notin\widehat{\mathcal B}_t
\]

模型即使对第一个右转非常自信，也不可能真正选对，因为正确答案尚未进入候选集合。

我们把这类情况称为：

> **Candidate Absence。**

### 反例二：正确入口已经出现，但“第二个”的证据没有出现

还有一种更容易被忽略的情况。

机器人当前看到一个右侧入口，这个入口在环境真值中确实是第二个右侧入口；但此前的第一个入口被完全遮挡，机器人从来没有观察到它。

此时：

\[
b^*\in\widehat{\mathcal B}_t
\]

但是，模型无法验证当前入口为什么是“第二个”。

如果它直接进入，行为结果可能碰巧正确，但它并没有可靠完成“第二个右侧入口”的语言 grounding。

我们把这类情况称为：

> **Referential Evidence Absence。**

这两个反例说明，RevealNav 需要同时判断：

1. 正确候选是否已经出现；
2. 确认其语言身份所需的序数、排除、时序或空间关系证据是否已经闭合。

---

## 三、已有工作解决了什么

目前最相关的工作大致可以分成五类。

### 1. AgentVLN：技能路由、主动探索和自我纠错

AgentVLN 使用 VLM 作为高层控制器，在导航、探索和目标定位等技能之间进行调度，并依据目标可见性切换导航阶段。

它的优点是系统能力完整，能够处理遮挡、偏航和目标搜索。

### 2. AdaNav：根据动作不确定性触发推理

AdaNav 使用 Action Entropy 判断什么时候需要额外推理。当动作分布不确定时多思考，分布确定时减少推理开销。

它主要回答：

> 模型什么时候不确定？

### 3. ProFocus：主动寻找缺失视觉信息

ProFocus 判断当前推理缺少哪些视觉信息，并主动获取针对性观测，同时从历史 waypoint 中筛选高价值候选。

它主要回答：

> 当前还缺什么信息，以及应该观察哪里？

### 4. DRIVE-Nav：维护跨帧稳定的方向候选

DRIVE-Nav 不直接在大量瞬时 frontier 上决策，而是维护 persistent directions，并在路口进行 inspection 和 verification。

它主要回答：

> 怎样组织随着观测逐渐出现的探索方向？

### 5. AwareVLN 与 LiveVLN：进度理解和连续执行

AwareVLN 显式理解当前空间状态和任务进度。

LiveVLN 解决推理和执行不同步的问题，让机器人在处理新观测时继续执行可更新的动作前缀。

这些工作分别提升了技能组织、不确定性触发、主动感知、候选管理、进度理解和运行时连续性。

但是，它们通常没有单独表示：

> 当前候选是否包含正确分支，以及确认该分支语言身份所需的历史证据是否完整。

---

## 四、已有方法在这些场景中可能出现什么错误

这里需要说明，下面是根据公开方法机制推导出的潜在失败模式，而不是在没有统一复现实验前断言已有方法一定失败。

### 1. AgentVLN 的第一类潜在错误：过早选择当前最合理入口

假设第一个右侧入口已经清晰可见，而第二个入口仍未出现。

第一个入口在方向和局部外观上与“右侧入口”部分匹配，因此它可能获得当前最高语义分。

如果系统在当前 waypoint 或技能中必须选择下一步，就可能高置信度进入第一个入口。

问题不是它不能区分两个入口，而是：

> 两个入口尚未同时进入比较空间。

### 2. AgentVLN 的第二类潜在错误：主动纠错发生得太晚

AgentVLN 能够在发现偏航或目标不匹配后主动探索和恢复。

但真实连续导航中，一旦机器人已经进入错误入口，或者越过正确入口的最后安全转向位置，后续纠错可能已经无法恢复。

因此：

- AgentVLN 更强调错误发生后如何探索和修正；
- RevealNav 强调不可恢复错误发生前，是否已经具备承诺条件。

### 3. AgentVLN 的第三类潜在错误：目标可见性不能替代中途分支显现

AgentVLN 的 target visibility 主要围绕最终目标或目标物体组织阶段路由。

但在完整路线中，“第二个右侧入口”只是中途路线事件，最终目标可能是后面的红色铁门。

红色铁门不可见，不能直接说明：

- 第二个入口是否已经出现；
- 当前入口是否是独立可通行候选；
- “第二个”和“白墙后”是否已经被验证。

### 4. AgentVLN 的第四类潜在错误：行为走对，但语言 grounding 没有成立

考虑第二个反例：当前可见入口事实上就是第二个，但第一个入口从未被观察。

如果 AgentVLN 根据当前唯一可见的右侧入口直接进入，它可能在行为结果上走对。

但同一当前画面下，如果指令改成“第一个右侧入口”，它是否仍会进入同一个入口？

如果答案是肯定的，那么它并没有真正解析序数条件，只是利用当前候选唯一性作出动作。

因此，最终 SR 可能把两种结果混在一起：

- 基于完整语言证据的正确决策；
- 没有证据、但碰巧走对的 accidental correctness。

RevealNav 会对这两者进行区分。

### 5. AdaNav：可能在错误或证据不完整的集合上得到低熵

假设当前候选只有直行和一个右侧入口，模型给出：

\[
P(\text{right})=0.9,
\qquad
P(\text{straight})=0.1
\]

动作熵很低，AdaNav 可能认为没有必要增加推理。

但低熵可能对应两种错误：

1. 正确的第二个入口还没有出现；
2. 当前入口确实是第二个，但“第二个”的前置计数证据没有出现。

因此我们要证明：

\[
\text{low entropy}
\not\Rightarrow
\text{target in set}
\]

并且：

\[
\text{target in set}
\not\Rightarrow
\text{referential evidence complete}
\]

### 6. ProFocus：知道还缺信息，但未必知道还能等多久

ProFocus 能判断当前缺少视觉信息，并主动获取观测。

但在分支场景中，还必须同时判断：

- 再观察一次后是否仍有合法转弯机会；
- 继续前进是否会越过入口；
- 当前是应该 FOLLOW、INSPECT，还是必须安全 fallback。

所以 RevealNav 不只判断“还要不要看”，还要判断“是否仍然来得及看”。

### 7. DRIVE-Nav：方向稳定不等于语言身份稳定

两个相邻的右侧入口在几何上可能都属于同一个右侧方向，但语言上分别是“第一个”和“第二个”。

如果只维护稳定右侧方向，可能无法表示：

- 第一个入口已经出现；
- 第二个入口尚未出现；
- 当前入口是第二个，但第一个入口证据缺失。

因此 RevealNav 的状态需要绑定 branch identity 和语言历史，而不仅是方向。

### 8. AwareVLN：知道下一阶段是什么，不等于知道当前候选身份

模型即使知道下一阶段是“寻找第二个右侧入口”，也不能仅凭进度判断当前入口究竟是第一个还是第二个。

进度可以辅助判断，但不能替代候选存在性和语言证据闭合。

### 9. LiveVLN：可能更流畅地执行错误决定

LiveVLN 解决的是动作供应和连续执行。

如果高层模型过早把当前入口当成目标，LiveVLN 可能只是更流畅地进入错误入口。

所以两者是正交的：

- LiveVLN 解决怎么连续执行；
- RevealNav 解决什么时候可以不可逆地选分支。

---

## 五、我们的核心表示：U/A/D Reveal State

我们保留三个状态，不增加第四个状态。

### 1. UNOBSERVED，U

\[
b^*\notin\mathcal B_t^{\mathrm{verified}}
\]

正确分支尚未成为独立、局部可通行的候选。

例如第一个入口已经出现，但第二个入口仍被遮挡。

### 2. AMBIGUOUS，A

目标分支已经进入候选集合：

\[
b^*\in\mathcal B_t^{\mathrm{verified}}
\]

但至少存在一种不充分性：

- 当前目标与竞争入口无法稳定区分；或者
- 目标虽然已经出现，但序数、排除、时序或空间关系证据尚未闭合。

例如：

> 当前可见入口事实上是第二个，但第一个入口从未被观察。

这个样本必须标为 A，而不能因为当前只有一个候选就标为 D。

### 3. DISCRIMINABLE，D

D 必须同时满足：

1. 目标已经成为独立可通行候选；
2. 目标能够与竞争候选稳定区分；
3. 所有影响身份的决定性语言约束都能由允许的历史观测验证。

例如，“第二个入口”进入 D，至少需要可靠观察或确认此前已经存在一个右侧入口。

整体状态大致遵循：

\[
U\rightarrow A\rightarrow D
\]

允许少量回退，但通过状态转移约束抑制逐帧抖动。

---

## 六、语言证据闭合

我们额外标注一个辅助变量：

\[
e_t^{\mathrm{ref}}
=
P(\text{决定性语言约束已由历史观测解析})
\]

数据中可以表示为：

```yaml
language_constraints:
  direction_right: resolved
  ordinal_second: unresolved
  landmark_after_white_wall: resolved

referential_evidence_complete: false
unresolved_constraints:
  - ordinal_predecessor_missing
```

它不是第四个 Reveal State，而是帮助区分 A 和 D 的证据变量。

对于典型反例：

```text
target_in_set = high
candidate_margin = high
referential_evidence_complete = low
Reveal State = A
```

这样模型不会因为当前候选唯一、匹配分高，就错误认为已经可以承诺。

---

## 七、方法设计

RevealNav 的核心仍然只有三个模块。

### 1. Candidate and Referential Evidence

模型同时估计三个问题：

1. 目标是否已经进入候选集合；
2. 正确候选是否超过最强竞争候选；
3. 确认目标语言身份所需的历史证据是否闭合。

候选匹配 margin 为：

\[
m_t=
score_t(b^*)-
\max_{j\neq *}score_t(b_j)
\]

训练重点构造以下反事实：

- 相同当前视觉，但历史中是否观察过前置入口不同；
- 相同当前画面，指令分别为“第一个入口”和“第二个入口”；
- 相同指令逻辑，不同入口显现顺序；
- 相似入口和假入口；
- 低熵但目标缺失；
- 低熵、目标已出现，但语言证据缺失；
- 镜像并同步修改左右关系；
- 候选顺序随机化；
- 移除历史帧、只保留当前帧的消融。

### 2. Reveal Belief Filter

模型维护时序 belief：

\[
q_t(z)=P(z_t=z),
\qquad z\in\{U,A,D\}
\]

它根据完整指令、历史 RGB、候选变化、语言约束证据和上一时刻状态，递归更新 U/A/D。

我们不要求标注精确转移帧，而是标：

- U 结束区间；
- A 区间；
- D 开始区间。

训练使用 interval-censored supervision，减少边界帧主观性。

### 3. Sequential Commitment

每个时刻输出：

\[
a_t=(u_t,c_t)
\]

其中：

- \(u_t\) 是运动方式；
- \(c_t\) 是是否对某个语义分支承诺。

机器人可以：

- FOLLOW + UNCOMMITTED：沿共享路线前缀继续运动；
- INSPECT + UNCOMMITTED：减速或短暂观察；
- ENTER + COMMIT：进入具体分支。

立即承诺的代价包括：

- 选错分支风险；
- 状态尚未达到 D；
- 语言证据尚未闭合。

继续观察的代价包括：

- 时间和路径延迟；
- 越过最后安全选择机会的风险。

因此，模型不是无限等待，而是在错误、延迟和错过机会之间作序贯决策。

---

## 八、不可解样本如何处理

存在一些事件，在当前传感器条件下原则上不可解。

例如第一个入口从未进入任何历史帧，也没有其他地标能够间接确认，而指令要求“第二个入口”。

如果直到最后安全选择机会之前，序数证据仍无法获得，那么不能用环境真值强迫模型进入 D。

这类事件标为：

```text
UNRESOLVABLE_BEFORE_SPLIT
```

处理方式是：

- 不混入标准可解事件成功率；
- 不把随机猜中计为可靠 grounding；
- 单独评价安全 fallback、abstention 和 unresolved detection；
- Gold 主测试保证标准事件原则上在 split 前可解。

这可以避免数据集要求模型预测不可观察信息。

---

## 九、数据与实验范围

主论文只保留三类证据。

### 1. RxR-CE-Reveal

作为唯一公共主 benchmark，利用长路线语言、地标关系和连续环境构造逐前缀 Reveal Event。

环境几何只生成标签，不作为模型输入。

### 2. Shuangpu-Gold

使用双浦镇机器狗单目鱼眼连续视频，离线转换为标准 RGB 多视图。

重点覆盖：

- 隐藏窄路；
- 连续多个入口；
- 第一个入口隐藏、第二个入口可见；
- 相似入口；
- 假入口；
- 植被、车辆和建筑遮挡；
- 一条路线中的多个 Reveal Event。

### 3. Physical Quadruped

真实闭环中，Base 和 RevealNav 使用相同：

- 底层控制器；
- 速度；
- 观测频率；
- 候选前端；
- 终点定位模块。

只改变 Reveal State 和分支承诺机制。

---

## 十、候选前端协议

为了避免论文同时变成入口检测论文，固定两种候选输入。

### Oracle Current Candidates

人工或模拟器提供当前真实可见候选，用于隔离 Reveal State 和承诺方法。

### Frozen Automatic Candidate Frontend

采用冻结的：

\[
\text{VLM point proposal}
+
\text{traversability score}
+
\text{temporal matching}
\]

这样可以把失败分解为：

- 候选发现；
- 候选跟踪；
- Target-in-Set；
- Referential Evidence；
- Reveal State；
- 语言匹配；
- 过早承诺；
- 错过机会；
- 低层执行。

---

## 十一、主实验要证明什么

### 证据一：Candidate Absence 确实导致低熵错误

目标尚未进入集合，但强模型对当前错误入口高置信度提交。

### 证据二：Referential Evidence Absence 是独立失败模式

目标已经出现，甚至当前候选唯一，但“第二个”“跳过第一个”等身份约束尚未被历史证据支持。

模型可能：

- 错误提交；
- 或行为碰巧正确，但 grounding 不成立。

### 证据三：普通 uncertainty 和 visible classifier 不够

需要同时证明：

\[
\text{low entropy}
\not\Rightarrow
\text{target in set}
\]

以及：

\[
\text{target in set}
\not\Rightarrow
\text{referential evidence complete}
\]

### 证据四：历史证据是必要的

重点反事实是：

- 当前画面完全相同；
- 一组历史看见过第一个入口；
- 另一组历史没有看见；
- 指令都要求第二个入口。

正确模型应在前者进入 D，在后者保持 A。

### 证据五：序贯决策形成更好的 Risk–Delay Pareto

在相同延迟下降低 premature commitment，或在相同风险下更早正确提交。

### 证据六：普通路线不退化

RevealNav 不能因为过度谨慎拖累无歧义路线。

### 证据七：自动候选和真实机器人仍保留收益

Oracle 下有效只是证明核心假设成立；自动前端和真实闭环需要证明系统价值。

---

## 十二、三个决定性实验

### 实验一：两类低熵 False-Ready

分别构造：

1. Candidate Absence：目标不在集合；
2. Referential Evidence Absence：目标在集合，但语言身份所需证据不完整。

比较：

- max-softmax；
- action entropy；
- no-match；
- binary target-visible classifier；
- current-frame ordinal/relation reasoner；
- history-aware evidence-completeness classifier。

同时报告：

- wrong commitment；
- accidental correct commitment。

### 实验二：U/A/D 和证据闭合标注一致性

至少 300 个 Reveal Event、三名标注者、严格截断前缀。

报告：

- U/A/D Fleiss’ \(\kappa\)；
- evidence-completeness 一致性；
- D 起点偏差；
- unresolved constraint 类型；
- `UNRESOLVABLE_BEFORE_SPLIT` 比例。

### 实验三：Risk–Delay Pareto

训练最小模型：

\[
(I,V_{\le t},\mathcal B_t)
\rightarrow
q_t(U,A,D),e_t^{\mathrm{ref}}
\]

比较：

- entropy threshold；
- temporal visible head；
- current-frame relation reasoner；
- independent U/A/D；
- Reveal Filter without evidence completeness；
- Full Reveal Filter；
- Full Reveal Filter + sequential commitment。

三个实验同时成立后，才启动自动候选、大规模弱标签、联合训练和真实闭环。

---

## 十三、论文故事

整篇论文只讲一条主线。

### 第一幕：现有 VLN 的隐含假设

现有方法研究怎样从当前候选中选对，默认正确候选及其语言身份证据已经存在。

### 第二幕：两个反例

在前向有限视场中：

- 正确分支可能尚未出现；
- 正确分支也可能已经出现，但确认它是“第二个”或“白墙后”的证据尚未出现。

### 第三幕：新的数据表示

逐前缀标注：

- U：目标尚未成为候选；
- A：目标已出现，但候选或语言身份仍不充分；
- D：目标及其语言身份均可稳定验证。

同时标注 referential evidence completeness。

### 第四幕：新的方法

模型联合估计候选存在性、竞争证据和语言约束闭合，通过时序 Reveal Belief 决定什么时候可以承诺。

### 第五幕：闭环价值

在相同延迟下降低错误进入，或者在相同错误风险下更早正确承诺，并从公共仿真迁移到真实户外路线和机器狗。

英文核心句可以是：

> **Existing VLN agents learn which visible action to choose. RevealNav first asks whether the correct branch—and the evidence needed to identify it—has been revealed.**

中文可以说：

> **现有 VLN 研究如何从可见选项中选对；RevealNav 首先判断正确分支及其语言身份是否已经被充分显现。**

---

## 十四、最终贡献

正式论文只写三项贡献。

第一，揭示 full-route VLN 中目标候选或其语言身份证据可能尚未充分显现，并构建逐前缀、区间标注的 U/A/D Reveal State 和证据闭合 benchmark。

第二，提出语言条件 Reveal Belief Filter，通过候选存在性、候选对比和历史语言约束闭合，建模目标未出现、已出现但身份不可辨、以及已可辨三个阶段。

第三，提出序贯分支承诺策略，联合考虑错误进入、观察延迟和剩余选择机会，并在公共连续 VLN、真实户外数据和物理机器狗上验证。

---

## 十五、当前风险

当前最大的风险有五个。

第一，非平凡 Reveal Event 比例不足。

第二，U/A/D 或 evidence-completeness 人工标注不稳定。

第三，普通时序 VLM 已经能够利用历史解决序数证据，完整方法增益有限。

第四，自动候选前端吞噬 Oracle 下收益。

第五，不可解样本比例过高，说明现有相机和路线协议无法提供足够证据。

这些风险均对应明确的 Pivot 或 Stop 条件，不再通过增加模块规避。

---

## 十六、希望讨论的问题

我希望重点讨论三个问题。

第一，是否认可将问题定义从“正确候选是否出现”扩展为“正确候选及其语言身份证据是否充分显现”。

第二，是否认可不增加第四状态，而是将证据未闭合统一放入 A，并通过 `referential_evidence_complete` 辅助监督。

第三，论文方法定位是否应当以 Reveal State 为核心表示，以 Sequential Commitment 作为产生闭环价值的决策层。

我的当前判断是：

> Candidate Absence 解释为什么当前选项中可能没有正确答案；Referential Evidence Absence 解释为什么即使走对，也可能没有真正完成语言 grounding。两者共同构成 RevealNav 相比普通 uncertainty、target visibility 和动态候选方法最清楚的切入点。

---

## 十七、总结

最后总结一下。

RevealNav 不是重新解决整个 VLN，也不是提出新的 SLAM、局部规划或主动探索系统。

它解决的是一个具体但关键的问题：

> 在作出不可逆分支选择前，机器人是否已经看到正确分支，并获得了足够证据确认它满足完整语言描述？

我们使用 U/A/D Reveal State 表示目标从未出现、已经出现但身份未闭合、到最终可辨的过程，再通过序贯承诺机制平衡错误、延迟和错过机会。

所以，这个工作的核心不是“看不清就等”，而是：

> **只有当正确分支及其语言身份都能够由当前历史证据验证时，机器人才能真正承诺。**

重要补充：拓扑VLN，把拓扑作为上下文topoVLN，U-A-D是封装类似skill中的要点；分支记忆提供额外的运行中上下文，探索行为/分支行为，回退信号/停止信号也是UAD模块来给出
