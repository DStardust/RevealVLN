# 双分支批量数据生产：主 agent 接收与当前进度

2026-09-09。本节点依据用户“主要生产普通数据和特殊数据，都要大批量合格数据”执行。**普通数据正在生产；特殊数据已完成跨屋候选编译，但尚未新增训练合格物理族。** 不把候选或回放中间状态冒充最终训练数据。

## 普通数据：已启动首批1,000条新物理路线

本地官方 train 已实际清点：
- R2R-CE v1-3：10,819条原指令、3,603条不同物理参考路线。
- RxR-CE v0 guide：60,300条原记录，选择英语19,996条、6,668条不同物理参考路线；其余语言暂不进入本轮生产，不声称多语言全量。
- 英语来源合计30,815条指令、10,271条物理参考路线；这些是注释来源，不是实产RGB/动作。
- 共用冻结51 FIT /5 INTERNAL_DEV /5 INTERNAL_CONFIRM。FIT源指令R2R 9,260、RxR 16,966；源划分不等于独立未暴露证明。
- 首批固定R2R/RxR各500条，51 FIT屋、2,997指令别名，排除旧pilot所有100个尝试物理路线。保持普通目标300万有效单步记录；1000只是生产批次。

真实编译＋第二次完整物理回放＋独立导出回读后发布不可变shard。路径阈值、像素/pose精确重放、动作尺度、STOP、因果白名单都不变。每50条提交一次，仅索引内样本可用于后续经准入训练；原始route目录或LEDGER里的CERTIFIED仅表示回放阶段，不表示导出最终合格。

第一段运行50条后在严格导出审计暂停：49条双回放通过、1条路径偏离。回读发现10条RxR首次转向有竖直位移，既有约1.67e-6m的小值，也有约0.0104m和0.2107m的较大值。尚未完成起点高度/navmesh归因，不能全部当数值误差放行。
原运行138.22秒与失败、GPU清理证据保留。恢复V2只改逐路线隔离与批量继续，不改1e-6转向阈值、不snap源起点、不重跑已尝试50条。

**首个已发布shard：39合格路线、117指令记录、7,188个有效动作监督；10条导出隔离，1条生成拒收。** 这是新数据，不是模型导航收益。首片非均匀完整样本，不外推全批质量比例。

当前GPU2单worker有监督继续剩余任务；不释放占位、不碰外部GPU任务、不训练。上限原批总4小时/200GiB/1000候选，恢复扣除已用时间。完成或失败时监督器清理自己的进程；没有需要恢复的占位。本文是首片接收快照，不是1000条完成报告。

实时权威：
- [来源清单](../../data_pipeline/ordinary_scale_v1/SOURCE_INVENTORY.json)
- [共享房屋划分](../../data_pipeline/ordinary_scale_v1/SPLIT_FREEZE.json)
- [恢复后的实时进度](../../data_pipeline/ordinary_scale_v1/recovery_v1/PROGRESS.json)
- [首片验收](../../data_pipeline/ordinary_scale_v1/shards/shard_0000.audit.json)
- [数据分片目录](../../data_pipeline/ordinary_scale_v1/shards)
- [恢复限定与质量规则](../../data_pipeline/ordinary_scale_v1/recovery_v1/AMENDMENT_ZH.md)
- 监督器收口后写 data_pipeline/ordinary_scale_v1/recovery_v1/run/RESULT.json；不存在该文件时不能声称作业完成。

## 特殊数据：已修复元数据适用性，实际族仍待构造

主 agent 委派了独立CPU模块，已读取并接收代码/清单/质量分级，复跑17测试及13项交付哈希：
- 51 FIT屋仅读取 .house，43屋满足新声明角色组合；1,376个确定性语义候选（每屋32上限）。
- 8屋不适用留账；原5屋在新角色组合下均有候选，不把旧固定任务4/5拒绝改PASS。
- 与旧5屋runtime inventory逐对象raw/category/room差分一致；51资产哈希及全部候选回读通过。
- 新真实物理族0、新机制训练合格族0。候选不是独立物理族，同一屋的不同组合可能共享大量轨迹。

[特殊数据报告](../../data_pipeline/mechanism_scale_v1/REPORT_ZH.md)、[候选清单](../../data_pipeline/mechanism_scale_v1/acceptance_v1/candidates.json)、[质量分级](../../data_pipeline/mechanism_scale_v1/QUALITY_TIERS.json)。

接下来的特殊数据工程工作已明确，而不是再搜方法：
1. 将旧开环几何提案改为受真实状态反馈约束的合法去程；所有实际动作仍经统一记录器、预算与碰撞检查。先测物理合法性，不直接把1,376候选灌入旧失败生产器。
2. 校验实际执行逆路、两帧可观察见证、公共尾段和原1e-5有界汇合；不能teleport补造历史。
3. 形成3历史×3续接×2任务、27真实重放与54任务求值，通过因果导出及强状态监督标签。
4. 等长度/当前画面/query-only反捷径验收后才提升为MECHANISM_TRAIN_READY；先向300独立合格族、至少20屋推进，再按独立族学习曲线与产率决定正式更大配额。30族仅构造检查批次，不能作为最终论文数据规模。
5. 同时测内容存储逐帧全目录扫描与持久账本开销，改增量计数前先验证恢复/去重/失败一致性，不取消留证换速度。

首个新机制runtime须以这些CPU产物冻结具体配置/预算后由主agent准入；当前没有在后台运行机制仿真，不让用户误以为两条线都已大规模出货。

## 主线与权限

Qwen3.5-2B和MAINLINE_FREEZE_V3未变。普通示范训练通用导航；特殊数据通过真实交叉续接监督同一运行期执行记忆。自然任务与特殊任务分开报告，同数据强任务状态、action-only及通用未来预测对照均保留。

本节点授权数据生产工程，不授权Qwen训练、方法效能实验、环境改动或新下载。不把自动数据检查称为自然语言完整语义证明、CVPR贡献充分或泛化PASS。根项目其他线和旧封存未改。

