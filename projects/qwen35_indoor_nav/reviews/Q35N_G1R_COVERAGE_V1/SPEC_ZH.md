# G1R起点覆盖单项修订

依据用户明确要求补齐真实机制族，主agent批准本节点。旧G1F及数据/模型环境封存不动；不训练/下载，不改语言、类别、两帧256px、历史512/续接160、8步公共尾部、零碰撞及精确像素汇合。
只改候选外层遍历：tail外层、yaw中层、32个u最内层；最多256个候选，因此本版固定原第一tail FFFFFFFF、yaw0..7、全部32u。相较旧版本256配置只覆盖2个u，本版首32配置覆盖所有u；不是扩大旧失败节点预算。
继续使用原冻结greedy路径、witness排序、原始/压缩逆路线、中性padding和完整族预检。子路径及失败完全记账，不能把partial loop或pose近似通过算完整族。
第一个完整候选立即冻结；若出现候选，再按P2R1做独立27物理replay、54任务求值及完整数据验收。没有候选时不得运行下游或导出Y。
物理GPU改为3（已核实占位、借用后恢复），任务图像判据不变。G0R renderer来自同型号GPU2，迁移到3须本轮重新预览witness；不能直接拿GPU2的像素当GPU3证书。
上限：256候选、8GiB输出、16GiB RAM、8GiB GPU、1小时wall。完成/失败均清理并回主agent裁决；不能只因无正结果改阈值。
