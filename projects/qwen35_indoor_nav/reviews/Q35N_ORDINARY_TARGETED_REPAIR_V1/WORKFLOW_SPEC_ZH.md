# 两个串行有界检查

先完成已启动的固定 batch=1 纯模型复测并审计，生成新的配对报告；之后只运行一次冻结的视觉停滞保护对照。
CPU 单元测试、独立保护审核反例测试、所有源码锁通过才允许后者开始。GPU1 必须再次核实空闲。
不改变冻结的阈值/方向/上限，不挑掉失败样本，不复写旧结果，不训练或重新造数。

等待前置结果最多 4,200 秒；没有前置完整结果或发生失败则只记录停止，不操作前置进程、不自动重试。
保护组保持自己的 3,600 秒/500 动作/资源上限与独立进程组清理；监督层最多额外等待 5,520 秒，异常仅终止自己创建的 launcher，由其清理所属子进程。
同配对原本成功损失、新增成功、逐屋和 nDTW 同时报告。已注册的工程开发信号不代表创新、充分泛化或医疗机器人安全验收。
本次授权来源是用户「检查并做出针对性修改」，所有新实验均为普通基座工程诊断。
