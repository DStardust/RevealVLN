# V20：同指令终点与困难负例

保留V13为参考，640条真实训练轨迹提供401个实际成功STOP和31639个逐轨迹去重的范围外输入。新实现显式配对同历史下的真实终点与三个确定性困难位置：特征最相似、V13 STOP margin最高、距离边界最近。重复配对去重；未执行后缀不造标签，其他范围内中途输入不标负例。

32屋拟合一次固定凸目标：终点BCE、逐轨迹负例BCE、配对排序和逐输入V13继续决策保护，各权重1，原L2系数0.001。只更新2049个STOP参数，三种运动行、best4k及输入不改。8个历史诊断屋仅报告；没有截距校准、网格搜索或按DEV挑checkpoint。规则优化是工程假设，非论文创新结论。

GPU0/1做完整DEV100同进程配对；只有SR高于V13且SPL不降才继续一次完整公开unseen1839，随后停止。亏损也保留完整分母。最多24GPU会话小时，80GiB产物，后台独立运行。原18770网页同时显示本任务和100k记忆迁移测试。

运行：项目Python -I -S -B standalone.py start JOB -- 项目Python -I -S -B pipeline.py --run-id contrast_001 --resume。准备数据已完成；恢复使用相同run-id和新服务名，不覆盖旧尝试。

准备期间两处接口错误（旧common导入、COLLECT不含stopped字段）均在GPU启动/模型优化前修复，保留PREPARATION_SOURCE及日志；无新物理执行或效能结果。真正训练与评测由冻结SOURCE_LOCK后的流水线登记。
