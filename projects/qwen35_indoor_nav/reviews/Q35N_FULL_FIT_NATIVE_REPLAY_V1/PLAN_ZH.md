# 原入口完整FIT输入复现 V1

日期2026-09-14。普通历史配对R1已闭合负向（2帧SR10%、8帧12%，旧best21%）。
本节点只排除执行一致性风险，不训练、不拟合STOP、不构建新仿真轨迹、不声明导航改善。

假设：原FIT评测入口和原best4k权重，在固定batch1下，可重现全部5487种已存因果输入的
原四类logits。旧V12特征提取80个动作不一致、旧32输入缓存诊断FAIL、旧前缀诊断退出竞态
均保留；此前原前缀32输入数值一致仅是本次扩大覆盖的依据，不是全池通过。

输入固定为ordinary_stop_row_v12/POLICY_INPUTS.jsonl的全部5487行、原有顺序；
对应原64个FIT任务/16屋/7225原始出现，原指令、最近2个RGB、最近8个已执行动作。
只读CPU核对其与原FIT日志逐出现一致、像素hash/尺寸正确、与内部DEV屋不交叉。
原始目标/距离/房屋和logits仅由CPU准备/事后审计解析（冻结校验可读文件字节hash），策略仅接四字段白名单中的指令/
RGB/已执行动作；record_id只作对账，不编码。没有验证或测试监督。

逐字复用原ordinary_stop_calibration_fit_v2派生evaluate.py的完整模型加载、collate、forward
和32次接口前向，截断在创建windows/仿真之前，再遍历全部5487输入；原Window.item用于
恢复原图像接口。原模型/权重/FP32-BF16类型不改，原种子1209，原固定batch1不改。
不输出/保存新策略权重，不加载失败的8帧最终权重。

事前门槛：全5487输入完整；全部4类logits对原日志max_abs≤1e-5，全部argmax相同；
checkpoint和trainable fingerprint不变，0优化/反向/仿真；全部锁与清理通过。
没有中途选择、门槛放宽、删异常输入、重试或自动后续训练。任何偏差完整保存为FAIL，
它不能直接解释此前SR退化；通过也只证明该固定输入域可复现，不是泛化或40%达标。

仅GPU1实时空闲后一次有界检查，不借占位：1200秒启动总墙钟（worker最多1100秒，留100秒清理），5519次模型前向决策上限
（5487重放+32原接口），0反向/更新/仿真；模型25GiB、GPU总量28GiB、进程树RSS64GiB、
本节点输出1GiB。原FIT的7项autotune配置复制到本节点私有缓存，源缓存只读。
全部代码/资产/预算在启动前冻结；共享环境、旧源码/输入锁/权重/失败、生产与其他GPU不动。

复用经实测的owned_process_r1及R1训练监督器的只读进程树/身份信号函数，
只终止本节点精确识别的子进程；缺失瞬间不当成需重启，正常退出仍写最终回执。
资源超限先保存采样再停止自身。GPU检查结束后CPU独立比较logits，0额外模型前向。
