请审查 DStardust/RevealVLN 的 `codex/q35n-v15-legal-data-20260919` 分支，入口为：

`projects/qwen35_indoor_nav/reviews/Q35N_V15_HANDOFF_20260920/README_ZH.md`

先实际读取链接证据，列明读取路径与无法读取项，再决定下一步。冻结实验结果提交是 `62344f890d040032d5c9c8467133b1a9b376752b`；之后的交接补充没有新增训练或更改旧分数。不要只复述本 prompt 后声称已审计。

我需要的判断是：当前应优先补什么数据、修哪条学习/评测路径，还是已有证据支持更换模型/记忆架构？给一个最有判别力、成本可控且可被否定的下一实验，不再铺开大量研究方向。

必须承认的事实：

- 固定 best4k Qwen3.5-2B，BF16/原 attention 与 FLA 路径，有限记忆 8×64；本轮未训练底模。
- 6 个真实 debug 族、72 次完整交叉执行、20 条去重物理历史、1 FIT 屋 / 1 CHECK 屋。原 `training_admission=false` 保留；物理回放通过不自动构成正式研究数据准入。
- B1 动作监督、强 B2 精确组合式状态监督、Ours 交叉结果监督；1 族 / 3 族两档，3 个种子，18 模型、10,800 次更新。全部已完成，不能要求重做同一拟合来制造进展。
- 训练族教师分叉都能学会；正确/错误/sham 记忆对照有真实动作依赖。但所有臂、规模、种子的新屋教师分叉均 0/12。
- 216 次实际自主续接后，仅 B1 有 1 PASS；128 FAIL、87 UNKNOWN。UNKNOWN 由原检查器碰撞规则产生，未当负例或删除。不能以部分可知轨迹冒充完整成功率结论。
- 144 项组内前缀审计严格通过，原生 logits 差为 0；另一个层面的训练缓存与现场跨会话比较有 5 次 argmax 翻转。必须区分这两个数值问题。
- 本轮已有真正的长序列轻量记忆反传与参数更新，关键事件至监督点相隔 135 步。不能再将“实现真实反向”作为尚未完成的唯一任务。
- 现有 systemd 后台流水线已通过启动终端退出后持续执行的实际测试；后续长任务不依赖 Codex 在线，不把额度耗尽当停止实验的计划。

请特别审查这些竞争解释：

1. 数据覆盖与事件识别：只有一个训练屋，类别/房间/尺度与屋分布共同变化；B2 对没有发生的事件存在误报。是否应先补跨屋且语义组合匹配的正负例，而非换大模型？
2. 可辨识性与捷径：起始 RGB 与历史身份不同，所有任务又有通用成功后缀。现有正向拟合是否只支持实例记忆和有限集合效率，不足以支持语义执行记忆？
3. 动作分布和闭环恢复：教师分叉学习能否传到自主轨迹？现有共享轨迹池是否缺少合法偏离和恢复？如何只改一个变量来验证？
4. 评测定义：工厂为拒绝非法数据而设的碰撞 UNKNOWN 规则，是否适合自主导航的完整效果估计？如需修订，只能提出新的前瞻协议，保留本轮标签，不事后改成功条件制造收益。
5. 数值传输：固定缓存上的 CHECK 已失败，跨会话差异也确实存在。如何判断数值修复是必要前置、独立工程问题，还是不足以解释主要失效？不凭 FLA 猜测更换 kernel。
6. 最强简单替代：精确状态和后缀字段可以重建所有 Y；Ours 不比 B2 信息更多。何种额外证据才支持保留交叉监督的模型主张？如果 B2 同样有效且更简单，应允许优先采用它。

输出请包含：证据化诊断、一个下一实验的具体输入/数据族隔离/固定因素/唯一变化/强对照/分母/主要终点/停止条件，以及哪些结果会支持“补数据”、哪些才支持“改架构”。预算依据现有吞吐给出范围和未知项，不虚构所需时间；不要复活旧专用 GPU、4100 秒或必须 SR40 才能开发的限制。

不要承诺一定得到正结果、能发文章或达到 SR40；也不要把当前有限失败夸大为整个执行记忆方向已被否定。方向 B 小物体导航本轮不启动。回答应该能直接交回 Codex 形成单一、可独立后台执行的实现—测量任务。
