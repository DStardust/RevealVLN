# V12离线/实际前缀一致性诊断 V2

V1原缓存32输入仍max_abs0.488566失败，不能将自动调优差异归因为已经定位的原因。保留所有FAIL，不拟合STOP。

下一廉价区分：逐字节复用原FIT evaluate.py的模型加载/编码/前向和16单条+2个batch8接口预热前缀，截断在启动仿真之前。仅在预热前后插入固定32输入诊断（仍0..15加300..4800），每输入各一次正常forward和一次只读head hook的forward，共128+32=160前向决策。CPU对比PNG和原Window.frombytes的实际collate每个张量相等；同时校验原trainable fingerprint。原launcher环境白名单恢复，使用独立缓存内原FIT七配置副本，不改环境/源码/原缓存。

GPU1空闲确认，600秒、25GiB、16GiB RSS、1GiB输出；0仿真/训练/参数更新。报告预热前后差异、hook有无差异、原日志差异、原提取缓存差异，不按结果挑配置或模型。全日志max_abs<=1e-5和动作一致仍是恢复提取的门槛，未通过不能进入拟合。不是导航收益。
