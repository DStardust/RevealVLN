# 等价失败剪枝：不重置256候选预算

2026-09-09，尚无冻结候选。前三组完成的24个candidate均拒绝于K-loop，另有在途子路线。本节点发现旧执行器对已经在outbound出现的禁用事件仍回放长逆动作序列，属于可避免的重复计算。

本次只加入逻辑等价的提前拒绝：完整outbound已有forbidden SEE2，则任何延长历史仍然包含该事件，不可能满足原冻结的“不得形成该事件”。因此直接拒绝此view，省略注定无效的raw/compressed return replay。未知证据也不生成真值。没有修改类别、阈值、路线生成、有效候选的raw/compressed双重重放、汇合标准、候选顺序或冻结规则。

旧worker用SIGINT退出并清理，只作用于本节点精确PID；源文件原版、CODE_LOCK、日志、已执行轨迹和计数保留。新阶段从原DISCOVERY_ATTEMPTS末尾之后继续，已完成候选不重跑，最多累计256个唯一外层候选。在途但未完成的一个候选重新核验；其之前已执行的动作仍计入成本，不删除。该调整不是失败后换方法或再获得256次机会。

将报告这是一项确定性必要条件剪枝；它提高工程速度，不作为算法创新或导航收益。
