# 普通历史配对R1闭合：两组均未超过旧基座

2026-09-14 06:17 CST。两组训练、两次完整内部100条评测、自动比较及独立CPU审计均已结束。
本轮没有合格候选，不执行这两组权重的完整1839条评测，不采用失败权重为新基座。
SR≥40%的用户目标没有达成；旧best4k内部21%和旧41800完整1839条22.02%仍分别保留。

## 导航结果

以下三行都是同一组反复暴露的内部100条开发路线，不能冒充完整val_unseen或独立测试。
SPL、nDTW统一以0–100显示。

| 权重 | SR | SPL | nDTW | 曾进入目标范围OSR | 相对旧best配对赢/输 |
|---|---:|---:|---:|---:|---:|
| 旧best4k，保留 | 21% | 18.017 | 36.588 | 47% | — |
| 新2帧4000步，不采用 | 10% | 7.917 | 30.519 | 40% | 3 / 14 |
| 新8帧4000步，不采用 | 12% | 9.927 | 32.073 | 50% | 5 / 14 |

8帧相对本轮2帧为8赢6输，SR+2个百分点、SPL+2.010、nDTW+1.554，
通过了这一有限对照的事前平均开发门槛。但5屋bootstrap的SR差95%区间为
[-4.60,+7.48]个百分点；去掉两个不同的获益房屋时，SR差均可归零。
因此不能说历史输入带来了稳定收益，更不能以“12比10高”代替“超过旧21%”。

8帧相对旧best：SR-9个百分点、SPL-8.090、nDTW-4.515；
每个留一屋的SR差都为负。原比较器的完整基准候选为null，没有降低门槛。

8帧失败分解：12成功、38曾进入目标但未成功停止、29未到目标就停、21未到目标且用尽预算。
共70条主动STOP、21,780环境动作、10,381碰撞。OSR50%不是SR50%；
即使停车问题很突出，另外50条从未到目标，也不能把STOP当作唯一瓶颈。

## 实际验收与计算成本

两组各4000更新/384000普通训练决策、同顺序/初始化/优化器；旧64恢复或特殊训练均0。
各组另有12次容量诊断前向/3次反向/0更新，包含在训练启动墙钟内、单独记数量。
每次评测另有32次接口前向，非新环境动作。

| 项目 | GPU数 | 启动到结束墙钟 | 范围 |
|---|---:|---:|---|
| 2帧训练 | 3 | 5921.49秒 | 更新循环5723.33秒 |
| 8帧训练 | 3 | 9845.41秒 | 更新循环9646.45秒 |
| 2帧完整100条评测 | 1 | 2031.20秒 | 23191环境动作 |
| 8帧完整100条评测 | 1 | 2272.37秒 | 21780环境动作 |

以上按任务卡数乘启动墙钟合计约14.33 GPU小时，不是实测GPU利用率积分，也不是项目全部成本。
旧V1的4步/384决策内存失败另有三卡200.99秒；此前CPU准备、GPU接口诊断、借还/等待时间
均在各自旧回执中保留，不从总项目成本里删除。新8帧评测峰值GPU16506MiB、RSS39116312576字节。

8帧最终checkpoint SHA：7c594519a8d152561656376bf0eb7705c8a93910f49e6fe0ec0d6864c5de135e。
三rank全部exit0/无清理信号，FP32参数和AdamW矩有限、最终参数指纹一致，
主agent另以实际checkpoint重算指纹并与三rank精确匹配。
GPU3/4/5占位已恢复；GPU1评测正常exit0，实时NVML为空，worker和watcher均已自然退出。

`treatment_prefix8_RESULT.json`独立CPU审计通过：6048项文件锁、全部21780实际因果输入帧、
旧/新共200条官方SR/SPL重放、聚合nDTW/sDTW核对、checkpoint绑定、评测参数不变和清理。
这是重复读取既有轨迹，0新模型前向、0新更新、0新仿真，不是又测了200条新导航。
原评测聚合器承担逐轨迹exact-DTW检查；本次额外官方类重放并未再独立重算DTW距离矩阵。

回执`training_processes_unchanged=false`保留：其旧定义要求每一项前后都非null，
但本次评测前后6个训练rank均已结束、均为null，所以该字段为false。
实际仍存活训练进程身份不变字段为true、外部信号为空；不将这个空进程列表判据伪报成干扰训练。

## 下一步：先分清执行一致性和学习瓶颈

| 假设 | 目前裁决 | 下一最小区分 |
|---|---|---|
| 只加到8帧即可修好普通基座 | 本配方不支持；有限对照略好但未超过旧best且跨屋不稳 | 不延长本轮、不挑中间权重 |
| 算子缓存不同导致本轮退化 | 未证实；配置差异不是因果证据 | 原入口、原权重的完整已存FIT输入复现，不改训练参数 |
| 整个原评测都不可复现 | 也未证实；原入口32个跨全池位置已逐位重现旧logits | 将原入口验证扩至完整5487输入，0仿真/更新；先冻结范围和预算 |
| 可训练覆盖不足或普通监督难学 | 待区分；434180参数/18层linear attention冻结是事实，不是原因证明 | 原入口可用后，以FIT小集和固定检查集做可学性/输入监督对齐，再决定是否测试标准全层适配 |
| 现有数据量必须再扩产 | 不能由本轮支持；此前265万完整一轮也负向 | 只补诊断证实的普通数据缺口，不自动重启生产 |

旧V12提取器5487输入80个动作不一致的FAIL尚未解除。已核实其与原入口使用相同模型源码/
checkpoint、都用inference_mode；原前缀32输入则预热前后、hook有无、PNG/Window编码均一致，
并且准确重现旧输出。它支持优先统一原执行入口，不支持把旧V12偏差直接解释为本轮训练退化。
目前没有定位单一根因，没有启动新的STOP拟合、适配器训练、特殊训练或完整基准。

这份失败收口和下一步方向不是新GPU实验的运行锁。下一实际GPU检查需另行冻结精确源码、
输入、有限预算和进程清理；已关闭节点不重跑覆盖，40%与后续约60%的目标不缩小。
