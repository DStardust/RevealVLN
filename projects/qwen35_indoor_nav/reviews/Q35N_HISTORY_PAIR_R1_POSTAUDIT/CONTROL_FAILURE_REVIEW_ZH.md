# 普通2帧再训练：完整开发结果为负，8帧对照仍在途

## 已确认

2帧组固定完成4000更新/384000普通决策，三rank最终参数一致、参数与优化器均FP32有限、
预取线程join、三个训练根进程exit0。GPU训练占用墙钟5921.49秒，
其中实际更新循环5723.33秒；原先V1四步内存失败成本另外保留。
最终权重SHA：1f380ab88d68a9aa1b946773a8856fee25681847c827d0085fee73240fdce376。

固定内部100条完整结果：SR10%、SPL7.9170%、nDTW30.5186%、OSR40%。
对旧best4k：SR21%、SPL18.0170%、nDTW36.5880%、OSR47%。
配对3赢/14输；三指标开发门槛失败。23,191动作、10,913次碰撞，
30条曾进入目标范围但未成功STOP，另36条未到目标就停、24条未到目标且耗尽预算。
评测墙钟2031.20秒，GPU1进程exit0/清理通过，未停止训练或外部任务。

独立第三次只读审核已复算旧/新共200条官方成功率与SPL，
检查全部23,191个真实输入帧选择及原始RGB哈希，6048项源锁、匹配物理配置、
最终权重绑定和模型参数不变全部通过。该审核PASS不改写导航效能FAIL，
也不是额外200条新导航。

## 解释边界与下一步

| 假设 | 当前证据能支持什么 | 最小后续区分 |
|---|---|---|
| 仅追加当前2帧普通数据训练就能提高导航 | 本配方不支持，反而下降11个百分点；不能推广成所有普通数据无用 | 不采用此权重，不自动跑完整1839条 |
| 视觉历史不足是关键瓶颈 | 尚未判定 | 继续既定同数据/初始化/优化器的8帧组，固定最终4000步与完整100条 |
| 指标口径或漏路线导致假退化 | 当前证据不支持 | 全100条及官方指标已独立复核 |
| 算子自动调优差异导致分数变化 | 只读发现7个共同缓存键中4个按实际Triton规则推得的配置不同；没有同权重因果对照 | 如需归因，另冻结同checkpoint执行复现诊断；不据缓存差异解释全部退化，不改正在训练/待评测的冻结配置 |
| 学习率、矩重置、可训练范围或标签可观测性有问题 | 都没有被本次单独分离，不能只凭参数少就声称架构瓶颈 | 8帧结果之后，先做FIT范围内的最小训练可学性/输入标签对齐诊断，再决定训练范围或模块 |

8帧组仍从原best4k桥开始，不接着训练失败的2帧最终权重。
其第200步数值验收已通过，导航结果未知。
只有超过旧21%基座且满足原SPL/nDTW门槛的候选，才考虑单独冻结完整1839条预算。
阶段A目标仍为完整R2R-CE v1-3 val_unseen SR≥40%；本次未达成。
不引入特殊数据、不重启已关闭生产、不使用付费API、不把标准8帧历史写成原创贡献。

