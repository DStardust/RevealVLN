本轮结论：有效完成；STOPFIX主任务净退步，不采用。

768/768真实续接、128/128完整六模型组。源码锁、注册清单及封存组文件哈希已复核；384组前缀原生logits逐位一致，最大差0，无缺项。原MONOTONIC主任务66/192，STOPFIX62/192，差−2.08个百分点；task_T均98/192。主任务碰撞轨迹22→28，耗尽26→34；失败惩罚成本0.7115→0.7319（越低越好）。三个seed差值为−3、+1、−2个成功。

全部主任务成功变化为1赢5输。首次动作分歧逐条复核：唯一赢例是在真实状态满足时，STOPFIX将右转改为STOP。5个输例来自2个物理族，全部在真实状态已满足、原MONOTONIC准备STOP时，被残差改为继续转向；4例随后耗尽500步，另一例碰撞后才停。3个输例的历史状态预测低于0.5，另2个状态四位阈值判断已正确仍被读出否决。FIRST_DIVERGENCES.json包含全部证据索引。

后续建议：保留原MONOTONIC为工作基线。若先做快速工程对照，只测试一个明确变量：同一固定读出的STOP残差取非负值，避免它否决原MONOTONIC的STOP；不改原生Qwen STOP规则、动作语义、数据或训练预算。该做法仍可能新增过早停止，需要真实配对验证，不能将本轮输例离线删掉后宣称新SR。

更大的研究缺口：原MONOTONIC在missing历史层只有8/96成功，STOPFIX仍8/96；说明当前停止修补没有解决完成缺失前置事件的闭环能力。下一项数据工作应优先审计FIT中这类可恢复轨迹与任务一致动作监督，再补合法实际执行的覆盖，而非先扩大模型或盲目增加同型样本。已满足时仍继续的教师动作不必然非法，但不应把合法早停统一理解为任务错误。有关原因仍是待验证解释，不是已证明的数据或容量定论。

快速STOP保护即使得到小幅DEV收益也只是工程修补；尚不能当论文核心贡献或普通VLN-CE收益。当前只有一个已暴露DEV屋，不能外推稳定泛化。评测GPU会话3.05888小时；0、1卡释放，2–7卡占位已恢复，结果已发布f2f0b5ac。本次仅只读检查，没有启动新训练或导航。
