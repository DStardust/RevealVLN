# 冻结特征的Triton配置一致性诊断

V12原5487特征与旧FIT日志max_abs0.762128、80动作不同；禁止拟合，失败保留。CPU只读发现同6个autotune键中4个最快配置不同；其余相同。Triton源码按保存timings最小项选择配置，FLA默认将其缓存。此为候选原因，不提前归因。

最便宜检验：同V12冻结模型/提取代码/输入，无预热或像素改变，仅将原FIT的7个.autotune.json逐字节复制到本诊断新可写缓存，原缓存只读不动。固定32个索引：0..15及300,600,...,4800；不按已见偏差大小挑选。32前向，GPU1确认空闲、300秒/25GiB/16GiB RSS/1GiB输出；无仿真/优化，不释放其他占位。对旧FIT日志仍max_abs<=1e-5且argmax全部相同，否则不作修复通过。

保存原/新配置对照和7文件SHA，运行前冻结；运行后旧复制的7文件不得被改写、记录额外autotune键。若通过，另版本化同5487特征全量复核；此诊断不是导航正向结果，不扫内核/不以分数挑配置。
