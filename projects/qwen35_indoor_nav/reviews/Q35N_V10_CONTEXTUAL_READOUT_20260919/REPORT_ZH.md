TRAINING_COMPLETE_NAVIGATION_RUNNING_EFFECT_UNKNOWN

V9三种子完整闭环不采用。V10真实GPU轻量训练与12份权重读回已完成，五臂三种子导航正在运行，尚无导航收益结论。

修复对象是动作读出：将当前2048维因果特征与原512维记忆输入同一128宽MLP，末层零初始化。N0将记忆输入置零；B1/B2/Ours读取真实记忆。四臂使用相同初始化、原V9采样序列、轨迹池、600更新和三个种子；保留原损失、编码器、输入、STOP保护和成功定义。N0的记忆写入/递归参数不应更新。

CPU已完成5项真实缓存前反向/梯度测试，闭环编排9项测试。训练与闭环源文件在启动前分别复核22/33个冻结指纹。实际完成7200次轻量更新；复用已审计因果缓存，新Qwen特征前向0、底模更新0。

全部12份固定最终权重读回验证通过；已按预先固定三种子、每种子100组、原生+B2+Ours+B1+N0五臂启动导航。完整计划1500次episode，完成数量以逐组日志为准。只有真实配对SR和对照能支持收益；N0改善不代表记忆或交叉监督贡献。

协议与入口：research/continuation_memory_v1/contextual_readout_v10/{PROTOCOL.json,launch.py,run.py,train.py,model.py}；closed_loop_bench/ordinary_memory_transfer_v10/{PROTOCOL.json,launch.py,evaluate.py}。原始SEE2资产准入限制、数据已暴露状态及旧失败记录不变。

实际训练耗时0.557490 GPU小时，正常清理、未向外部进程发信号。B2/Ours在GPU上的关键写入到监督点有211或310步间隔的非零辅助梯度；N0写入与递归参数未更新。梯度可达不等于已学会保留有效信息。

|对照|自然CHECK动作准确率（三种子均值）|动作CE|
|---|---:|---:|
|N0|76.0945%|0.578511|
|B1|76.0679%|0.570791|
|B2|75.9353%|0.568526|
|Ours|76.0679%|0.570375|

原生准确率72.1942%。N0与记忆臂基本相当，不支持记忆增量。SEE2的48个CHECK历史相反标签对，B2三个种子双方正确数10/10/8，Ours为0/0/11；这些结果没有稳定的交叉监督优势，原始数据准入限制继续保留。不会据此筛选种子、重选检查点或取消既定闭环清单。

首5组/25次episode已封存：原生2/5、B2 0/5、Ours2/5、B1 0/5、N0 0/5。Ours对原生1胜1负（恢复1763、丢失8981），不能宣称SR净收益；SPL略升、nDTW下降。所有10个比较前缀logits逐位一致、参数前后未变。保持完整三种子清单继续，不按这5条选方法或种子。

首20组/100次episode已封存：原生4/20、B2 3/20、Ours4/20、B1 4/20、N0 1/20。Ours相对原生SR持平，SPL0.18428高于0.15859、动作2561少于3324、碰撞1214少于1496，但nDTW0.41524低于0.46126。不称全面提升，仍需完整100组及其余两个种子。所有比较前缀logits逐位一致、参数未变。

CPU只读记忆敏感性测量已完成：12份固定最终权重、全部60条CHECK教师轨迹/3769决策，原生STOP仍保留；参数前后未变、0更新、0新导航、22.998秒。Ours三种子平均动作准确率：完整记忆75.9972%、清零76.1210%、FIT轨迹均值记忆76.2183%。清零分别改变104/150/144个动作，说明动作读出依赖记忆，但没有在该离线指标上证明动态记忆有增量。N0三种输入输出逐位一致，CPU原始动作正确数与既有GPU记录全部一致。固定记忆仅由508条FIT轨迹的各轨迹记忆均值再取平均计算，不用标签；清零/固定记忆均不是已核验任务状态的供体对照，不能据此证明语义变量或直接归因闭环成败。证据位于contextual_readout_v10/{diagnose_memory.py,MEMORY_DIAGNOSIS.json}，不作为新候选策略送入仿真。

首次基础设施中断发生在种子1209第28组reset：B2上一组第49步结束，其他臂运行到500步后还需CPU审计，触发继承的180秒空闲读超时。已有27个完整组/135次episode封存，参数未变；没有资源超限或外部任务采样，耗时2984.520秒。按INFRA_REPAIR_001修复为环境服务等待新请求不设空闲时限，父进程单次响应180秒和launcher900秒无进展监控保留；断管关闭不再遮蔽原始错误。12项CPU验收通过，其中实际socket等待/EOF、断管清理和执行器除传输部分外与V5逐字一致。原PROTOCOL与失败session只读保留，新session记录修订及有效源指纹，从第28组重开五臂；不重选模型、不改变输入、动作、STOP、评测或顺序，不拼接半组。本次为第1次基础设施恢复。

用户要求立即推进有证据支撑的论文候选后，新增了固定设置的CPU读出探针，9000次诊断头更新、22.365秒、原运行策略参数不变。Ours新结果读出头的FIT历史对双方正确数从10/0/13升至50/49/48（52对），CHECK从0/0/11升至9/11/12（48对），但CHECK全部标签正确数从143/144/136降至124/110/118（216个）。这不是正向方法收益；它表明训练拟合存在空间，但直接重拟合读出器不能解决泛化。完整证据为contextual_readout_v10/READOUT_PROBE_RESULT.json。后续query_semantics_v11仅修复训练查询语义表示，单独CPU运行匹配B2/Ours；V10固定闭环继续完成，不被替换或删去负结果。

种子1209完整100组/500次episode已经封存：A原生21%、B2 14%、Ours10%、B1 18%、N0 10%；各臂决策数18596/16285/15939/15765/18024，总84609。全部前缀logits逐位一致，运行参数不变。该种子含一次已解释的传输恢复，总GPU占用3.095740小时。独立CPU指标/轨迹读回已启动，完成证据写入SEED_REVIEW_1209.json；其余两个固定种子继续，不能用这一种子冒充三种子结论。Ours相对原生为负11个百分点，本版没有已观察到的可采用或论文正向证据。
