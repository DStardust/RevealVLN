# 真实事件约束续接搜索 V1

NUMERICAL_JOIN_V1的公共尾部/汇合已可构造，但现有greedy到床路径全部额外触发D，故C0失败。保留16个失败。本版只替换离线续接规划：按真实观测约束搜索F/L/R，不改变任务程序、标签、阈值或动作预算。继承独立数值共同状态协议，原始精确汇合仍false。

- 候选固定已诊断u_index22/yaw0，public_tail=LRLRLRLR。历史使用前节点RAW_BASE_0登记的动作，不把缓存trace当本次验收；本轮完整重放。没有新训练/导航结果选择，这是旧暴露接口族的目的性数据构造。
- C0：禁止D/K，首次实际B两帧证据后STOP。C_D：禁止K，先实际D后B立即STOP。C_K：禁止D，先实际K后B立即STOP。
- 加权best-first搜索，节点包含实际pose、当前eligible实例像素集合及已发生anchor状态；每个分支用官方set_state恢复**规划分支状态**后执行一个真实F/L/R并渲染，禁止把这些分支初始化冒充完整轨迹。
- 去重key采用0.125m空间量化、15°yaw、当前可见eligible集合、anchor状态；它是搜索裁剪启发式，不宣称完备性/最优性。展开顺序F/L/R，优先级g+3*欧氏目标距离/0.25；目标为下一所需类别的已有合法viewpoint，实际停止判据仍SEE2，不是距离。
- 每个续接最多12000展开、160动作含STOP；全节点1小时、8GiB输出、16GiB RAM/8GiB GPU。记录真实分支动作/渲染/碰撞/事件裁剪计数，最终搜索路径必须从s独立完整实际重放并满足同谓词，不能拼接分支帧。
- 三个后缀找到后，重新执行9个完整history+continuation，独立求18格真值/同query关系才冻结第一个且唯一候选。之后27/54独立certification，失败不换候选。
- GPU3借用恢复，无下载/模型/训练。后续若通过只说明本小族编译可运行；搜索工具本身不新增论文创新主张。
