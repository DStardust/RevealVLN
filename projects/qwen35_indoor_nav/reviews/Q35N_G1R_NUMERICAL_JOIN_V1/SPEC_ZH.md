# G1R 数值共同状态重建 V1：显式协议修订

## 裁决与继承边界

主 agent依据用户补齐机制族授权及MAINLINE_FREEZE_V3第4.1节批准本独立工程/正确性修订。它实现V3已允许的共同合法状态重建，不改变交叉续接学习算法，不重开一般文献搜索。原G1F/G1R原始精确汇合失败永久保留。本节点即使成功，也只能称**数值规范化共同状态下的真实历史族**，不能称未经校正的原始轨迹像素自然完全相同。

前置证据：EVENT_CONSTRAINT_CORRECTION 的两组histories均满足事件、等长、零碰撞、1e-4几何阈值，但u间位置差1.7624e-6 m/旋转差约2.4e-7 rad导致RGB逐字节不同。不能把这些原始哈希改成相等，也不能把它判成算法正收益。

## 冻结数值操作

- 仍只检查原u_index22/yaw0、1；原8tails及greedycontinuations顺序不变，最多2base/16完整family尝试。
- 原始历史全部逐动作真实执行，无中间重定位。仅在回到u、开始8步共同尾部前允许一次明确登记的数值共同状态重建；不新增任务事件、不跳过任何运动、不改变动作预算/事件步号。
- 共同target取这条历史开始时已合法的同一u pose，不依据未来标签选择位置。原始到达与target的agent/sensor位置和旋转修正分别必须<=1e-5 m/rad，比原1e-4几何到达阈值严格10倍。超出立即拒绝，不能加大容忍或寻找远处目标。
- 用官方Agent.set_state重建共同pose并立即真实渲染。记录原始和重建后pose、RGB/semantic内容哈希及原始数组，明确标注normalization boundary。原始与重建后该时刻的四类SEE2实例集合必须完全一致；任一事件变化拒绝。
- 重建后的u/s poses/sensor/RGB/semantic必须精确跨历史共享（几何沿用<=1e-4，哈希严格相等）；随后8个共同动作和所有continuation完全真实执行，不再修正。
- 不把raw记录覆盖为canonical记录：完整trace含normalization_events，raw和canonical分别保存。history输入采用登记后的实际重渲染观测；这是定义清晰的离线干预数据，不冒充无干预自然采集数据。模型不接收pose、校正幅度或normalization标记。
- 最终任务真值从原始历史事件及真实共同尾部/续接重算。强制边界事件不变，所有3x3x2预期关系保持，三值/非法样本规则不改。

## 验收与主张

第一个完整候选冻结；独立27物理重放/54任务求值、原始像素复核、数值操作审计、两级schema/crosschecks全过才导出18个interface_only cell。原始精确汇合为false、规范化精确汇合单列true/false。失败不能回退到第二候选。该族不证明一般自然历史的构造率、泛化、导航收益或创新贡献成立。

预算1h wall、8GiB输出/16GiB RAM/8GiB GPU，GPU3借用恢复；无下载/模型/训练。规划缓存只读复用同GPU3旧记录，完整历史/续接仍本轮实际重放。正式验收另有1h/8GiB上限。
