# 首批生产恢复 V2：逐路线严格隔离

首个运行在50个终态候选后的整片审核中失败，GPU已清理；49个回放认证不等于49个导出合格。10个RxR记录首次转向出现竖直位移，包含约1.67e-6m数值变化、0.0104m及0.2107m实际跳变。原1e-6转向位移门槛一律不变，不校正起点/放宽门槛。

改动仅为审核失败的路线逐例 quarantine 而非让整片终止；所有导出结构/像素/路径检查复用未改的V1 audit。AssertionError留完整栈并隔离，其他基础设施异常仍终止。空片可以发布但不能声称含合格数据。加入隔离用例测试。

输入是同一冻结1000任务；已结束50个任务只读回审，余950执行，不更换候选、不重做旧失败。旧代码、输入锁、原SUPERVISOR RESULT及PROGRESS不改；新代码和进度放recovery_v1。允许向原批次新增未尝试route目录、append账本和此前不存在的shard索引，以复用内容存储；该范围是明确的恢复追加，不覆盖旧文件。每片先写quarantine清单再发布仅合格索引。

总资源仍原4小时/200GiB，扣除已运行138.22秒；本恢复至多14250秒。GPU2仍仅用空闲，不释放任何占位，不训练。记录所有不合格原因及来源产率，后续源起点/导航mesh兼容问题另开版本，不悄悄修起点后称同一示范。

