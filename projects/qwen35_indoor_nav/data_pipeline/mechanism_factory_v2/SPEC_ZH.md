# 通用机制构造器 V2：CPU 实现节点

用户“实现下一步”授权本节点实现与CPU验证；沿用上一轮多线分工，不启动仿真、模型、GPU或训练，不读取外部SFT结果。所有新代码/测试/报告位于本目录；旧封存代码和数据只读。

目标是移除单屋、单起点、角色字母和可变全局依赖，不改变MAINLINE_FREEZE_V3任务语义或上一轮P0协议。一次有界数值共同状态重建、256像素同实例两帧、2任务×3历史×3续接、27真实复放仍为未来运行要求。当前CPU重算旧日志不算新物理族。

实现分工：主agent负责backend无关的构造调度/动作回放接口、整体验收和状态；委派纯任务编译器与测试；委派候选/预算/失败账本与几何聚类及测试。各自不改他人文件、根状态或旧节点。

模块契约：

- compiler.py：实例配置roles、tasks、eligible；独立像素事件、完整性/unknown、任务检查、强M2、保序query与policy白名单。无全局可变任务配置。沿用旧标签语义并在27×2旧日志上差分；通用角色单测不冒充新房屋运行。
- planning.py：固定已登记20候选、首批5；路线前8不同位置→tail/yaw/u不超过64；规范house/config/roles缓存键；有界动作/时间账本和冻结后不替换；几何连通聚类与去重。实际物理预算在backend调用前计账，资源截断非负例。
- factory.py：显式注入backend，候选迭代、真实trace要求和完整家族预检；使用fake backend的控制流测试必须标为合成工程夹具。Habitat生产适配器若未实际运行，不宣称runtime可用；CLI默认仅CPU prepare/verify，generate/certify无新运行授权必须拒绝。

验收：CPU测试包含unknown、非法STOP/时间、实例混淆、角色重命名/配置隔离、候选顺序、预算边界/重试不清零、冻结禁止替换、几何去重、policy泄漏、历史与续接组合关系。差分旧18cell与27trace；完整源码/输入hash、失败测试与修正留账，运行结束统一封存。测试通过不是科学收益，不新增训练准入。

退出：交付可复用CPU实现并列明真实Habitat适配/5候选造数的未验部分。不得为了宣布完成而把接口stub当已可生产；也不自动修改SFT或正式标签以追求通过。
