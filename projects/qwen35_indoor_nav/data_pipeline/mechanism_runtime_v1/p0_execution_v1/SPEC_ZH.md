# 五个固定候选 P0：实际生成节点

用户“执行下一步”批准一次五候选构造诊断。完整定义继承已封存的 multifamily_plan/protocol.json、runtime_v1/NEXT_P0_ZH.md；不改方法、事件阈值、场景划分、角色组合或候选顺序。

只在本子目录保存新增执行记录，既有封存文件只读。使用既有已测试的 p0_driver_v1，启动前核验源码/资产/解释器、重跑其32项CPU测试、冻结完整配置、复核GPU2状态；无需重装或重跑旧smoke。外部SFT不读取、不写入、不干扰。

吞吐决策：旧9轨迹含导出回读约424秒，不能视为27轨迹可在480秒认证。本次不扩大或重置预算；发现/认证分别计时，未完成明确RESOURCE_CENSORED。该批只回答固定候选在当前工程预算下的可构造性，不估计总体任务比例，更不否证记忆学习。

执行上限：5屋各一个bundle，总6000秒/200000动作；发现720秒/20000动作，冻结后认证480秒/20000动作；12GiB内容、14GiB停止高水位、16GiB绝对磁盘、16GiB RAM、8GiB整卡显存。保留全部失败和未开始项，首个完整preflight冻结后不得替换。完成五候选或触发总停止条件后结束，不自动P1。

输出认证族须通过3种seed、27轨迹/54求值、完整导出回读、几何去重及反捷径后续审计。未认证轨迹只保留诊断，不能投入训练。执行结束核查自身进程与GPU清理、原封存哈希、数据分组和实际计数，生成中文报告与机器裁决。
