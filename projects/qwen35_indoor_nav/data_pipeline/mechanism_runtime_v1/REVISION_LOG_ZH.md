# 实现与审核记录

1. 三个互不重叠的 CPU 模块由延续授权的并行代理完成：Habitat backend、V4 导出/loader、内容存储/资源检查。主 agent 负责集成、资源进程管理与最终裁决，不声称独立人工评审。
2. CPU 验收前修复 loader 构造缺 compiler 参数，并增加全量前缀/动作 RGB 回读与监督独立重算；修复监督器只看父进程退出而遗漏同 session 后代的清理问题，增加回归测试。
3. cpu_v1 的 58 测试通过后，增加本地 CUDA/GL 缓存目录，重新运行 cpu_v2 的 58 测试并按最终代码授权 smoke。此前一次用隔离模式直接启动 test_guard.py 的 import-path 错误不构成机制失败；最终测试入口显式设置源码路径。
4. smoke_v1 是一次实际 GPU2 运行，未修改已锁执行源码；九轨迹与旧数据逐帧一致，真实 2877 次运动动作，GPU 无自身残留。
5. P0 代码另放 p0_driver_v1，未运行 GPU。审核补齐父进程的完整代码/配置/解释器准入、结束采样、异常清理状态、session+supervisor RAM、内容子预算和退出空间；物理认证结果在导出前另存，异常保存预算与冻结账本。相关 CPU 回归结果单列，不能算作 P0 实际验证。
6. 本轮按生产接口验收节点收口。P0 候选坐标和资产已解析，但保持草案，不把尚未运行的调度器标成实际扩量成功。
