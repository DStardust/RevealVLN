# 短续接的已完成地标重访控制 V2

revisit_v1在8条完整真实轨迹/1730动作后被原QUERY_LENGTH拒收；三历史动作计数与闭合已通过，0族，不覆盖。C_A原130动作编码210项，超160项。只在新版本缩短续接中的A回路，不改变三历史、任务、可见性、汇合、查询编码和160项门槛。

从已保存实际轨迹定位连续实例A的两帧，保护对应动作边界后分段消除冗余转向。CPU仅提供动作提案，不声称新帧、事件或物理合法性。新动作必须通过原18格预检、冻结、27实际重放/54求值与原V4导出/回读。一次选择CPU候选中最短的第一条；其余候选不自动替换。

本节点仍是completed_subgoal_revisit_placement_not_event_free_detour，不能混报无事件绕行；三历史原动作计数完全一致。修复此前潜伏的导出调用接口：split使用原导出器允许的candidate_fit_pool，不使用FIT；house FIT属性不变。物理证书先持久化，再导出，避免导出错误丢失已完成回放结论。原共享代码只读，所有差异显式适配并输入锁定。

仅GPU2，监督3000秒、累计30000动作、总输出8GiB（继承7GiB提前停止）、content6GiB、RAM8GiB、仅清自身。GPU1跨屋scout及GPU5普通生产互不干扰。不借/停外部进程，不训练。不将首个局部合格族当多屋稳定或模型增益。
