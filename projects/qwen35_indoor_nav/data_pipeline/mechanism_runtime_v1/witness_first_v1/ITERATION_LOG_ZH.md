# 特殊数据持续迭代记录

用户要求：稳定生成目标特殊样本之前持续推进，普通生产后台继续。所有尝试独立版本，不覆盖失败，不训练，不声称数据工程成功就是模型/论文收益。

| 节点 | 实际结果/范围 | 后续归因 |
|---|---|---|
| feedback_generation_v1 | 3候选0族，141保存trace/14205动作；2资源截断+1无目标配置耗尽 | 共同可见的预定角色难以独立经历；冗余逆回路与日志成本 |
| compact_loop_v2/recovery_v1 | 3候选0族，229trace/12804动作，0碰撞；1资源截断、1配置耗尽、1无全角色可达源点 | 动作能合法闭合但B常见A；误写的NO_SOURCE_COORDINATE_ON_NAVMESH实际表示四角色联合可达筛选为空，不是没有navmesh |
| compatibility_cpu | 固定370真实trace，679前缀兼容程序；7套三闭合组件均仅1屋2起点，I无F | 实际证据可挖出A/B，但纯转向I不冒充移动绕行 |
| assembly_v1 | 固定来源index4，8种短中性移动全部触发A或B，384动作/11trace/0族 | 禁止把这些移动称event-free；旧结果保留 |
| revisit_v1 | GPU2实际运行，固定相同组件；改为已完成地标重访的位置不变性，精确匹配F/L/R | 单独控制类型，独立重放；不是原中性回路成功；等待全矩阵/认证 |
| scout_v1 | 跨屋真实witness-bank在实现准备，未准入运行 | 先真实回路后选兼容事件；不是只在单屋换角色凑数据量 |

17项bank接口CPU测试、15项真实Journal/中断trace测试经主agent复跑通过。新预算提交减少一次同步提交，但没有整体加速实测。原V3主线不变；同组工作、UAD/EgoCoT没有新数据复用。

动态终态优先于本表；已完成节点不自动续跑或延长预算。下一批每次固定配置与预算后执行。初步稳定工程门槛正在CPU细化：必须跨屋、跨事前固定批次、不同物理族，不能拿重复seed、同hub换角色或接口族凑数量。

## 后续实际迭代（2026-09-09 21:48）

- revisit_v1：8实际trace/1730动作，三history计数与闭合通过，矩阵QUERY_LENGTH失败，0族。原130动作C_A编码210项，不能放宽原160上限。
- short_revisit_v2：仅a续接116→34，history不动；8实际trace/1648动作后MATRIX_OUTCOME_MISMATCH。末R13改为L11虽终姿同，却看到B；原失败保留，不把几何最短当语义等价。
- short_revisit_v3：同一预先CPU列表第二候选，a续接36动作，只消相邻相反转向、不换绕行方向；已通过完整18格预检并冻结，三seed实播认证仍进行。尚未出最终族质量PASS。
- scout_v1：GPU1实际采集3FIT屋；前2屋closed且均产出组件兼容候选，第三屋进行尾部采集。候选不是整族证书。已解决旧固定4角色/单屋纯转向I的组件供给瓶颈。
- batch_plan_v1：精确F/L/R计数拼接CPU规划，V1/V2全部保留。第二屋部分F数匹配却净转数+24/-24不匹配，单靠LR对不可能补齐。正在新winding版本提出整圈填充；整圈必须实际证实不引入A/B，失败即拒，不改最终计数/物理/语义checker。
- 普通recovery_v4：1000自然收口，960 replay-certified，其中770严格合格、190隔离；另40失败。严格2307指令/167870决策，GPU5占位PID3133144已独立核实恢复。当前无普通活跃worker。

初步跨屋生产门槛现冻结于quality_cpu/batch_acceptance_v1/SPEC.json：6个合格独立hub、至少3FIT屋、至少两个事前冻结批次，每批至少3独立hub尝试且至少2合格；1m去重，不将seed/角色变化凑独立量。只用于初步工程重复生产，不是统计稳定、导航泛化或论文收益。

21:53 更新：short_revisit_v3已完成42总trace/11352动作/0碰撞并正确导出；独立quality_cpu/batch_acceptance_v1/v3_final_audit/FAMILY_REPORT.json为quality_pass=true，原27认证回放/54求值/18格、三history F16/L116/R116、4组历史反转、6组任务差异、强M2重算与policy输入隔离均通过。**新增合格控制族1，跨屋生产稳定尚未成立**。scout_v1已3屋/262完整trace/121个闭合紧凑回路/160组件候选收口；GPU1释放后按已冻结独立片scout_next_v1/shard_0继续采集另外3FIT屋，未启动shard_1。
