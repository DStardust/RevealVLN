# 特殊数据多样性 V1：受控语言与真实移动公共尾段

2026-09-11。用户已授权本会话接替 OpenCode 实现多样性调整，并要求先生成、审核及实际看过新数据图，再更新教师讲稿。无新增收费模型 API；不修改任何旧源码、配置、输入锁、失败记录或现有训练。

## 本轮可检验范围

保留现有 see2 程序及其 256 像素／同实例连续两帧判据。实现五种有严格反向解析的表达；同族所有表达保持同一场景分组、总采样权重不变，不将改写计为独立物理族。首次只为每个新物理族选择一个表达用于真实认证，其他表达为可验证提案，不直接加入训练。

保留原始三个等 F/L/R 计数历史及其已完成子目标回访位置对照。将固定摆头尾段改为原冻结验证器已经支持的 FFFFFFFF：连续八次实际前进（名义 2 米）。历史前后增加同一朝向变换，仍真实闭合到初始规范状态。公共移动必须无碰撞、无 anchor_A/anchor_B/terminal 事件，实际位移 1.95–2.05 米，最近两帧与八个执行动作族内完全一致。

从新的决策位置实际走回原组件起点，再执行原续接动作，完整重算标签和动作监督；返回是物理运动，不是 teleport。该返回段属于受控构造路径，不宣传为最优路线或自然家庭任务分布。每族仍 3 历史 × 3 续接 × 2 任务，额外 27 次／3 种种子重放和 54 次求值，强 M2、所有内容哈希和政策输入隔离不变。

## 事前选择和预算

候选只来自 2026-09-11 已强审的 178 个族，作为合法组件来源，不冒充新增房屋。首先按原房屋合格族数量升序，然后按历史长度、续接长度和 ID 确定性选择，每屋至多一源，共六屋。要求原历史不超过 440 步、最长续接不超过 112 步。初批仅扩展这些房屋中的实际决策位置和路线，不声称扩大到未见场景。

每源固定四个朝向候选：原朝向加 0、6、12、18 个 15 度单位。先检查移动公共段，再重放历史及矩阵；失败原因完整保留。每源按事前顺序至多接受第一个合格配置，不根据报告用图是否好看替换物理失败。图的可读性筛选另行记录，不能改变数据合格分母。

GPU1，UUID GPU-734a5268-31fe-6452-105b-36cd08c3d9c8，仅在无现存 worker／占位且空闲时启动；不操作 GPU0、2、3–7。使用与 GPU1 既有队列相同的 per-GPU 锁。总 60000 动作／3600 工厂秒／3900 supervisor 秒；每源 discovery 15000 动作／1000 秒、certification 20000 动作／1500 秒；内容 6 GiB、总盘 7 GiB、RAM 8 GiB。活动显存保守计量沿已有独立修订；idle/final 保留原 guard，原 XML 与判定分开保存。没有自动重试或无限延长。训练准入仍为 false。

## 验收与边界

CPU：五表达和反解析等价；原类别、raw 子类、房型、顺序及停止语义无改变；错误词序／省略连续条件／新增方位／调换角色均拒绝；新路径计数、动作上限与 source hash 检查。真实：自然闭合、完整重放、资源清理，再调用已有只读强审核核心，新增语言与位移核验作为额外门槛。旧 FAIL 不改。

本批至少两个不同房屋合格才记为小批构造验收通过；少于两个则明确不足，不用单图冒充稳定量产。全部通过也不表示导航收益、自然指令迁移或医疗服务能力。

顺序到访／返回任务是下一独立语义版本：看见、进入房间、到达物体附近不是同一谓词。本轮可形成规格和 CPU 检查器，但没有对应新程序的真实重放证书前，不放入训练或汇报成果。43 屋新 hub scout 仍按独立采集准入推进，不将本批旧屋中新位置算成新房屋。
