# Scout 与普通生产只读收口核验

最终快照：`snapshot_20260909T134809741519Z.json`。此监测未修改活跃/封存源码、配置、数据或STATUS，未停止任何进程。`snapshot.py` 可重复只读审查，新增时间戳快照不覆盖旧证据。

普通1000/1000固定路线全部唯一终态、与JOBS集合精确一致；20个shard索引哈希、逐片条目/决策计数、跨片route互斥、policy_file唯一性、隔离互斥全部通过。960条replay-certified中，770条严格通过、190条严格隔离；另40条非认证终态。严格训练索引含2307条指令记录、167870条指令条件决策；R2R1458指令、RxR849指令。190隔离均RxR，全部保留原turn位移 delta<1e-6 断言失败，不放宽阈值，不混入严格索引。

普通监督returncode=0、error=null。GPU5占位恢复PID3133144；监测独立核验了/proc命令、cwd、tmux pane %150身份，以及GPU5 XML里的该PID 29334MiB显存，与原占位相符。未手动恢复或操作GPU。

Scout正常收口，监督1205.51秒、worker1202.81秒、13010真实动作。262/262完整motion trace，0部分轨迹、0碰撞；128/128 outbound完成，121/128紧凑往返全pose闭合（94.53125%），7条非闭合不用于closed anchor。127个闭合trace还包括6条公共尾，不应混同128次compact的闭合率。

| 房屋 | 完整trace | 动作 | hub候选数 | 组件候选总数 |
| --- | ---: | ---: | --- | ---: |
| 17DRP5sb8fy | 98 | 4320 | 32 / 32 | 64 |
| 1LXtFkjw3qL | 66 | 4490 | 0 / 32 | 32 |
| 1pXnuDYAj8r | 98 | 4200 | 32 / 32 | 64 |

三个已选FIT屋均有候选，5/6 hub可提出组合；160返回候选均all_components_stored=true且干扰闭合回路含至少2个F。保留1LXt第一hub无互斥闭合A/B对的零候选结果。该运行证明几何筛选与实际witness-bank在这3个已选屋上能产出组件，不证明跨新屋泛化、不证明组合拼接完整物理证书、不证明导航效能或训练收益。新增可训练物理族仍为0。

Scout监督cleanup_complete=true、error=null、returncode=0；GPU1最终仅原外部650/246/246MiB context，未停止任何外部进程、无占位需要恢复。最终生成过程未触发预算或未知异常；尚未见需通过放宽质量阈值处理的吞吐瓶颈。

下一步仅由主agent按独立协议对候选做相同F/L/R动作计数、公共末端/短窗口一致性与18-cell/27-replay完整认证；另行准备的六屋扩展不得把配置或候选当已生成数据。
