# 已完成地标重访控制：真实拼接 V1

前节点assembly_v1的8种短中性往返全部实际闭合但触发A或B，0族，旧失败保留。不能把这些回路当无事件中性控制。

本节点明确改用“已完成地标重访的放置位置”作为不改变残余任务的移动控制，**不声称是完全不涉及目标的绕行**。对于固定first-see-anchor-then-current-terminal-STOP程序，A已被观察后再次观察A且不观察B、没有STOP，不改变任何任务已完成anchor状态；最终仍必须由真实18格Y核实等价，不仅凭文字推断。

沿用同一冻结组件提案index4，不换房屋或角色。令i=a+旧纯转向I尾，但i现在包含真实前进和回返；实测必须看到A和I、不见B/T且闭合。H_A=i+a、H_A_I=a+i、H_B=b+b，三序列不同。三者前进数和净转向必须相同，再由公共起点LR对补齐L/R，各动作计数精确相同且总长<=512。H_A与H_A_I是同一重访组件的顺序变化，不宣称前者没有I经历。新增控制类别独立标记，不与event-free/spatial-detour stratum混计。

所有组装历史重新实际回放。公共尾、agent和两个sensor数值汇合<=1e-5、真实连续实例事件、完整交叉矩阵、27重放/54求值、V4回读和policy/query隔离保持原检查器不变。必须严格控制计数捷径。首族最多仅证明这一控制类型可执行，不能宣称跨房屋稳定或完整科学收益。

同GPU2非独占、最多3000秒/30000动作/8GiB输出的独立有界节点；具体发现/认证预算沿用assembly配置。不停止外部进程，不碰普通GPU5，不训练。用户持续迭代造数授权覆盖本次版本化修订；不覆盖旧失败，不把控制类型变化隐藏成原中性回路成功。
