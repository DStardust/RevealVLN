# 特殊数据优先：紧凑真实回返构造 V2

用户本轮“优先处理特殊数据，普通持续生成”授权内的数据工程修订，不修改论文V3、标签器、封存失败或普通生产者。

旧反馈V1终态：3候选、141探测、14205动作、0新族；2个600秒资源截断，1个16配置无anchor_A目标。无目标不能推断场景不存在目标，未逐阶段记录的几何原因不臆测。

V2保持完全相同三个语义角色/房屋候选，不根据成功结果换角色，保留所有V1失败。仅改变发现过程：

1. 从已登记R2R源坐标筛与navmesh相差<=1e-5的位置，不挪动源坐标。每个位置需四角色都存在V1生成器内<=8m的几何目标；按最大/总最近目标测地距离再坐标排序，前4位置×4朝向，共<=16配置。筛选只用元数据及navmesh，配置在实际动作前冻结；没有有效点则明确记为几何候选耗尽，不伪造可达性证据。
2. 先执行公共尾LRLRLRLR，检查没有anchor_A/B/terminal事件，排除不可能通过最终公共尾约束的配置；全程真实动作且计费。
3. 实际回放去程和紧凑逆向回返，不再额外执行每个F展开为12L+F+12R的另一条未采用轨迹。不是把理想逆轨迹当可行轨迹：被采用的每条紧凑回返仍实际执行并检查完整事件、碰撞、agent及两个sensor位姿差<=1e-5。记录省去的未使用序列长度，仅为动作序列算术，不宣称已测整体加速或导航收益。

原histories构造/等长/无关绕行、完整矩阵检查、数值汇合和边界事件、公共近期观测、27重放/54求值、导出回读保持继承，不放宽阈值；新族仍只PHYSICAL_REPLAY_CERTIFIED_PENDING_SHORTCUT_AUDIT，training_admission=false。

运行范围：只GPU1现有低资源上下文非独占有界生成，不停止任何外部进程；原普通GPU5继续。监督使用XML全进程清单，含G渲染；自身总显存保守上界<4096MiB、外部各<=768/总<=2048MiB；RAM8GiB、输出8GiB，7GiB提前停止，内容6GiB。总3000秒监督/2700秒工厂、40000实际动作；每候选发现600秒/12000动作、认证600秒/20000动作，不追加预算或候选。自己的Popen进程组由supervisor清理，不依赖项目Python缺少的pidfd扩展；退出后XML确认自己的renderer不在列表。

CPU通过仅批准数据构造探针。成功也不直接证明反捷径合格、贡献充分或导航提升。失败不可覆盖、不可自动扩卡或训练。
