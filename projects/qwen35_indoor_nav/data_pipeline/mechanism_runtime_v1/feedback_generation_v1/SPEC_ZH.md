# 反馈式真实机制数据构造 V1

用户“开始生成数据”准入本新数据工程节点。旧 P0、语义候选、工厂源码和所有失败只读；不训练模型，不重开论文主张。产物只能按真实质量层级入库。

取已封存语义候选manifest前3个不同FIT房屋、各第0候选，不根据轨迹结果换角色。使用共用51/5/5划分。room输出文本仅将原tv room-code表述为TV room、familyroom/lounge表述为family room/lounge，任务谓词和角色实例不变，语句在实际动作前冻结。来源R2R train的参考路点是起点候选，非物理行动证据。

新FeedbackFactory覆盖 routes：明确是实际状态反馈发现，而不再声称纯几何提案。每目标从指定起点真实重置，native greedy follower只给离线动作建议，查询前后必须不改变真实pose；所有动作先持久预算再sim.step，保存实际RGB/semantic/碰撞。目标来自语义实例周围0.75/1.25m圆周8方位snap后的navmesh位置，路径<=8m，按距离/实例/半径/角度排序最多8个，单去程含朝向见证最多140动作。完整轨迹随后仍由原TraceRunner独立回放，原inverse合法性、1e-5汇合、公共尾、3×3×2、27重放/54求值和V4回读不变。

每屋在登记R2R参考坐标中筛与navmesh差<=1e-5的位置（只筛，不修正原位置）；按到角色中心的总距离确定性取前4坐标，每个yaw 0/6/12/18，公共尾LRLRLRLR，共最多16配置。派生配置在首次真实动作之前单独落盘冻结。无可用坐标记几何不适用。

TrackedContentStore保持原NPY/像素哈希/去重/原子提交和fsync，仅将已成功写入文件的容量核算改为增量；失败立即poisoned并停止，不继续使用不完整计量。结束进行独立全目录核对，CPU相同序列、容量/失败测试必须先过。

资源：GPU1，uuid GPU-734a5268-31fe-6452-105b-36cd08c3d9c8，非独占渲染，启动util=0、外部context各<=768MiB/合计<=2048MiB；运行中仍按此外部资源界限记录，新冷context出现本身不停止，超限只停止自己的worker。绝不停止外部进程或占位。总3000秒监督/2700秒工厂预算、40000动作/8GiB输出/8GiB RAM/自身GPU4GiB；每屋发现600秒/12000动作、认证600秒/20000动作，先触及任何总预算即截断，不追加候选。总store内容上限6GiB，余2GiB留证据账本，监督7GiB提前停止留1GiB余量；并行不占普通GPU2。

全量尝试/实际trace/碰撞/拒绝/资源截断入账。完整物理族在导出后仍为PHYSICAL_REPLAY_CERTIFIED，训练准入false；反捷径和强状态标签跨族验收未通过不得升级MECHANISM_TRAIN_READY，不能把本节点自动标为论文科学PASS。完整族0时如实报告，不用去程数量冒充数据族。
