# 首批生产监控修复 V1

原运行完成16候选（15认证、1路线偏移拒绝），第17项执行后写PNG期间，`du -sb`遇到原子改名导致退出码1。父监控误将统计竞态当致命错误，终止自身worker；GPU3原占位已恢复，原产物不覆盖。
本版只修复磁盘采样，使用逐文件stat并容忍已被原子替换的文件消失；不改变编译器、阈值、参考路线、任务或验收。
复用原锁定worker.py的实际编译/回放函数，输出改为本目录。前16个完成项不重做，第17个在途项重做，余下共84个，累计仍100个固定候选；在途重试成本单列，不伪装成第一次执行。
原未完成route_016及未被索引的PNG保留，不作为训练样本。前阶段最终精确动作计数若因SIGTERM未落盘则标unknown，只报告已保存计数下界，不凭估计冒充精确总量。
GPU与占位借用按父目录GPU_TRANSPORT_AMENDMENT_V1，保持完整恢复义务。剩余worker墙钟上限扣除原运行墙钟，磁盘预算覆盖父节点与恢复目录之和。
