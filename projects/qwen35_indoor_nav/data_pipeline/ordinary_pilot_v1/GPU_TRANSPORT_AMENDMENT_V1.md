# GPU分配修订（首次回放之前）

初始启动的GPU2空闲检查失败；无worker、仿真、轨迹或标签启动。`CODE_AND_INPUT_LOCK.json`保留当时源码，不伪装成实际运行锁。
GPU2有另一项目实际任务，不停止、不读取其项目内容。GPU1同样有实际任务上下文。
依据用户此前明确许可“没有空闲gpu可以释放后面占位的程序，并在用完之后占用回去”，本节点改借GPU3。
仅允许释放已核实的项目占位进程：tmux `vla_idle_occupancy_20260904:0.0` 中 `scripts/occupy_idle_gpu.py --gpu 3`；保存实际PID、完整命令、cwd、pane身份和其他上下文。
恢复运行原有占位环境仅为还原用户资源状态，不将旧环境用于Q35N数据、模型或科学计算。不得停止真正任务或用途不明进程。
GPU3 UUID=`GPU-a62dba8b-285b-57e6-b6d2-cf6e5788864a`；最终实际运行锁为 `GPU3_CODE_AND_INPUT_LOCK.json`。所有任务选择、标签、阈值、配额和预算不变。清理自己的worker后恢复原占位，成功/失败均记录恢复结果。
