# 扩产监控断连恢复 V1

用户报告旧 18769 页面显示 TypeError: Failed to fetch，表格保留旧数值。服务器接口持续返回 200 且 GPU4/5 生产进度增加，尚未确定浏览器到服务端的具体断点。不能把本修订描述为修好了用户侧 SSH 或已复现根因。

仅新增本目录的独立只读监控和受控切换工具，沿用 127.0.0.1:18769。旧 runtime_v2 全部冻结代码和输入锁保持不变；不停止、重启或修改生产、训练、评测、占位。只允许核验后停止旧 monitor.py PID1421413，并启动本目录的新监控；失败则恢复原监控入口，不触碰其他进程。

服务端：只读调用校验过源码哈希的旧统计函数；单个后台线程每 5 秒读取，HTTP 请求立即返回缓存快照，避免请求堆积；读取失败保留最近成功快照但明确标为过期，首次失败返回 503；首页服务端渲染并嵌入快照；查询参数可用；监听仅回环，所有写入方法拒绝。训练/完整基准状态从实际文件读取，不再一直显示评测运行中。

浏览器：同源相对路径请求支持带路径前缀的转发；每次只允许一个请求，8 秒超时，失败退避重连，手动重试/网络恢复可触发；请求失败不清空已有数据，但显著显示断连、上次成功时间和过期提示。区分 HTTP 错误、非 JSON 响应、超时与浏览器网络错误，不声称能由页面自动修复断开的 SSH 隧道。

验收：CPU 单测缓存/读取失败/恢复、只读 HTTP、相对路径、HTML 注入防护、前端单请求及超时/断连恢复；切换前后记录源码与输入锁哈希、生产 supervisor 身份。新监控最多一个采集线程，无 GPU、无依赖安装、无训练和生产操作；24 小时日志仅记录状态转换，无请求凭据。
