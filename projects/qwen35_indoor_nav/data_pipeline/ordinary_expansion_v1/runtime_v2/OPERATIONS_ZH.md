# 停训转扩产：运行与接手

## 当前任务边界

训练已正常停止，最终检查点位于 `sft_acceptance/ordinary_sync_recovery_v1/formal/attempt_001/checkpoint_000051301.pt`。不要自动重启训练；冻结代码、协议、历史输入锁和失败结果不变。GPU1 完整 R2R-CE 验证仍运行，使用其事前冻结的 41,800 检查点；不要把本次最终训练检查点误报为正在测的权重。

扩产入口：本目录 `launch.py`，tmux `q35n_envdrop_gpu345_v2`。三张 GPU3/4/5 先各做固定 3 条不同房屋哨兵；只有 `lanes/gpu_N/attempt_000/SENTINEL_GATE.json` 明确 `strict_pass=true` 才会接续该卡固定后续分片。每卡是唯一 producer，输出在相邻 `envdrop_production_v2/shard_XXXX`，与旧失败目录和元数据目录物理分离。首波固定 9,661 条，不重复旧已尝试路线。

不要再次执行入口、prepare 或 stop_training；都是一次性节点。失败只能先核验 RESULT、RESTORATION 和未完成目录，再规划新版本，不能重跑覆盖当前输出。成功或失败均由各卡 supervisor 恢复原占位，完整回执必须核实。控制器只在三卡全部闭合且恢复成功后自动进行最终 CPU 复核合并。

## 监控

- 原训练页面：服务端 `127.0.0.1:18766`，保留，不修改其冻结实现。
- 完整基准页面：服务端 `127.0.0.1:18768`，保留。
- 新扩产页面：服务端 `127.0.0.1:18769`，tmux `q35n_envdrop_monitor_v2`。
- 新扩产机器接口：`GET /api/status`；健康检查 `GET /healthz`；其他写入方法拒绝。所有页面仅监听回环地址，不暴露无认证的公网写接口。

在本地用与原页面相同的 SSH 登录目标额外转发 `-L 18769:127.0.0.1:18769`，浏览器打开 `http://127.0.0.1:18769/`。页面每 10 秒刷新，分别显示每卡已处理、严格合格路线与动作；旧数据恢复审核单独列出，不重复加算。

## 数量与下一步

当前训练曾使用 1,394,744 条人工指令条件化动作记录；这不含本轮 EnvDrop 新数据。旧数据恢复的权威结果在 `recovery_inventory_20260911_v1/run_001/RESULT.json`，未出现成功结果前不以候选索引作为最终入训数据。本波最终强审核合并结果在 `runtime_v2/merge/RESULT.json`；滚动逐片计数不冒充完整闭合。

后续按三个互不重叠索引的实际合格动作数计算 300 万基础目标缺口，再从现有官方 EnvDrop 未选来源冻结下一波。该规模是工程目标，不保证 SR/SPL 自动提升。恢复导航、STOP 困难状态、特殊任务历史机制等数据仍需各自版本化监督与审核，不能把失败轨迹直接转成负标签，也不能用重复 epoch 或同路线改写指令冒充新物理覆盖。
