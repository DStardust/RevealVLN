# 主 agent 扩产准入记录

用户明确要求停训扩产。经精确 PID/启动时间/argv/cwd/uid/RANK 检查，只向旧训练 rank0 发出正常停止请求。训练正常收口于 51,301 更新，最终 checkpoint SHA256 为 `4d60cdb09a909121856e438547df5c6e1e9ea35fc40a8f257302bf563af4979e`；全部原训练进程已退出，GPU3/4/5 均有成功的占位恢复回执。评测仍使用事前冻结的 41,800 检查点，不被本次停训切换或停止。

15 项 CPU 测试通过。747 项新旧来源、资产、分片、实现、资源配置输入绑定核验通过。新锁 SHA256：`c693ff433c3a02557c5d125400ce77d877b396a4d26544ffd7420fba1189509f`。新代码全部在 runtime_v2；旧编译和强审核未改变，原首批检查函数与最终索引校验函数 AST 相同。外部 GPU 检查、显存解析、占位退出/排空、worker 清理逻辑 AST 保持原样；版本变更仅针对三卡独立分片、有限预算、各卡哨兵绑定和精确环境恢复。

首波 9,661 条 / 24 个 FIT 房屋：GPU3 3,253 条，GPU4 3,253 条，GPU5 3,155 条。逐目录核验旧 10,339 个已尝试目录与本波完全不交叠；其中旧终态 10,338、未完成 1，旧未完成不重试。三卡各固定三个不同房屋哨兵，严格回读失败即停止该卡，不替换样本。CPU 测试和代码核验仅是工程准入，不是首批物理审核通过、最终入训或模型效果通过。

准入：在本锁绑定下，仅启动新三卡普通数据生产和只读 18769 监控。固定后续分片由通过哨兵的各卡自动接续。全波闭合且恢复占位后，控制器自动触发有界最终 CPU 合并。失败无自动重试；不启动训练、特殊旧批次或其他 GPU；GPU1 完整评测和旧数据恢复审核保持运行。

本轮不承诺 300 万动作已达标。最终严格合格数用于计算下一波缺口；本波不是新的科学方法实验，也不更改论文创新裁决。
