# 2026-09-11 普通数据扩产 V2

用户授权：把训练停了开始扩产吧。本节点只停止经身份核验的本项目普通训练，保存最终检查点；不停止 GPU1 完整基准评测、CPU 旧数据恢复审核或任何外部任务。旧生产、训练源码和输入锁只读。

首波固定 9,661 条：来自 recovery_inventory_20260911_v1/run_001/RESCUE_JOBS.json，均在旧 20,000 路线中从未尝试。旧 10,338 个终态和 1 个未完成目录不重复。以旧冻结来源、FIT 屋划分及物理路线键排重，保持官方 EnvDrop 自动英文指令来源级别，不冒称人工语义真值。

仅借用已恢复并实时核验的 GPU3/4/5 占位。三个独立队列、42 个互斥分片。每卡先固定 3 条、来自不同 FIT 房屋的哨兵，全部真实编译、两次回放认证、逐帧 CPU 强回读通过后，才接续该卡后续 13 个分片；每片最多 250 条。哨兵按房屋字典序与原候选顺序事前确定，不依据结果挑选或换样。后续分片保持原房屋组顺序。

编译器、动作接口、224 RGB、碰撞/路径走廊/waypoint、唯一末尾 STOP、像素/位姿/两次实际回放要求及逐路线隔离标准全部沿用封存实现。每 50 条即时严格回读；全批闭合且占位恢复后再次 CPU 审核合并。中断不自动重试、未完成不标为负样本、质量失败不替换。自动接续仅指尚未开始的固定后续分片。

每卡墙钟 43,200 秒（12 小时），哨兵片 600 秒、常规片 3,000 秒，片内提前 120 秒收尾；剩余预算不足完整下一片则不启动。每片磁盘 12 GiB（11 GiB 提前停）、42 片 504 GiB，元数据/合并 8 GiB，总 512 GiB；每 worker RSS 12 GiB、自有显存保守上界 4,096 MiB。保留原 V4 XML 活跃进程显存保守核算，不改变外部负载与恢复阈值。失败和成功都只清理自身 worker，最终恢复原占位 argv/cwd/uid/项目缓存环境；不对外部进程发信号。

启动前：确认训练 STOPPED、checkpoint SHA、三卡占位恢复；固定所有代码、来源、资产、分片、身份和预算；CPU 单测通过并登记逐卡主审准入。GPU1 评测、原训练数据与 CPU 恢复节点不写入本次可变输入。

此波加上原人工数据与旧数据恢复，仍不保证达到 300 万有效动作；不以重复 epoch、改写指令或失败路线凑数。完成后按实际严格合格量计算缺口，再从现有官方 EnvDrop 的未选来源中准备下一独立波次。本节点不自动恢复训练，不实现新模型模块，不宣称导航收益。
