# GPU3 恢复队列接手说明

本节点由用户要求继续 GPU3 普通扩产后启动。旧 runtime_v2 的 GPU3 因 250 条片预算不足关闭；不要再启动它，不改旧 SOURCE/INPUT_LOCK/RESULT。原 GPU4/5 在本操作中只读，不重新借卡或重复启动；自然完成后保留其恢复回执。

当前生产 tmux：`q35n_envdrop_gpu3_recovery_v1`。控制器 `launch.py`，生产 supervisor `run.py --gpu 3`，本次已登记 supervisor PID1563222；PID 仅为启动记录，操作前必须重新核验身份。固定 3112 未尝试路线 / 23 个 FIT 房屋，先 3 条哨兵，随后 63 片、每片最多 50 条。执行预算首片 480 秒、常规片 3480 秒（注册上限减 120 秒收尾）；整卡 12 小时，有余量才启动下一完整片。

只有 `lanes/gpu_3/attempt_000/SENTINEL_GATE.json` 的原强审通过才能自动接续。每 50 条即时审核，成功闭合并恢复占位后自动做本恢复波最终 CPU 复核合并。失败不自动重试，只记录失败并恢复占位。禁止改本冻结源码或对旧 partial 隐式重放。

旧数据补审已完成：140 条完整路线、9190 条有效动作；此前发布 103 条，额外 37 条现已复核通过。索引在 `old_salvage/TRAINING_INDEX.jsonl`，引用旧数据，未覆盖旧索引。`envdrop_0b264fe96107cd01107c` 一个未完成样本保留不重试；因此原三卡总计划 9661 在没有另行恢复这一个 partial 的情况下，最多 9660 个终态，不要伪造 100%。

监控继续使用 `127.0.0.1:18769`。新监控实现为相邻 `monitor_gpu3_recovery_v1`，实际进程位于原 tmux `q35n_envdrop_monitor_v2` / pane %302（不要按名字误认为仍是旧统计逻辑）。它汇总旧 GPU3 140 条与新恢复片，旧失败另留 `prior_failure`，不重复计数。用户若有旧页面可强制刷新，不需要换转发端口。

注意原 V2 控制器仍会按原协议将整个旧三卡波记为含 GPU3 失败，不会因为这里恢复而自动修改旧结论。完整训练索引需要在所有来源独立收口后，以新版本合并旧 GPU4/5、旧 GPU3 补审和本恢复波，并再次验证去重和输入隔离。当前不自动复训、不改变 51301 最终训练检查点或已经完成的 41800 完整评测。
