# GPU3 普通扩产恢复 V1

用户要求让 GPU3 继续生成。仅接续旧 runtime_v2 GPU3 分配范围内未尝试的路线，不更改 GPU4/5、训练、评测、已冻结源码和锁。旧失败 SHARD_WALL_BUDGET 保留。

原因：旧 GPU3 的 shard_0003 在房屋 VzqfbhrpDEA 生成 137 条全部 CERTIFIED，路线均耗时约 20 秒，250 条预计超过 80 分钟；旧执行上限 2880 秒不够。不是据此判定物理数据失败。原卡共 140 终态（含 3 哨兵）、1 partial、3112 未尝试，按目录/ledger/原 JOBS 逐项核验。

新输出 envdrop_production_gpu3_recovery_v1 与 runtime 元数据分离。固定 3112 条，不和其他卡 JOBS 相交；23 个 FIT 房屋。事前按房屋字典序选择首三个不同房屋的未尝试样本为新哨兵；全过原强回读才自动接续。余 3109 条保持原顺序，每片最多 50 条，共 64 片。首片 600 秒，其他片 3600 秒，提前 120 秒收尾；GPU3 总 43200 秒，剩余不足完整下一片则停止并恢复占位，不启动短预算尾片。不承诺 12 小时一定全部产完。

每片 4 GiB（3 GiB 提前停），64 片 256 GiB + 元数据及合并 8 GiB，总 264 GiB；worker RAM 12 GiB、自有显存保守上界 4096 MiB，外部进程阈值与原版一致。只借精确核验的 GPU3 占位 PID1476990 / 原 pane %153，成功/失败/中断后按原 argv/cwd/uid/cache 环境恢复。无外部进程信号，无自动重试。

旧 140 个完整终态做独立 CPU 强回读补齐未发布的 37 条，旧隔离（如有）不撤销，旧已发布索引必须与新读回完全一致。old_salvage 的索引只引用旧数据、不覆盖它。partial envdrop_0b264fe96107cd01107c 保留诊断、不作负例、不重试。新源只采用源元数据/资产继承冻结，变化仅在资源调度与分片，不改编译/审核门槛。

启动前冻结来源、预算、身份、代码与分片；CPU 测试及旧完整数据复核后准入。完成新卡并恢复占位后进行本恢复波最终强合并；不训练。原 V2 全波因 GPU3 失败不会自动整体合并，未来在各节点正式收口后由独立合并版本解决，不能修改旧控制器让失败变 PASS。原 18769 监控后续用独立新版统计展示旧结果+恢复分片，并保留旧失败原因。
