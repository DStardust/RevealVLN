# 主 agent GPU3 恢复准入

用户明确要求 GPU3 继续生成。新节点固定 3112 条从未尝试路线，来自旧 GPU3 分配范围，与其他两卡物理键/任务 ID 不交叠。旧 140 条全部完成 CPU 原门槛强回读，9190 动作；补回原未发布 37 条，不重新仿真，不覆盖旧索引。1 个 partial 独立保留。

8 项 CPU 检查通过：来源/分片完整性、非法分片、重复排除、编译兼容、原物理审核/占位/外部资源判断 AST 一致、预算尾片拒绝、未批准不碰 GPU、哨兵证据绑定。实际旧最慢路线 36.14 秒，50 条加启动余量低于新 3480 秒执行预算；这不是未来每条耗时保证，总卡仍 12 小时有界。

1679 项输入绑定已冻结：`fcb9eb183ccf48b66e6a532b55c23550b96c4c76312cdc42a309bae73aedf6f4`。旧源码/锁保持不变，新增代码仅本节点，数据仅相邻 envdrop_production_gpu3_recovery_v1。GPU3 占位在旧成功恢复记录与 live pane/%153、pid/starttime/argv/cwd/uid/cache/GPU UUID 下核验后才能借用。

准入首个新三样本哨兵，全部通过原强回读才能接续 50 条片。只借 GPU3，不动 GPU4/5、训练或评测。任一阶段失败保留回执并恢复占位，不自动重试。成功闭合后本恢复波独立强合并；不将旧 V2 全波失败改为 PASS，不自动训练。18769 的显示接入独立 monitor_gpu3_recovery_v1，不修改原监控或生产冻结源码。
