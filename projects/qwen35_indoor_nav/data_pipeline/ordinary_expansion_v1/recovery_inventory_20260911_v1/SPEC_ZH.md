# 普通数据优先：EnvDrop旧产物回收与未尝试来源清点

2026-09-11，承接用户“普通导航基座也可以加创新模块，还是先造足够多的数据”。优先补足原定300万有效指令条件动作监督，数量目标不是足够学会导航的保证。模块设计可以预研，未完成具体近邻与算子审查前不称原创，也不热改当前普通基座。

本节点只执行CPU数据回收：读取原20,000条EnvDrop计划、21片账本、索引、隔离清单、结束/还卡证据，分清已终态、部分开始、完全未尝试。对可回收终态调用原封不动的ordinary_scale_v1/audit.py，重读真实RGB像素与轨迹/双回放证书。两路CPU并行上限，单次墙钟7200秒、进程树RSS16GiB、新输出2GiB；不复制RGB、不启动仿真/训练、不操作GPU或原进程。失败停止自身，保留新输出，不自动重试。

只写本目录。旧运行失败SHARD_WALL_BUDGET与所有原始结果/源码/输入锁保持不变；旧控制器/worker必须已关闭，现存锁只用rb打开并尝试互斥锁，不创建或改写旧文件。原生产者进程PID如果仍存在则拒绝；不尝试停止任何原进程。

质量门槛不变。已隔离的记录不复活；已接受记录本次不一致则报错停止，不悄悄删除。尚未发布到旧索引、但有完整CERTIFIED终态的记录允许本次严格重审，单列恢复来源。部分目录或残缺账本保持未知、不得直接重跑。完全未尝试来源单独产生RESCUE_JOBS.json，当前不自动赋予GPU执行权限。

普通旧训练快照仍为5849路线/17548指令/1394744条件动作。EnvDrop为官方合成英语指令，独立分账；新的CPU回收结果不能自动追加到活动训练快照。新训练需独立snapshot与配比，保留人工指令。不把闭环验证失败轨迹回灌训练，不用DEV/CONFIRM/官方val/test补数量。

本次数据将保留完整RGB/动作前缀，使下一版能检验更长历史，而非仅存两帧窗口。现有严格无碰撞专家示范仍独立成池；后续纠偏数据需要单独规范合法恢复标签，不能放宽本池碰撞规则混进失败轨迹。特殊交叉族继续按原族/祖先/房屋分组，不以模板改写凑物理数量。

完成判据：全21片元数据可复核；原终态成本和旧拒绝全部保留；原审计代码哈希及输入元数据前后不变；全部本次接收路线重审成功、物理键/指令别名无重复且不在旧排除表；输出正式独立索引和拒绝/partial/待办表。只有完整结束才生成RESULT.json及TRAINING_INDEX.jsonl，运行中仅写候选索引并标记NOT_ADMITTED。

资源调度后续另开新版本：先安排未尝试项的小片连续生产，首片强审核通过才接续；不恢复已关闭旧队列，不抢GPU1评测和GPU3/4/5训练。保留原300万首档和特殊万族远期工程目标，但不声称本次CPU回收已经达到。
