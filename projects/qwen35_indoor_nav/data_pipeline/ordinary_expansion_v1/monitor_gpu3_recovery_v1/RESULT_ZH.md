# GPU3 恢复统计接入

18769 同端口的新只读监控已启动，PID1563749 / 原 pane %302。只停止了上一监控 PID1530172，没有停止生产或占位进程。部署预览及切换回执在 DEPLOY_SUCCESS.json，5 项统计/保护测试通过。

复用上一监控的缓存、过期提示、超时和断连重试实现，其代码哈希保持不变；新功能仅聚合旧 GPU3 终态与 runtime_gpu3_recovery_v1 新分片。旧 37 条补审只修正合格统计，不重复增加已生成路线；未完成 1 条不隐式计入 completed。GPU4/5 统计与原收口来源一致。

浏览器连接故障与生产失败仍单独显示。GPU3 的旧失败可在 API 的 prior_failure 查看；当前 stage 取新恢复队列；recovery 字段保留旧终态、新生成数、未重试数量。该页面没有训练或队列控制接口。
