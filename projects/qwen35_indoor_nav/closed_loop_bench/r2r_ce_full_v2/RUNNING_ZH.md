# 全量评测运行说明

状态快照：2026-09-11 15:58（北京时间），EVALUATING，完成32/1839条。该数字不是最终结果；实时状态见18768页面或run_001/PROGRESS.json。启动初审核对了32条已完成轨迹、4536个动作，剩余进行中动作不冒充已完成路线。

## 本次测什么

- 检查点：原全量准备时最新完整保存的41,800步，SHA256为40a28ceb9ec688b2b5a1285ce0fe82c87af6b20a36ca1261ced762be13da3343。训练仍继续，本次不追逐更新的权重。
- R2R-CE val_unseen完整1839条指令、11个房屋，不是8条小测，也不是隐藏test或榜单提交。
- GPU1独占本次模型和8路仿真；原训练GPU3/4/5、生产队列、占位任务、冻结训练代码及输入锁不改。
- 完成后自动计算SR、SPL、NE、OSR、nDTW、SDTW、逐屋结果和失败类别，并做逐步动作及指标审计。只有全1839条齐全且审计通过才输出完整成绩。

## 修复和验收

首次full_v1因错误要求数据缓存距离等于实时测地距离而中断，13条旧成绩不混入本轮，3279动作成本保留。全量预检确认1839条均可达，39条存在缓存差异；本版复用官方实时距离函数，以冻结的CPU逐条实测值进行严格初始距离校验，不改成功阈值、不删样本、不换检查点。

30项CPU测试、全量几何预检、8路批量/单条动作一致性门均通过。原13条重跑轨迹、动作、SR/SPL完全一致；曾中断的episode939已正常完成。记录见run_001/STARTUP_AUDIT.json。当前尚无完整SR/SPL，不能把局部均值当全量成绩，也不能与旧8条均值直接相减断言进步。

## 远程监控与结果位置

只读页面：http://127.0.0.1:18768/；JSON接口：http://127.0.0.1:18768/api/status；健康检查：/healthz。原训练页面18766保持不变。访问远程服务器时，在现有SSH连接中增加本地18768到服务器127.0.0.1:18768的端口转发，例如：

```bash
ssh -N -L 18768:127.0.0.1:18768 用户名@服务器
```

评测会话：q35n_r2r_full_v2；只读监控会话：q35n_r2r_full_monitor_v2。当前评测已在后台运行，切勿再次执行launch.py或重复启动评测。

结束后自动生成run_001/RESULT.json、run_001/REPORT_ZH.md、run_001/episodes.csv。当前运行状态以进度页为准；失败/资源截断将单列，不自动重试，不将缺失项计0或悄悄跳过。预计数小时，早期ETA受启动开销、房屋顺序和短路线先结束影响明显。

详细冻结口径：SPEC_ZH.md、PROTOCOL.json、SOURCE_LOCK.json。此说明是启动后的状态快照，不属于模型输入或冻结评测源码。
