# 普通历史配对评测 R1（训练完成前登记）

两分支分别使用 ordinary_history8_paired_train_r1 的固定最终4000步权重；
不以中途损失或检查点成绩挑选。两者同一384000决策、同一初始化/优化器。
control_recent2 输入最近2帧；treatment_prefix8 输入7张前缀分位历史+当前帧（不足左黑图）。
基础FP32主参数模型从当前训练目录加载，checkpoint绑定必须含正确分支名。

## 本次只准入完整内部开发100条

照搬 ordinary_expanded_dev_after_single_v1 的100条/5屋、指令、初始位姿、几何预检、
旧接口parity图、seed1209/environment0、8仿真lane但实际推理batch1。
传感器与动作：224 RGB、HFOV90、相机1.25m、机器人1.5m/0.1m、前进0.25m、
转向15°、允许sliding、500动作、显式STOP且距离<3m，官方Habitat0.1.7指标+精确DTW。
不增加深度、位姿、目标位置、未来图、oracle或控制器作为策略输入。
不追加仿真采集或训练。既有已暴露100条仅开发，不冒充完整val_unseen。

每分支仅一次，GPU1必须实时确认为空；不借占位，不干扰GPU3/4/5训练。
每分支7200秒、28GiB GPU（模型25GiB）、64GiB CPU、4GiB输出、最多50000环境动作。
这是独立评测预算，非复用短诊断权限。8帧较慢，两分支采用同一墙钟上限。
旧接口16输入各做单条与batch8诊断，共32前向决策另记；正式一直batch1，
诊断不过也不切换batch8，不据诊断分数更新参数。模型前后fingerprint必须不变。
允许control完成验收并GPU3/4/5该分支退出后，在GPU1评测它，同时8帧训练继续；
只需该分支最终验收，无须为此停止另一分支。也可顺序在两分支完成后评测。

## 逐动作真实输入审计

从每条reset原始RGB哈希与每步执行后RGB哈希独立重建已见序列，
与实际送给模型的images数、帧索引、原始哈希、黑图掩码、已执行动作数逐项核对。
历史选择任何未来索引、错图、跨episode遗留或padding不符均拒绝聚合。
完整100条、所有动作记录对齐、模型不变和清理闭合后才接收指标。

## 筛选，不提前宣布成功

历史采用门槛保持训练协议：8帧相对2帧SR严格升、SPL不降、nDTW下降≤0.01。
另分别对保留的旧best4k（SR21%、SPL18.0169839%、nDTW36.5879701%）做同标准比较，
报告逐episode赢/输、5屋bootstrap区间、逐屋和留一屋差值，不把平均小正号讲成稳定普适收益。
标准基座后续完整1839条候选，仅在至少一个分支相对旧best通过上述三指标门槛后选择；
8帧候选还必须通过对2帧门槛。若有多个合格候选，SR优先、再SPL、仍同分选2帧。
若都不通过，保留失败，不自动对低于旧基座的候选花完整基准预算。
完整1839条仍需单独冻结执行预算；本次100条不是用户SR≥40%终点。

