# 低学习率分支：同输入配对评测

仅低学习率分支的固定最终8,000步，同较好的4,000步对照；不是从失败8,000步接续。训练LR5e-6，另完整报告高LR失败分支。旧规范如下，其输入/物理/计分/资源/门槛逐字沿用。

# 扩产第二段最终检查点的配对评测

前置：ordinary_expanded_low_lr_v3 正常到累计 8,000 更新、首个新保存点验收通过、三卡占位恢复。未达此前置不改用任意较早检查点凑结果。
对照：扩产首段 4,000 更新的 ordinary_expanded_dev_after_single_v1，同一批 100 条／5 个 INTERNAL_DEV 房屋。
严格复用该固定单条推理实现：batch=1、8 个隔离仿真通道、同 seed=1209、两帧 RGB／八已执行动作、greedy、500 动作、0.25m／15°、明确 STOP 且 <3m、原官方物理计分与独立轨迹审核。没有视觉停滞保护。

唯一变化是模型训练到下一段。主 SR；正向工程门槛：ΔSR>0，ΔSPL≥0，ΔnDTW≥−0.01。逐屋配对新增/损失、留一屋和五屋 bootstrap 一并报告，不能把开发正号视为独立论文收益。
GPU1 再次核实空闲后一次运行，最多 3,600 秒、28GiB GPU、64GiB CPU、4GiB 输出、50,000 动作，0 参数更新；只清理本进程组，无占位借用、无自动重试。

运行前绑定最终 checkpoint 哈希；输入、源码、训练协议与门槛提前封存，未知最终模型哈希不伪装成已准入。原结果、源码和输入锁不变。
