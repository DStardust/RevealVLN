# 100k普通迁移的资源监控恢复

原进程因DISK_SCAN_DEADLINE停止：最后完整磁盘统计约7.49GB/128744文件，耗时108.5秒，下一次超过120秒。不是产物达到160GiB上限。原失败和1290个完整pair保留，剩余549对从完整A/B边界续跑。

修复仅运行资源统计：扫描转独立CPU子进程，定期输出明确标为下界的部分扫描值；GPU/RSS/时限检查不再等待完整扫描。扫描仍有1200秒上限，磁盘可用空间和产物预算继续检查；完整测量及时间保留。计费仅读取已知session/attempt EXIT记录，移除每10秒对全部图片树的递归遍历；已有GPU小时与新会话分别准确累加。

原模型、适配器、前向、动作、环境、episode顺序、成功定义和SOURCE_LOCK不改。四项CPU验收通过；新资源运行源码单独锁定。原18770监控读同一run目录，续跑不依赖Codex会话。该修复不提供任何新方法收益声明。
