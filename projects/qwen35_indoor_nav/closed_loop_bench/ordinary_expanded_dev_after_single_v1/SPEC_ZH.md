# 扩产后模型：固定单条推理复测 V1

用户授权：2026-09-12「检查并做出针对性修改」。本节点修复比较配置不一致，不改模型。

旧模型 51,301 步的 100 条 INTERNAL_DEV 评测实际 batch=1；扩产后新更新 4,000 步的评测实际 batch=8。
原数值一致性门槛与回退行为均保留，不能把两个实际批量不同的结果作为严格配对提升证据。

本次固定使用同一个扩产后 4,000 步检查点、同 100 条输入、同 5 屋、同种子 1209、同 8 个独立仿真通道；唯一推理修改是强制模型 batch=1。
批量数值诊断仍记录真实通过与否，但不再据此切换推理批量。对照复用旧模型已经完成的 batch=1 结果。
不改变模型结构、权重、动作集、greedy 决策、输入窗口、物理参数、STOP 半径、500 动作上限、官方指标或独立轨迹审核。

先验验收：100/100 完整且轨迹审核通过；实际 batch=1；检查点与输入哈希相同；参数更新为 0。
导航判断报告 SR、SPL、nDTW、OSR、逐屋差异以及配对胜负。旧注册的开发信号仅为 ΔSR>0 且 ΔSPL≥−0.02，不代表科学通过。
没有训练、调阈值、反复挑子集或自动长训准入；旧 batch=8 结果不覆盖、不删除。

资源：GPU1 经 XML 核验空闲才启动，不借占位，不接触 GPU0/2 或其他任务。最多 3,600 秒、28 GiB GPU、64 GiB RSS、4 GiB 输出、50,000 动作。
仅启动一个独立进程组，失败仅清理本组，不自动重试。新文件限本目录，沿用项目内已审计环境及只读资产。
