# 图表监控与失败生产任务恢复

实读验收时间：2026-09-10 12:32:53 CST。用户指令：“有用的信息做成图表，然后把失败的任务重新挂上”。这是普通训练当前交接的补充，不覆盖历史失败、冻结源码或输入锁。

## 已上线图表

新服务 `sft_acceptance/monitor_charts_v1/server.py`，tmux `q35n_monitor_charts_v1`，PID 3745792，仍仅监听 `127.0.0.1:18766`。原监控 PID 3710131 经 PID/starttime/cwd/argv 核实后单独退出；训练、5条健康生产队列身份前后完全一致。未停止任何外部任务或 GPU0 任务。

在本地电脑保持 SSH 隧道后打开 http://127.0.0.1:18766/ ：

```bash
ssh -N -L 18766:127.0.0.1:18766 <用户名>@hpda-jushendaohang-005
```

页面每10秒刷新：训练 CE/20点均值、吞吐、四动作目标/预测/召回、混淆矩阵、训练/epoch进度、计费决策预算（含probe）、剩余wall/update、GPU显存、checkpoint/心跳、生产队列和恢复说明。历史失败仍显示；RUNNING不等于强审合格，在线训练指标不等于DEV或闭环导航收益。

只读接口 `/api/status` 与 `/healthz` 保留；POST等写方法405，任意文件路径404。无公网监听、CDN或HTTP控制入口。13项CPU测试与真实日志的Node DOM绘图测试通过；没有浏览器布局/截图验收。源码4文件已封存于本目录 `CODE_SEAL.json`；部署证据 `MAIN_DEPLOYMENT_RESULT.json`。项目解释器未暴露pidfd接口的首次控制前退出也保留，未产生信号；随后采用即时完整身份复核与单PID信号。

验收读到184个图表点，普通训练仍为GPU2/PID3710394，125次优化器更新、3887个训练决策，最新checkpoint为 `checkpoint_000000100.pt`。这不是模型已训练成功或导航效果合格的声明。

## 已重新挂载的生产任务

新计划 `data_pipeline/auto_production_v1/manual_failure_recovery_gpu1_20260910_v1/PLAN.json`，tmux `q35n_manual_recovery_gpu1_v1`，queue PID3746526。为避免和GPU2训练竞争，把两条已失败逻辑lane合并到GPU1串行恢复；不是重复并发启动原队列。

| 顺序 | 原批次 | 新批次 | 本次性质 |
|---|---|---|---|
| 1 | 225 | 500 | 外部资源中断后一次人工新尝试 |
| 2 | 244 | 501 | preworker资源准入失败后一次人工新尝试 |
| 3 | 226 | 502 | 从未执行的预约迁移 |
| 4 | 245 | 503 | 从未执行的预约迁移 |
| 5 | 227 | 504 | 从未执行的预约迁移 |
| 6 | 246 | 505 | 从未执行的预约迁移 |
| 7 | 228 | 506 | 从未执行的预约迁移 |
| 8 | 247 | 507 | 从未执行的预约迁移 |

新准备节点 `data_pipeline/mechanism_runtime_v1/witness_first_v1/manual_recovery_20260910_v2`。V1 CPU准备因读取244旧receipt错误字段保护性失败，未生成QUEUE或启动GPU；V2新增真实receipt回归测试，V1保留。7项恢复测试、八批输入锁和真实Habitat解释器worker准入、精确主授权均通过。

本次独立有界12小时（约12:31:48至次日00:31:48）、最多8批/24候选尝试、0新增独立候选；每批沿用3900秒监督/3600秒factory/60000动作/7GiB/1200秒强审。每批需完整5400秒预约，尾批可能因余量不足保持未启动，不承诺全部完成。原SOURCE_LOCK与failed/partial都只读，人工重试不改成自动无限重试；不能把重复候选计作新的独立来源。

首批500的supervisor PID3746530、worker PID3746564均已启动，读取进度为8条完成trace/956动作，稍后的动作journal已记录1100次确认返回；不同读取时刻不强行对齐。0个已强审合格族，正式强审核尚未开始。原队列框架将在本批自然完成并清理后执行原强审核，再在预算允许时接续下一批；运行/审计命令失败会停止该lane，不自动重试。

验收时共有6条运行中的生产队列（GPU1恢复、GPU3/4/5/7特殊、GPU6普通扩源）和1个GPU2训练任务。健康队列/训练PID/starttime/argv全保持；GPU1原守卫两次原始XML准入通过，没有释放/恢复占位或操作外部进程。

下一次接管先只读本恢复节点 `MAIN_LIVE_ACCEPTANCE.json`、新lane的EVENTS/RESULT和首批运行状态。不要重复运行prepare或queue，不根据旧FAIL表格自动重试历史任务，不编辑封存代码和输入锁。强审仍写原严格审核根下的新batch_500等独立目录，沿用物理/语义/18格/27重放/54求值门槛。
