# 普通导航继续推进：正向结果与下一关

本页接续晚间 `ORDINARY_TARGETED_REPAIR_20260912_ZH.md`；原训练网页仍为 **18766**。

## 已得到的正向结果

同一批 100 条 INTERNAL_DEV、同 batch=1、同物理及计分规则：

| 版本 | SR | SPL | nDTW |
|---|---:|---:|---:|
| 扩产前基座 | 14.00% | 12.67% | 36.74% |
| 扩产首段 4,000 更新 | 21.00% | 18.02% | 36.59% |

新增成功 13 条，失去成功 6 条，净增加 7 条。扩产训练有正向开发信号，但不是全部能力都变好：两个房屋退步，nDTW 基本持平；五屋开发集不足以宣称稳定泛化或论文科学收益。
相比之下，视觉停滞保护没有增加成功，nDTW 降至 35.55%，因此**不采用该保护**。失败记录完整保留，不调整阈值继续追分。

## 正在执行的下一关

用户要求继续推进，因此新建 `sft_acceptance/ordinary_expanded_continue_v2`，从扩产第 4,000 步直接接续到最多第 8,000 步。
保留全部训练参数、AdamW 动量、数据位置与原学习率日程。下一段为 **341,939** 个不与前段重复的指令条件动作：R2R 69,418、RxR 109,925、EnvDrop 162,596；仍是相同的 265 万动作池，不是重新造一份数据。
三卡已实际运行。第 4,200 步保存点验收通过：28 个可训练张量确有更新，优化器计步保持连续，所有张量有限，三卡恢复位置一致，旧源码/输入锁未改变。

训练结束并恢复占位后，自动用固定第 8,000 步检查点做同 100 条纯模型评测，不挑中途最高分，不加入失败的保护或特殊数据。
事前正向工程门槛：相对第 4,000 步，**SR 上升、SPL 不下降、nDTW 下降不超过 1 个百分点**。逐屋、配对新增/损失、留一屋及五屋 bootstrap 均保留。任何失败只关闭本段，不自动重试或启动下一轮长训。

## 查进度与结果

- 现有 SSH 转发下访问 `http://127.0.0.1:18766/`，区分本段新增与扩产累计；闭环结果独立展示。
- 训练：`sft_acceptance/ordinary_expanded_continue_v2/formal/STATUS.json`。
- 串行任务：`reviews/Q35N_ORDINARY_CONTINUE_V2/WORKFLOW_STATUS.json`。
- 首个新检查点验收：同目录 `FIRST_CHECKPOINT_ACCEPTANCE.json`。
- 完整评测后：同目录 `RESULT.json`、`REPORT_ZH.md`、`WORKFLOW_RESULT.json`。
- 本轮评测：`closed_loop_bench/ordinary_expanded_continue_dev_v2/run_001/RESULT.json`。

这一步属于普通导航基座建设，不是 UAD/特殊任务记忆的架构创新。特殊数据、最近邻创新审查和老师版论文故事仍与本轮标准训练严格分开。
