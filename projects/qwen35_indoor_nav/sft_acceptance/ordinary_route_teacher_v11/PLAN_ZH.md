# V11 训练事前计划：有序路点纠错监督

状态：等待data_pipeline/ordinary_route_teacher_v11数据门槛；本文件不启动训练。用户继续直到新正向结果，当前仍无新正向。

唯一工程干预是纠错教师目标定义；不沿用V9 KL或V10时间窗口，部署/训练仍原last2 RGB和last8执行动作。相对V8R1同最佳4k FP32 master初值、AdamW矩/RNG/原时钟4000→5000、LR5e-6、seed1209、1000更新、三rank6144token上限，模型结构/参数不变。

数据规则在新教师结果前固定：取V6原4983种纠错来源ID与V11审核合法ID的交集，输入字节必须相同；全部沿用旧V6 source reconciliation排除普通重复输入的结果。不同reference阶段导致的冲突/unknown由新数据层整组隔离，不混回旧goal标签、不投票。忽略旧集合之外的新ID，以减少覆盖变化。

原V6固定计划的89172普通决策/顺序/标签/权重完全保留；对每个原纠错出现，若ID在交集则替换为新教师标签，若不在则删除这次出现。其它相对顺序和1000个rank更新槽不变，禁止补随机新样本。索引重新映射，原token估计/图数/指令/历史不变，不超过原batch token上限。采用原不变inflection权重规则：目标与上一动作不同或无历史时3.2，否则1；标签变化造成权重按同规则变化，单列数量。不是完全同一标签/权重/决策数，不宣称严格等数据或新增独立数据。

准入需新数据原门槛通过、交集>=1000且覆盖>=8FIT屋、每个rank原1000更新槽非空、恢复比例仍在8%至12%、无新旧普通输入冲突、全部因果输入/像素/目标正确。若不满足不训练，不修改阈值迁就。新索引只变恢复部分，最佳4k FP32 master桥只重新绑定新索引元数据，全部参数/矩/RNG逐张量精确不变。

训练1800s、lease2100s，GPU3/4/5实时核准占位借还；短TMP .t11实际双worker验收，不安装/付费/重启生产。首200只做更新/数值/对账检查，不用中间权重评分。最终1000一次同100条/5屋、batch1、500动作、显式STOP<3m、官方SR/SPL/nDTW及逐动作审核，GPU1实时空闲才运行。

主门槛仍对照纯最佳4k：ΔSR>0、ΔSPL>=0、ΔnDTW>=−0.01。此为多次暴露INTERNAL_DEV工程信号，非独立确认、非UAD/论文创新。保留所有FAIL/原权重/旧标签/输入锁，不自动更换标签规则、补数据或延长训练。新教师定义与可认证覆盖共同构成这次工程数据干预，不将观察到的差值独占归因为某个具体路点机制。
