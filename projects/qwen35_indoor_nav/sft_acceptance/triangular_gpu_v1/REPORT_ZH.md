# 三角求解GPU工程短测：不作为直接替换

结论：`FAIL_DROP_IN_NUMERICAL_CORRECTNESS`，不采用该后端。主训练GPU5/6未改动，GPU7占位已恢复。

CPU6项代数/梯度测试通过不代表真实Qwen完整链条通过。固定一条训练路线前4步，在Qwen3.5-2B初始策略上做同卡forward+backward：参考中位1.8324秒，三角重写0.9424秒，短测速度比1.9444；但logit最大差0.97396超过冻结上限0.07141，梯度relative L2约0.993–1.002、cosine约0.566–0.573，明显不合格。同卡参考梯度重复误差仅0.0261。

因此只能说该实验的计算更快，不能说获得可用的训练提速。局部代数等价、FP32舍入行为和完整循环模型的数值表现不同；未捕获真实内部矩阵或FP64参考，尚不能确定差异来自数值放大、具体后端实现还是遗漏的兼容条件，不武断归因。

GPU7总租借43.29秒，参数更新0、导航episode0、下载0。返回码0表示测试脚本正常结束，不代表候选通过；BENCHMARK.correctness_pass=false，recommended_for_next_version=false。所有当前训练和原失败保持不变。

本节点关闭，不放宽阈值改PASS，不把该局部线性代数优化作为论文原创算法。后续效率方向优先官方训练内核的隔离验收；先完成当前普通SFT终点。
