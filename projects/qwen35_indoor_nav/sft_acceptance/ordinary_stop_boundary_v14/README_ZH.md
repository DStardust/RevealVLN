# 普通导航：集中修提前STOP

固定单项改动：在V13的BCE+L2训练目标上加入同轨迹停车边界排序；λ=1、logit间隔1、正则.001，CPU float64 LBFGS最多200步，无搜索。只有STOP行2049参数学习；Qwen、LoRA、运动行和输入均冻结。距离真值仅离线生成监督，不进运行期。

从既有5487个真实输入、64条物理轨迹中构造888对（32条轨迹、16屋）：3–6m应继续的负状态与同路线时间最近的<3m合法停车状态。888是派生对数，不是新轨迹。不从DEV的6条失败里抽训练帧。

A=原样V13 checkpoint（上轮SR25%）；B=V14固定目标训练候选。每对同一模型进程、同运行配置；GPU1–6分片完成100对。保留标准3m主动STOP、500决策、224RGB两帧和8实际动作。不会用旧25%替代本轮A。目标是减少提前错停，同时保住合法停车收益；闭环结果出来前不宣称提升。

独立服务运行pipeline.py --run-id boundary_001，不依赖Codex在线。原监控18770新增当前V14比较，同时保留V13和100k长训信息。正式结果在runs/boundary_001/REVIEW.json与REPORT_ZH.md，训练梯度/参数更新在OPTIMIZER.jsonl及TRAIN_RESULT.json，真实样本引用在DATA_MANIFEST.json。

六卡每进程8GiB模型上限，为原100k长训的周期测试保留显存；只清理本任务核验的进程组。旧V13、原模型卡与部署不改。没有自动部署，没有论文或未暴露泛化主张。
