# V10：两帧视觉与八步执行历史对齐（普通基座输入修订）
用户要求继续直到第一个正向结果。当前未达成，最佳纯扩产4k仍SR21%、SPL18.0169839%、nDTW36.5879701%。只允许下述一个新输入因素；不是架构创新/UAD贡献。

## 顺序归因与反对意见
V8R1精度修复本身实际生效，但100条SR15%，8赢/14输；V9 lambda1/T1固定输出KL使丢失成功减少到7条、仅新增3条，SR17%，仍未过门槛。停止该系数方案，不扫KL/精度/学习率。训练数据抽检24条三来源、1855决策、120张RGB通过因果时间对应，不能甩锅给已证伪的“一帧错位”假设。
当前视觉只有[t-1,t]，而已执行动作窗口为[t-8,t)。只读FIT64条7225决策：6713个完整八步窗口中4978个八步前画面不同于前一步；1765个相邻图相同窗口中只有30个能由八步前画面变成不同。因此“提供较长运动上下文”有可执行证据，不能宣称解决无限卡住。4983旧纠错ID的首个固定出现均可重建新视图，新指纹4983个、冲突0。导航帮助尚未证明。

## 唯一变更和不变项
每个决策仍1或2张224RGB：t=0用[0]，t>0用[max(0,t-8),t]，仍旧图在前当前图在后。执行动作仍max(0,t-8):t共最多8个。8来自既有动作窗，不从分数选择/扫描stride。
原普通完整轨迹按相同选择器读取旧图片；纠错ID使用原有SUPERVISION中首个合法出现，重建其真实过去，不生成新画面/新标签。旧ID保留为来源键，不再冒称是新视图内容哈希。单独冻结新视图及fingerprint，审核实际本段普通＋纠错的完全重复输入目标冲突。若有冲突则不启动，不覆盖旧输入锁。
对照为V8R1：同最佳4k的FP32 master初始化桥/优化器矩/RNG、同V6的1000更新及99047决策/顺序/权重/标签/采样索引/种子/LR/3rank6144token预算，原CE，无V9 KL。模型结构、参数量、每步编码图数、动作和传感器不变。不把V9最终失败权重作为起点。
训练loader与在线Window必须用相同选择器。在线只缓存最多9张真实既往RGB，实际编码仍最多2张，不使用位姿/目标/场景ID/真值或未来帧。此方法仍是有限窗口，不是持续记忆，也不解决UAD特殊任务。

## 事前验证、预算与门槛
CPU：完整7200余FIT步可重建性、4983新view像素哈希、固定计划89172普通输入＋9875纠错抽取的目标冲突、同图数/动作数、边界/终止/跨路线reset、拒绝特权字段、训练与在线选择一致、旧完整基线分析结果精确回归。GPU首200仅检查真实非零有限梯度/对账，不按分数挑权重。
训练1800s，lease2100s；GPU3/4/5只释放实时精确登记的占位并finally恢复；短TMP项目.t10，经真实双worker传输检查。原七生产队列、特殊池、所有FAIL和冻结源均不改，不安装、不付费调用。仅此一次1000更新，不自动重试/下一轮。
最终固定1000后验收再GPU1空闲卡同100条INTERNAL_DEV、5屋、batch1、500动作、显式STOP<3m、原官方指标。新增日志记录实际送入模型的两帧RGB哈希和全局帧索引；最终逐动作独立按原真实回放RGB核对选择器。指标计算不改；停滞输入指纹分析必须用实际选择图，而非继续假设[t-1,t]。
正向门槛仍对照纯最佳4k：ΔSR>0、ΔSPL>=0、ΔnDTW>=-0.01。信息的时间跨度是本次有意变更，不能声称策略收到完全相同信息；原物理路线、观察传感器和编码图数/预算相同。不是官方val_unseen或独立论文确认。进程/接口通过不算导航正向。

## 复用范围
时间采样属于普通工程操作，未提出新算法或学术独占贡献。NaVILA八帧配方仅是已存RECIPE_DECISION中的历史参考，本次不声称复现其采样规则；本轮网络查询两次连接失败，未冒称新增读取官方仓库或论文。本次选择完全由本地既有八步动作接口和固定对照定义。
