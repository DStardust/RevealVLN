# 扩产数据第二段：保留训练状态的有界接续

用户 2026-09-12 晚间授权「继续推进工作到正向结果」。本次是普通导航基座训练，不是新方法实验。

## 前置证据与失败处理

同 100 条 INTERNAL_DEV、固定 batch=1：扩产首段 4,000 更新相对旧基座 SR 14%→21%、SPL 12.67%→18.02%、nDTW 36.74%→36.59%。新增成功 13 条、失去 6 条；两个房屋退步，不能宣称稳定泛化。
视觉停滞保护 13 条路线 29 次干预，SR/SPL 无改善，nDTW 下降 1.04 个百分点，开发门槛失败；保持关闭，旧失败不得覆盖。

## 唯一干预与不变量

首段只处理 340,712 / 2,650,347 个计划决策。假设：同一规范继续学习下一段专家数据能带来更多普通导航收益。
从 `ordinary_expanded_v1/formal/attempt_001/checkpoint_000004000.pt` 原文件直接读取，SHA c30a0936ce85723ebca8fa95d52e67453200af708e0c237c6654b583ff7f9775。
保留权重、AdamW 动量、数据游标 epoch=0/position=4000、随机状态；不重新暖启动、不重置优化器、不重放计划前 4,000 个 batch。沿用相同 seed=1209、31,059 步一轮计划、5e-5 峰值 LR 的原 cosine 日程与 3% warmup，不重新 warmup。
精确保存状态不等于声明跨进程 BF16 逐位轨迹一致；沿用前版本保存的 rank0 随机状态恢复机制。三卡各自决策计数由固定计划重算。
模型、两帧 RGB／八个已执行动作接口、inflection=3.2、三卡梯度归约和 FLA 内核逐字复用；无执行保护、无目标坐标、无特殊数据、无 DEV/CONFIRM 入训。

## 事前预算与关卡

- 一个新版本、一次尝试；最多额外 4,000 次更新（扩产累计到 8,000）、额外 360,000 决策或 4,200 秒，先到者停止；每 200 更新保存。
- GPU3/4/5 仅借实时核实的项目占位，记录 pane/argv/cwd/PID/starttime，双重检查后停止精确占位，finally 恢复。GPU0/1/2/6/7 不动；GPU1 单独用于后置评测。
- CPU 验收：冻结源哈希、同快照/同 sample index、优化器与游标完整、前后计划决策不重复、学习率连续、无额外部署字段、预算同步、无自动重试。
- 首个新保存点 4,200 更新必须检查有限 loss/梯度、参数实际更新、三卡计数与原继续位置，不能把启动日志当训练完成。
- 训练和资源恢复完整后，仅对固定最终 8,000 检查点做同一批 100 条、同 batch=1、同 500 动作、同物理和官方指标。不是从多个检查点挑最好一个。
- 本段正向工程结果事前定义：相对纯模型 4,000，SR 严格上升，SPL 不下降，nDTW 不下降超过 1 个百分点；并完整报告逐屋、配对新增/损失、留一屋和房屋 bootstrap。技术通过、工程开发信号和论文确认三者分开。
- 任何失败只关闭本段并保留记录，不自动重试、继续下一段或改阈值。没有证明每次继续训练都会提升。

原 18766 继续监控：第二段新旧曲线分段，扩产累计计数和本段增量分别显示。旧 51,301 是扩产前更新数，不能与新 8,000 当作同一日程混算。
