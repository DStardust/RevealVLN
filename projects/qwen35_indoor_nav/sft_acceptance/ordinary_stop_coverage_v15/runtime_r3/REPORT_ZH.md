# RGB入口修复

原始失败起点稳定全黑，正常对照起点可以渲染，同位置左转两次后出现真实场景纹理。没有证据显示全局渲染器失效；初始视向缺可见纹理不能作为RGB接口非法的判据。保留真实黑帧给模型，不修图、不换位姿、不强制转向，不宣称修好了扫描缺口。

R3保持原shape/type、姿态、输入历史、STOP和距离评测；均匀帧额外同状态复读核验并记录统计。6个左转仅是独立诊断，不用于训练或导航分数。旧264条、两次失败和全部成本保留。新启动续完剩余376条，然后原定训练与DEV/unseen配对。
