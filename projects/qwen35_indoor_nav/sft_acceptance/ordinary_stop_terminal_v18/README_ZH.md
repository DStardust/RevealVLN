# V18：真实终点STOP与负例集合归一化

本版修复V17在FIT上暴露的训练问题。V17把一段负例的logsumexp直接当风险损失，额外引入随负例数量增加的偏置；又仅以首次跨入3m的状态为正例，而该状态不一定有清楚终点视觉证据。实际V13在401个真实合法STOP终点中预测STOP240个，V17仅9个。V17计算已受控中止，保留训练、少量完整DEV对和全部失败尝试，不将这些部分导航结果作效能结论；没有根据DEV成功分数选此次修订。

V18维持V13运动输出、底模、640条物理轨迹/37645个因果特征、相同锚点与L2；负例池使用log-mean-exp消除上述长度偏置。正例改为轨迹实际执行且成功的终点STOP输入。其他范围内观察既不贴负标签，也不虚构STOP或新观测。范围外真实观察为负例。未使用已有64条恢复数据，未新增底模训练或物理采集。

执行前冻结FIT sanity：终点STOP召回保留至少参考V13的90%，每轨迹任意误停比例不高于参考。失败则仅封存本版训练并停止，不启动GPU、不继续换参数。通过后完成DEV100完整配对，再依照SR提升且SPL不降的预注册门槛决定是否执行一次公开unseen1839。此门槛不是论文泛化证据，所有已暴露数据如实标注。

六项CPU测试包括真实梯度更新、负例数量不引入均匀margin偏置、空mask数值稳定、运动冻结和500预算。训练结果与导航结果分开。初始化、正例位置及损失共同修正，不声称单独因果效应或论文新颖性。旧V13/V15/V16/V17的证据不覆盖。

后台pipeline与原18770监控均独立于Codex；完成/失败后只清理本任务所有者已核实的进程、归还占位租约。状态读取runs/terminal_001/STATUS.json。预算沿用最多48GPU会话小时/24hGPU阶段墙钟，实际先通过CPU门槛，不追逐低分重试。
