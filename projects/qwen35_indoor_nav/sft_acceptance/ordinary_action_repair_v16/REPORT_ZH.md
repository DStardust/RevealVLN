# 实施回交：已训练，闭环比较待完成

V15采集640/640全部封存，共52192个实际决策、8621个STOP监督正观测，教师无法继续0条；初始RGB问题按runtime_r3保留原始黑帧解决，原来失败的rank279已实际完成，旧失败记录未改写。V15已训练并开始DEV100，之后自动完整公开unseen1839。

V16已完成CPU冻结特征的实际前向、反向和参数更新，37645个相同因果输入、13599个合格教师运动标签，冲突0。最终优化44迭代、53次loss计算，8196个输出层参数可训练，底模更新0。训练拟合与导航SR分开；TRAIN_RESULT包含实际损失、梯度、参数变化和诊断，checkpoint已写出。

V16服务正等待V15完整评测，然后自动执行V15 STOP-only对V16 STOP+motion的DEV100与unseen1839。此时V16完成配对0，GPU前向评测尚未开始，效应UNKNOWN、不自动采用。两轮评测同时修改数据的共同收益不算运动监督的独立收益；运动行训练与额外CE也不拆分归因。

另已从320条V15训练策略轨迹登记226个合格恢复候选，逐屋固定选64条，在本轮评测之前冻结。后续真实回放到错误点再由路线教师恢复，共用500动作预算，无中途传送。新恢复数据不进入本次已完成训练。

诊断细节：house_probe的V16仅用32屋拟合，日志内V15参照是40屋最终头，因此不能将这两个诊断数值称作匹配留出增益；最终40屋诊断则仍为训练池拟合。新增监督作用须由注册闭环比较决定。

独立systemd服务与原18770监控已核验，stdin=/dev/null，无Codex/API依赖。八项CPU测试通过，不能替代本次V16实际GPU闭环验收。查看runs/motion_001/STATUS.json；最终结果由后台pipeline自动写入runs/motion_001/RESULT.json及REPORT_ZH.md。服务遭管理员停止、OOM或机器故障时仍可能中断，证据和完整pair保留。

本次产物有源码锁、协议、真实更新日志/权重、冻结恢复清单、启动和监控凭据。原始RGB/特征缓存及场景资产仍存本地，公开代码不能脱离这些本地依赖直接复现。现有公开unseen已暴露，不是盲测；没有新SR提升或论文创新结论。
