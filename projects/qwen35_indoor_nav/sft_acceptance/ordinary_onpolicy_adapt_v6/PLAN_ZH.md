# V6：访问状态纠错短程训练（普通基座工程配方）

本节点仅在ordinary_onpolicy_recovery_v2数据关和像素/双重重放审计PASS后准入。数据共5365条去重合法建议；已知54条与既有同输入R2R参考示范冲突，隔离。为避免把已有样本当新增监督，训练视图进一步去除所有既有相同因果输入（初查382条含上述54条），预期4983条新访问状态建议，最终以完整CPU交叉审核封存数为准。旧示范和原建议文件均不改写。未知、冲突和覆盖账本全部保留。

固定起点为较好4000步（SHA c30a0936ce85723ebca8fa95d52e67453200af708e0c237c6654b583ff7f9775），不是失败的R2R8000步。普通Qwen3.5-2B+原LoRA/action head不变，不加STOP偏置、部署控制器或特殊数据。

这是新数据阶段：权重、AdamW矩和源RNG保留；为了绑定新的索引和计划，阶段游标显式归零，优化器时间保留4000偏移，不虚称同数据无缝续训。峰值LR采用已试过的保守5e-6，原31059长度/931warmup余弦在4000处继续，固定再1000更新（最终AdamW step5000），不启动新warmup。该配方同时包含新监督、混合比例和短程低LR，不声称单因素因果归因或论文创新。

数据计划：原R2R中排除本分支前4000更新已见索引，从剩余池固定seed1209无放回抽100000个候选；纠错池固定seed1209按完整洗牌轮次形成11111个候选抽取（明确有重复，不计新独立数据）。对111111个候选用原6144 tokens/卡分桶、同3卡算法打包后固定取前1000更新。实际新样本比例及唯一数由CPU计划审核给出，约10%纠错、90%原R2R；不根据评测调比例。原示范inflection权重不变；纠错样本采用相同3.2系数，目标与真实上一执行动作不同时加权，空历史也加权。监督元数据不进模型。

训练限30分钟、1000新更新、CPU计划决策数加一最大batch的计数上限；lease35分钟。epoch=1仅表示预注册的1000更新计划完成，不代表全265万池再训一轮。GPU3/4/5借精确核验原占位并finally恢复；GPU0/2/6/7和外部任务不动。一尝试，无自动重试/第二段。

首个200更新检查：初始参数精确、优化器step4200、矩有限、参数确有更新、旧锁不变、三卡计划计数一致、LR为原时钟+低LR。固定1000更新阶段终点（累计新增分支优化器5000）最终CPU验收，再对原100DEV固定batch1/500动作/原指标完整评测。不得选中间checkpoint。

正向工程门槛仍相对较好4000步SR21%、SPL18.02%、nDTW36.59%：ΔSR>0、ΔSPL>=0、ΔnDTW>=-0.01。所有100条和逐动作审计完成才准入；报告逐屋/留一屋/配对不确定性。已多次暴露DEV的正号仅工程信号，不是独立确认/SOTA/UAD创新。若失败保留并重新诊断，不把数据合格、loss下降或老师建议正确率当导航收益。
