# 工程恢复 R1（恢复前登记）

首次运行在第1次AdamW更新完成后因日志unix字段重复退出，更新记录、失败、成本和GPU恢复证据保留。不是数值/OOM失败，也没有根据dev效能更改算法。

仅修复事件字典的重复参数。原初始checkpoint重载后须与已保存logits完全一致，复用原72条dev指令评估及10个before闭环，不重新评估或替换episode。由于更新1没有保存，无法声称精确续训；从初始checkpoint按同seed、同顺序重新训练399更新，连同旧1更新累计400。最终checkpoint实际包含399次更新，明确不同于无故障计划的400次。

全部源代码差异在RECOVERY_CODE_DIFF.patch，原配置EXPERIMENT_SPEC.json和划分不修改。本修订仅覆盖工程恢复和扣减预算。无新主配置、超参搜索、辅助损失或机制数据。累计GPU墙钟上限6小时、输出20GiB、closed总启动上限20均不变。

