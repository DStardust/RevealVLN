# 固定微批4修订：保留B8等价性FAIL

原ACCUMULATION_INTERFACE_V1在38.77秒被资源扫描目录ENOENT中止，无完整数值分组回执，实际部分前向数未知。R1只修监控后真实完成数值比较，但未通过事前门槛：recent2梯度相对L2=0.0474264/cos=0.9989973/argmax不一致；prefix8相对L2=0.0617577/cos=0.9982107/argmax一致。均不改PASS，不放宽5%/.999阈值，不把两种微批视为等价。

下游改变的是运行配置而非SR目标：两分支均固定microbatch4、每rank累积8次、三rank全局96、同384000样本/4000更新；不使用先前variable4/8草稿。只比较历史输入，不在两分支之间改变微批。该修订不保证收益；普通40%验收与原对照门槛均不变。固定4方案仍需实际GPU检验“逐微批独立梯度求和”与“同样微批顺序原地累积”一致，以及最长真实指令的开训容量检查，不能因为CPU代数通过直接训练。

共享优化配置固定为新建AdamW、lr5e-5、warmup120/总4000余弦、wd.01/betas.9,.999/eps1e-8/clip1；源best4k FP32 master参数逐位相同，旧矩不带入、两分支均重置。原PLAN中的未决定字段仍是历史草案，最终以新PROTOCOL/MAIN_AGENT_APPROVAL为准。B4性能证据仍只有短测，不声称达到B8吞吐。

容量：普通选样原2图估计token最大789；新8图最大1185，固定4的实际目标上界4740，保留6144硬断言。每rank开训前对最高长度4个真实输入做一次前向反向、0更新、参数不变；所有rank通过后才进入训练。此诊断成本单列12前向决策/3反向每分支，不混入384000优化样本。

