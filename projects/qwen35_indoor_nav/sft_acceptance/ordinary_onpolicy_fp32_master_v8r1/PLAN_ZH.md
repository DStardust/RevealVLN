# V8运输R1：短Unix通信路径

V8已完成真实三rank FP32主参数及初始BF16注入一致检查，但DataLoader首次传输因AF_UNIX路径过长失败，尚无成功训练更新。主agent仅精确停止自身租约，三卡占位恢复，旧失败、源码和锁不改。

R1复用同一份FP32初始桥接权重、同1000步计划、同模型与优化器配方，训练源码逐文件保持相同。只把TMPDIR/TMP/TEMP改为项目内短目录LINE/.t8r1，并事前实测双worker张量IPC和实际Unix socket绑定；不清理系统盘、不指向其他项目。独立缓存与输出目录；一次尝试30分钟/1000步，三卡占位finally恢复。

首200步及最终1000步验收、同100条DEV门槛仍按原V8 PLAN_ZH.md。主门槛仍需超过最佳4k21%/SPL18.0169839%且nDTW下降≤1pp；仅优于原V6的19%不算目标完成。无UAD创新、无新数据、无自动重试。源初始参数、矩、随机值和实际BF16输入相同。
