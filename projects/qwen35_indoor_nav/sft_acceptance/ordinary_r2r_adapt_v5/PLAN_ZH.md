# 普通导航 R2R 来源适配 V5（事前计划）

## 已知事实与最便宜的下一项对照

高学习率8,000步、低学习率8,000步、完整一轮31,059步与单个FIT选择的STOP偏置都没有超过纯模型4,000步的SR21% / SPL18.02% / nDTW36.59%。完整一轮实际SR14%、SPL12.99%、nDTW35.37%，不能采用。旧失败和最好权重保留。

假设：混合来源的优化目标与当前R2R闭环任务不够匹配。支持这一假设的仅是来源构成：R2R 532,416决策（20.1%），RxR 862,328，EnvDrop合成1,255,603（47.4%）；尚无因果证明。人工指令不意味着每条完美，合成来源也不等于错误。替代解释是离线模仿的分布偏移、短历史或适配容量不足，均未由本实验排除。

本次只改变继续训练的来源采样，保持4000步参数、AdamW矩、优化器时间、原余弦学习率、3卡6144 tokens/卡、seed1209、加权CE和模型架构不变。对照是已完成的混合来源高LR4000→8000分支；主准入仍对较好4000步，而非退步分支。

## 精确采样和计数

只读37,114记录/2,650,347决策原锁。R2R共8,700条指令、2,898条物理路线、51个FIT屋。重建原混合计划已学前4000更新作为计数前缀，不重新执行；从其余R2R决策建立同一长度分桶算法的无放回计划。这里“未见”仅指本扩产分支前4000步未使用该指令条件化样本，不承诺旧51,301步预训练或同路线其他指令从未见过。

残余R2R候选464,548决策；新段固定4000更新、396,001决策；新段只含R2R且与已学前缀索引不交叉。计划总8691更新，但只运行[4000,8000)，无自动补尾。新计划不冒充完整原数据的一轮；LR参考总长仍31059、warmup931，不因筛选缩短日程。最终全局已学736,713，其中本段396,001；rank0最终245,496。所有其他已失败分支计算另列，不计此分支覆盖。

## 有界运行与事前验收

只借经实时核实的GPU3/4/5原占位并finally恢复；不动GPU0/2/6/7与任何外部任务，GPU1只在空闲时作后续评测。训练70分钟墙钟、4000新更新、420,000新决策上限（以先到者停止），lease75分钟，一次尝试。200新更新做CPU检查：优化器step4200、矩保留、参数有限且变化、原LR和三卡计数正确。固定8000最终CPU验收后，才进入100条真实闭环评测；不选择中间checkpoint，不依据部分评测分数取消或修改。

同一100条/5个INTERNAL_DEV房屋、batch1、500动作、原STOP<3m和官方指标、逐动作回读审计。唯一正向工程门槛：相对4000步 ΔSR>0、ΔSPL>=0、ΔnDTW>=-0.01。报告新增/失去成功、逐屋、留一屋、五屋描述性bootstrap。门槛通过也仅是反复暴露DEV上的工程信号，不是独立确认或SOTA。

不加STOP偏置、不加视觉停滞控制、不混特殊数据、不使用目标/位姿/未来信息作为输入。本次为普通基座来源采样对照，不申报UAD模型创新。若失败，保留并重新诊断，不自动第二轮或搜索参数。
