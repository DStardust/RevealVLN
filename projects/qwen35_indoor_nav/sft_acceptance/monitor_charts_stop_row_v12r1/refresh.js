const el=id=>document.getElementById(id),pct=x=>x==null?'—':(100*x).toFixed(2)+'%';
async function refresh(){try{const res=await fetch('/api/status',{cache:'no-store'});if(!res.ok)throw Error('HTTP '+res.status);const d=await res.json(),t=d.stop_row,e=t.extraction||{},p=t.probe,r=t.result;
let phase='冻结特征提取中';if(t.launch?.status==='FAILED')phase='提取失败，已保留记录';else if(t.parity?.status==='FAIL')phase='特征一致性未通过';else if(p&&!p.pass_gate)phase='房屋留出探针未通过，不启动闭环';else if(p?.pass_gate)phase='探针通过，等待完整闭环验收';if(t.fit_eval)phase='FIT64闭环 '+t.fit_eval.completed+' / '+t.fit_eval.total;if(t.fit_report&&!t.fit_report.pass_gate)phase='完整FIT闭环未通过，不测DEV';if(t.dev_eval)phase='DEV100闭环 '+t.dev_eval.completed+' / '+t.dev_eval.total;if(r)phase=r.positive_development_signal?'完整100条达到事前正向门槛':'完整100条未达到正向门槛';
el('phase').textContent=phase;el('progress').value=e.completed||0;el('count').textContent='已提取 '+(e.completed||0)+' / 5487种输入；冻结参数更新：0；导航新正向结果：'+(t.positive_navigation_result?'已审核':'尚无');
el('probe').textContent=p?'留出STOP F1：'+pct(p.before.f1)+' → '+pct(p.after.f1)+'；precision '+pct(p.before.precision)+' → '+pct(p.after.precision)+'；recall '+pct(p.before.recall)+' → '+pct(p.after.recall)+'（不是导航SR）':'留出房屋指标尚未出齐。';
const rows=[['原51301步',.14,.126726765,.367404866],['当前最佳：扩产4k',.21,.18016983923442284,.3658797007353029],['原精度纠错V6（未采用）',.19,.17527383450573364,.3933786870665479],['FP32纠错V8（未采用）',.15,.1342932342183672,.3143712169898085],['输出约束V9（未采用）',.17,.15405530911805246,.37448192402776925],['时间对齐V10（未采用）',.19,.1689927517533272,.34937565356104655],['路点教师V11（未采用）',.13,.1063728898653936,.2791484632821602]];
if(r){const a=r.after.result;rows.push(['STOP行V12',a.sr,a.spl,a.ndtw]);}else rows.push(['STOP行V12：未完成DEV',null,null,null]);el('results').replaceChildren(...rows.map(row=>{const tr=document.createElement('tr');row.forEach((x,i)=>{const td=document.createElement('td');td.textContent=i?pct(x):x;tr.appendChild(td);});return tr;}));
el('verdict').textContent=t.positive_navigation_result?'达到预定工程正向门槛；仍需独立确认。':'尚未取得新的正向结果，最佳模型继续保留。';
el('resource').textContent='GPU1本次任务：'+(t.launch?.status||'运行/准备')+'；最近显存 '+(t.resource?.gpu?.memory_mib??'—')+' MiB；其他进程发送信号：0。';el('updated').textContent='更新时间：'+new Date().toLocaleString();el('error').textContent='';
}catch(err){el('error').textContent='状态读取失败：'+err.message;}}refresh();setInterval(refresh,10000);
