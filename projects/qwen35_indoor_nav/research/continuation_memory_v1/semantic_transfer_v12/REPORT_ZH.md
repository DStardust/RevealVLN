SCREEN_COMPLETE_NO_POLICY_BENEFIT_MEASURED

本轮完成真实冻结 Qwen 前向和固定诊断头训练，没有获得足以支持方法收益的迁移信号。没有启动 189893 输入的全量特征提取或放大训练。

既有 211 份导出全部通过文件、标签和动作归属检查，去除两个完全相同的可执行族后为 209 族、11 屋。原始 `training_admission=false` 及物理规范化、负控制缺项保持不变；这不是正式研究数据准入。90 族作为 FIT，原已暴露检查屋 82 族，另外三屋 37 族在训练前固定为额外检查分区。额外检查屋未参与本次轻量头训练，不能宣称底模从未见过这些屋。

有限筛查包含 3887 个诊断行、3569 个唯一因果输入。复用 493 个已核验缓存，实际新增 3076 次 best4k 前向，模型只加载一次，完整参数和持久 buffer 前后指纹相同。GPU 占用墙钟 396.006 秒（0.110002 GPU 小时），无底模更新、无新仿真。两种固定诊断头 × 三个固定种子 × 600 步，共 3600 次 CPU 诊断更新，9.687 秒；不是运行策略训练。

|分区|线性头：前置目标/终止目标|MLP：前置目标/终止目标|
|---|---:|---:|
|FIT，90 族|97.31% / 95.82%|99.09% / 99.49%|
|已暴露检查，82 族|60.03% / 70.68%|63.40% / 65.22%|
|额外三屋，37 族|52.05% / 68.52%|52.05% / 62.90%|

表中为当前 SEE2 见证的平衡准确率，三种子平均；不是“到访房间”、累计任务状态或导航 SR。所有保存预测的分区、房屋、数据族混淆表已独立重算。帧和历史相关，只有三间额外房屋，不将这些行当独立泛化样本。有限头未学好，也不能证明特征完全不含该信息。

`ACTION_TARGET_AUDIT.json` 另揭示一个有限候选集合中的目标问题：418 个任务族均存在不同历史共同成功的续接，其中 385 个在完整 500 决策预算内可行；原行为克隆同时监督短路线和多余绕行。这不是非法标签，也不是所有失败的根因证明。后续 V13 用同一真实轨迹池、同一三臂动作筛选规则，测试最短已执行 PASS 候选的动作监督是否建立真实策略记忆依赖。它不是全局最短路标签，不更改旧 Y 或公共导航成功定义。

可复算入口：`review_screen.py`；完整数据索引为 `DATA.json.gz`，无损解压 SHA 见 `DATA_ARTIFACT.json`。运行和预测证据位于 `features_run_001/`、`screen_cpu_run_001/`，汇总见 `SCREEN_REVIEW.json`。没有方法采用、SR40、论文成立或部署完成结论。
