DEBUG_MEMORY_LEARNABILITY_SIGNAL_NO_SUPERVISION_INCREMENT

获得了真实的记忆使用与可学习性进展，尚未获得交叉续接监督的稳定增量，也不能交付论文成立结论。四臂三种子和加强 B2 对照全部完成，没有选择最好种子、检查点或继续换权重追分。

唯一修复是把真实历史分叉的动作监督单独按族平均，权重固定为 1，加入既有损失。此前 52 个 FIT 分叉动作只占 5076 个动作监督的 1.02%，整体动作准确率约 98% 掩盖了关键失败。本次不修改轨迹、标签、模型输入、记忆结构或 STOP。分叉来自有限已执行 PASS 候选的最小动作成本教师，不是人工制造下一动作冲突，不是全局最短路，也不表示其他动作不可能成功。

主比较实际完成 7200 次 CPU 参数更新、2036.225 秒。加强 B2 另完成 1800 次更新、611.384 秒；两组 CPU 任务部分重叠，不把时间相加当总墙钟或独占设备性能。底模更新、新 Qwen 前向、GPU 小时均为零。两个真实缓存测试及加强 B2 的真实更新检查通过；15 个最终检查点均回读，实际动作和参数更新可核验。两次 CPU 测试额外使用 4 次一次性调试更新，与 9000 次正式更新分列。

正向进展的准确范围：Ours 在 FIT 的分叉成对正确数由 V13 的 5/78 升到 30/78。其正确记忆为 30，换成不相容历史记忆后为 1，相同任务状态的绕行 sham 为 27。关键旧事件距离决策点 103–461 步，超出原 8 动作短窗。这证明修复后策略头可学习使用运行记忆；没有证明交叉监督是原因，因为 B1 的 FIT 成对正确数更高，为 36/78。

|模型|种子 1209 CHECK /24|种子 1210 CHECK /24|种子 1211 CHECK /24|三种子合计 /72|
|---|---:|---:|---:|---:|
|N0，无记忆动作头|0|0|0|0|
|B1，记忆＋动作监督|2|2|1|5|
|B2，精确状态线性头|1|1|1|3|
|B2，两层精确状态头|0|1|1|2|
|Ours，交叉结果监督|4|1|0|5|

这些是 24 个已暴露 CHECK 分叉在三个种子下的重复测量，来自 12 族、两屋；72 不是 72 个独立案例。N0 的零上限只针对当前输入相同但教师动作不同的成对模仿指标，不是零导航成功率。Ours 对 B1 的差为 +2、−1、−1，对两层 B2 为 +4、0、−1：首种子优势未稳定复现，原冻结的探索信号条件未通过。

加强 B2 使用真实的四个组合式任务状态标签，读出头为 512→128→4，66180 参数；Ours 为 516→128→1，66305 参数。运行记忆、动作头初始化、样本日程、600 更新和原有数据完全一致。辅助头近似匹配容量但并非相同输入或 FLOPs：B2 监督每个有效前缀时刻，Ours 监督末端交叉查询。不能用线性 B2 较小这一差异维持优势叙事。

CHECK 的逐对交换分析也已核对：Ours 五个正确测量全部在不相容历史交换后变错，并在同状态 sham 下保留；B1 的五个正确测量同样如此。错误记忆还会让原本错误的案例偶然变对（Ours 2 个，B1 5 个），因此只看交换前后总数不能判定谁更会使用记忆。交换涉及全部历史表示，包括空间信息，不能声称唯一分离了任务状态。详见 `MEMORY_SWAP_TRANSITIONS.json`。

自然指令教师动作检查准确率（三种子平均）为 N0 75.63%、B1 75.99%、线性 B2 75.78%、两层 B2 75.70%、Ours 75.71%。这些离线数值不是闭环 SR；本次没有运行 V14 闭环导航，不借用 V10 的结果替代。

所有原始 SEE2 证书和 `training_admission=false` 保留，状态规范化、负控制和独立测试缺口没有被文档或训练消除。没有方法采用、公开全集收益、SR40、真机部署或论文创新结论。现在停止在同一 CHECK 池继续调整 Ours，完成已冻结的 V10 闭环清单。下一次支持论文主张所需的是物理合法的独立数据族、真正的闭环收益以及超过 B1/B2 的证据；当前小样本不证明整个研究方向永远无效。

运行入口为 `run_cpu.py` 和 `strong_state.py`，核验入口为 `review.py`、`review_strong_state.py`。协议、逐步日志、逐族动作与交换结果、完整初始/最终参数都保存在本目录；GitHub 保留代码、协议和可重算 JSON 日志，模型及特征缓存留在本机。主报告 `REVIEW.json` 和加强对照报告 `STRONG_STATE_REVIEW.json` 分开保存，不覆盖旧结果。
