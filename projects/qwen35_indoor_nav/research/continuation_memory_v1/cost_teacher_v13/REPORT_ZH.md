MATCHED_ACTOR_PILOT_COMPLETE — 未获得一致的交叉监督增量信号。

实际完成 N0/B2/Ours × 三种子 × 600 步，共 5400 次 CPU 策略头更新，1393.580 秒；底模更新、新 Qwen 前向和 GPU 小时均为零。九份最终参数已重新加载，完整指纹、参数变化和实际分叉动作全部复核一致。关键旧事件有梯度，运行记忆和动作参数确有更新；这两项事实不等于方法有效。

本轮唯一训练修复：每条真实历史/任务只从已有实际执行成功的续接中选择动作数最少的候选作为动作教师，STOP 计入成本；相同完整因果上下文的同成本动作冲突不监督，同标签重复仅计一次。原始 Y、精确状态、轨迹池、模型和所有对照的输入保持一致。这是有限候选的教师选择，不是全局最短路，也不表示其他合法动作无法完成任务。

|固定种子|B2：FIT 对/26|Ours：FIT 对/26|B2：CHECK 对/24|Ours：CHECK 对/24|
|---|---:|---:|---:|---:|
|1209|0|3|0|2|
|1210|1|0|2|0|
|1211|3|2|0|1|

每对需要同任务、同当前短窗但不同真实旧历史的两个动作都与对应教师一致。N0 三种子均为零，这是不同教师动作下确定性无记忆模型的上限，不是零导航成功率。Ours 对 B2 的 CHECK 差为 +2、−2、+1，未通过预先冻结的探索信号条件。Ours 的不相容历史交换后 CHECK 总计仍有 2 对正确，正确历史为 3 对；不能据此声称已经学会可靠任务记忆。相同状态的无关绕行 sham 和逐屋结果全部保留。

值得继续排错的实测问题：FIT 的普通动作准确率约 98%，但关键分叉几乎都没有成对学会。52 个 FIT 分叉动作仅占 5076 个动作监督的 1.02%。下一项 V14 仅为这类真实分叉增加单独的、按族平均的动作损失；N0/B1/B2/Ours 共用同一损失和轨迹池。补入 B1 可区分记忆架构和交叉结果监督的作用。这是一次明确的损失覆盖修复，不把它本身列为新算法。

边界：原 25 族 SEE2 的 `training_admission=false` 不变，包含状态规范化和负控制缺项；CHECK 已暴露，12 族来自两屋，三种子不是三份独立测试数据。这里只做离线真实策略头的教师轨迹分叉测量，没有 V13 闭环 SR、独立泛化、方法采用、SR40 或论文成立结论。

证据见 `PROTOCOL.json`、`ACTOR_ADMISSION.json`、`run_001/`、`REVIEW.json`；旧 V11 六个演员的同分叉回读见 `PRIOR_TEACHER_BRANCH_EVALUATION.json`。运行入口 `run_cpu.py`，核验入口 `review.py`；运行目录使用排他创建，不能覆盖旧运行。
