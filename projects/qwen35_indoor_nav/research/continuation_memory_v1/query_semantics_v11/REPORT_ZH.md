MATCHED_CPU_PILOT_COMPLETE_NO_INCREMENTAL_SIGNAL

V11实际完成B2/Ours三个固定种子、每臂600更新，共3600次轻量CPU参数更新，耗时1120.556秒。新增GPU小时0、底模更新0、新Qwen前向0、新导航episode0。6份最终权重已读回，运行记忆写入/递归与动作参数均更新；两臂旧事件到末端辅助监督均有真实梯度。4项CPU验收另外使用4次可丢弃调试更新，不混入3600次正式计数。

|种子|B2检查历史对双方正确|Ours检查历史对双方正确|B2检查标签正确|Ours检查标签正确|
|---|---:|---:|---:|---:|
|1209|8/48|6/48|148/216|138/216|
|1210|8/48|8/48|146/216|142/216|
|1211|6/48|8/48|144/216|150/216|

Ours对B2的历史对差值为-2/0/+2，平均0；全部查询准确率为66.36%，低于B2的67.59%。预先固定的原型正向条件未通过。144对是同48对的三个种子重复，不是144个独立新样本；资产仅来自两屋CHECK，且已经暴露，不给强泛化解释。

唯一训练接口修复是将未来查询的随机类型词向量/GRU替换为既有核验后缀语义。运行记忆、动作、输入窗口、数据、初始化、采样序列、更新数和损失权重沿用冻结实现；本轮B2/Ours均为CPU FP32。跨版本还存在CPU/GPU训练差异，不能把V10→V11的分数变化单独归因于查询修复。本轮对照只比较同时训练的B2/Ours。

准确率与旧事件梯度都不等于策略使用有效记忆。尚未运行V11闭环，不采用、不称SR收益或论文成立；PAPER_PLAN_ZH.md仍是候选方案。既有SEE2证书中的规范化/缺控制问题和training_admission=false不变，没有将该数据升级为正式训练或盲测数据。

下一项诊断需要区分冻结观测特征不能辨认当前见证与递归记忆不能保留旧见证；这些原因不能从本结果直接断言。下一轮若改结构，必须由这项诊断支撑，并保持强B2对照。不得从上述三个种子中仅选1211维持交叉监督优势。

运行命令：CUDA_VISIBLE_DEVICES='' projects/qwen35_indoor_nav/.envs/q35n_qwen_g2_v1/bin/python3 -I -B projects/qwen35_indoor_nav/research/continuation_memory_v1/query_semantics_v11/run_cpu.py。该入口为一次性run_001，不覆盖已有结果。读回命令使用同解释器执行audit_checkpoints.py，再用仓库标准库Python执行review.py。结果见run_001/RESULT.json、CHECKPOINT_AUDIT.json与REVIEW.json。
