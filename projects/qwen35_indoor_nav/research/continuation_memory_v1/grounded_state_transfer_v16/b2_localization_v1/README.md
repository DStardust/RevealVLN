# B2 冻结特征定位

独立服务 `q35n-b2-localize-20260921-02.service`，三个 CPU 种子并行，最长一小时。无需 Codex 在线，不运行新导航、不加载底模、不更新已有九个策略。仅训练诊断探针。

1. 从已认证原始轨迹按原 SEE2 编译器重算当前 anchor/terminal 事件。当前窗口按父族去重；真实因果历史按父族、任务、累计输入去重。FIT/DEV 的房屋、父族及输入隔离保持不变。
2. 当前事件的线性/128宽MLP探针：仅 FIT，400步固定终点，父族与正负类均衡，三个seed；不按DEV选择模型或阈值。
3. 读取九个 final1200 模型，在教师轨迹上计算状态、动作和间隔诊断。B1/B2记忆再训练 FIT-only 线性历史状态读出器；不回传到策略。
4. B2 在固定每 FIT 屋两个父族上测动作/辅助/保持损失对 writer+recurrent 参数的梯度关系。只计算梯度，不做策略更新。

产物在 `run_002/`：STATUS、COUNTS、BINDING、每seed进度/探针权重/预测、模型前后指纹、RESULT 与 REPORT_ZH。事件探针失败不能证明视觉信息不存在；正确状态下教师动作不一致也不能证明全部替代动作非法。局部梯度冲突不证明移除损失能提高SR。

已暴露单DEV屋，仅用于定位下一项修复，不宣称方法创新或普通VLN收益。所有旧源码/权重/结果只读保留。

首个诊断预处理因教师动作唯一假设失败，零探针更新；代码与日志保存在 run_001。修订后保留12个FIT同因果输入的多教师动作集合，仅改变诊断计数，不改标签或重训策略。
