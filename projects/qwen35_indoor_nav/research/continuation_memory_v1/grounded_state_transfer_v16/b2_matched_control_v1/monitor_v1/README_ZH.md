# 本轮实时训练 / 测试监控

沿用原地址：http://127.0.0.1:18770 。页面每 5 秒自动刷新，无需 Codex 在线。

远程访问仍使用之前的端口转发：VS Code「端口 / Ports」添加 18770 并打开浏览器；或在本地终端运行 `ssh -N -L 18770:127.0.0.1:18770 用户名@服务器地址`。

监控本轮 b2_matched_control_v1/runs/matched_001：九模型训练步数、最近 loss/梯度、八卡状态、128 完整组 / 1152 续接、三臂主任务和控制任务结果、B2 对 B1/Terminal 的配对胜负、未封存尝试与失败日志。

主任务与控制任务固定分母均为每臂 192；只纳入完整并封存、哈希和前缀核验通过的九模型组。部分结果不是最终方法结论。这是同一个已暴露 DEV 房屋的开发对照，不是普通 VLN-CE 测试。

独立只读监控服务：q35n-b2-monitor-20260921-01.service。旧监控服务已停止，旧监控代码和实验资产保留；训练服务未重启。

标准库实现，不加载模型、不占 GPU、不调用聊天 API、不修改运行源码或实验记录。监控断开不停止实验。详细状态可运行 `systemctl status q35n-b2-monitor-20260921-01.service --no-pager`。
