# 当前评测的加速核查

结论：8张卡已持续并行补批。当前没有已经证实、又不改变冻结运行源码和数值路径的即时加速改动；本次没有停进程、抢卡、改线程或启动额外GPU任务。

只读统计已封存的14批、112组、784条臂轨迹（未读取成功率或结果分数），总计28503.63 GPU会话秒：

|计时范围|秒|占工作进程墙钟|
|---|---:|---:|
|加载＋初始模型/头身份核验|323.38|1.13%|
|模型generation|4316.37|15.14%|
|观测编码＋记忆写入|1735.81|6.09%|
|现有日志未单独计时的部分|22128.07|77.63%|

16,785次generation的p50/p95为239/438毫秒；66,095次观测编码＋记忆写入为26.15/27.14毫秒。后者包含观测编码，不是仅递归记忆模块的耗时。模型/头加载和初始哈希合并计时，不能声称已测到单独哈希成本；最终状态哈希也没有单独计时。

硬件快照中8卡各占约21–24 GiB，剩余约8–11 GiB，均有模型进程；瞬时0%利用率不意味着卡已空闲。5秒/proc采样显示每worker约1个CPU核、209个线程，192核均可调度。追加3秒线程采样确认主要是主线程工作；不是209条线程都在竞争CPU。服务没有CPU配额、节流或OOM记录。短采样的物理读盘为0，不能据此排除全部启动I/O，也没有证据把慢归因为NAS。

77.63%只能称未归因开销。源码中每个query复制tokenizer、每个臂重建Habitat环境，是需要测量的候选，不能把源码观察直接写成瓶颈证明。下一次独立基础设施检查应分别计时：环境创建/切场景/reset/step/close、RGB与深度预处理、tokenizer复制及模板分词、输入哈希和传输、日志/audit、最终状态哈希与换批启动。现有冻结运行不插入这些修改。

状态快照：138/369组已记录、112组已封存，累计10.3324 GPU会话小时；这是核查时刻的进度，不是最终结果。全部精确计数、来源attempt SHA、采样局限见`ACCELERATION_REVIEW.json`。
