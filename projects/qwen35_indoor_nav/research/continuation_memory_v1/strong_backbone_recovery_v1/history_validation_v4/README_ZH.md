# 历史机制检验与冻结模型扩展验证 V4

这是用户于 2026-09-26 授权的后续验证，延续 `paper_narrative_20260925/WHOLE_VLN_PROGRAM_V1_ZH.md`，不改旧冻结规格、已训练权重或既有结果。

1. 固定确认实验的六个最终模型。CPU 在全部64条已记录 DEV 恢复/成功轨迹上，比较完整历史、相同记忆的 sham、最近8步、当前一步以及颠倒8步之前历史。干预时保持动作特征、原生 logits、模型权重相同，将干预状态的 L2 范数匹配原状态。LOCAL 是旧历史不敏感的负控制。完整分母、帮助/损害、CE 与范数误差全部保存。这不是真实自主轨迹上的干预，也不能独立证明语义记忆。
2. 冻结原生与六个模型，直接复用确认实验的 worker，在所有剩余合格物理路线运行。排除当前 StreamVLN 分支所有登记清单中的路线及其指令改写、旧记忆训练房屋与当前 FIT/DEV 房屋；不读分数选择样本。旧200条单列，不与新增路线混成确认性主结果。公开 benchmark 及这些房屋已有历史暴露，不声称盲测或新房屋。
3. 同组七臂同进程；原输入、处理后输入和原生动作前缀审计沿用旧实现。完整组与模型状态封存后计入；旧半组保留为失败尝试。记录模型状态前后指纹，无参数更新。新路线正负结果都完成，不按分数重试。

真实分母及源码/权重 SHA 以 `runs/validation_001/PROTOCOL.json`、`DATA_MANIFEST.json` 和 `SOURCE_LOCK.json` 为准。CPU 测试见同目录 `CPU_TEST_RESULT.json`。

独立入口：项目控制 Python 调用上一级 `standalone.py start NAME -- CONTROL_PY -I -B pipeline.py --run ABS_RUN`。恢复需新服务名指向同一 run 并加 `--resume`；不可重复启动。pipeline 自动完成 CPU 检验、最多八卡扩展评测和复核，结束时由原占位控制器恢复用户占位。0–7 卡为用户既有授权范围；64 GPU 会话小时/24墙钟小时/32 GiB 为沿用的资源上限，非时间预测。

监控仍为 <http://127.0.0.1:18770/>；新 API `/api/history_validation`，`/previous` 保留历史面板。服务独立于 Codex 和终端，不承诺跨断电、OOM 或管理员停止。

新增路线是本轮效应主结果。CPU 诊断只说明固定读出器对历史内容/顺序的敏感性；等范数不能消除全部分布偏移。尚未训练或验证新的数据贡献、交叉结果监督或整篇论文方法。
