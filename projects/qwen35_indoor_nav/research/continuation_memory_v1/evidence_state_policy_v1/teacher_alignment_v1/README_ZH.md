# 教师监督修复 V1

本轮测试监督目标对缺失前置事件恢复的影响。当前不是有效收益报告。旧 STOP 修复分支保持关闭。

- 保留原 MONOTONIC 8×64 架构、best4k、2048 特征、普通动作池、状态/事件/KL 权重和全部原始物理轨迹。
- 两臂 ORIGINAL/ALIGNED，种子1209/1210/1211，每模型1200步；共享初始化和逐步 schedule，底模更新0。最终检查点固定。
- 只从 FIT 原先已选的实际 PASS 教师中，登记接管点及其后第一次任务就绪位置。新增202次完整物理重放以实际执行 STOP，对244条教师记录认证替代动作。
- ALIGNED 修改该位置的动作目标并屏蔽其后的动作模仿。两臂的状态/事件/KL 仍来自相同原始实际轨迹；原尾部不是替代 STOP 后的虚构观测。辅助梯度曝光相同，动作标签数差异完整披露。
- 原有12个完整因果上下文的运动教师冲突仍保留，不在本轮额外解决。
- 原暴露 DEV：16变体/8父族/1屋，128条件×6模型=768次；每臂主任务192、task_T192。主任务 missing历史96/臂单列。全历史与 STOP 共用500预算，不改 safe_v16 成功定义。
- 继续信号需主任务正差至少2个seed、missing层正差；报告已保持成功、task_T与成本，不能把一个已暴露屋的收益称作论文新颖性或普通导航增益。

独立服务自行完成 prepare→collect→build_data→train→train_review→evaluate_continuations→review→publish。不依赖终端或聊天会话，基础设施错误保存状态并退出，恢复以新服务名、相同run-id加--resume。训练每200步保留优化器/RNG；评测只合并完整六模型条件组，旧半组保留。

资源沿用获准的0–7卡，暂时释放本用户已核验占位，结束后恢复。累计上限12 GPU会话小时、单工人连续3小时、产物40GiB。真实吞吐和资源竞争记录于RESOURCES/MONITOR；不按成绩重试。

进度网页沿用原端口18770，显示实际物理认证、训练步数、封存条件组和完整分母；自动发布代码、六个新head及文本证据到现有GitHub分支。许可场景/原始数组和大缓存留服务器。
