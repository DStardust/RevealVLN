READ_ONLY_FIRST_DIVERGENCE_LOCALIZATION_COMPLETE

完整检查原评测的192个主任务配对：13胜、23负、43双方成功、113双方失败；不是新增导航结果。
读取六个冻结轻量模型，CPU 完整因果递归 1862 步；0 新底模前向、0 训练更新、0 环境动作、0 GPU 小时。最终定位耗时 23.05 秒。

定位结论：这次 EVENT 退化主要表现在已学习的动作路径发生改变。事件状态校准改善没有保护原来的动作能力；现有证据不支持把问题直接归为记忆容量不足。

|23个退化配对的首分歧证据|数量|
|---|---:|
|第一步自主决策已经分歧|12/23|
|两臂四项状态的0.5诊断分类相同|21/23|
|两臂历史状态分类相同|23/23|
|ORIGINAL合法STOP，EVENT继续运动|11/23|
|上述11个中，两臂都预测ready≥0.5|9/11|
|主要不利动作margin变化来自动作路径|22/23|
|只替换成ORIGINAL预测状态，可恢复该步原动作|1/23|
|只替换成ORIGINAL完整动作路径贡献，可恢复该步原动作|19/23|

分数拆分直接使用实际日志和真实模型权重：method_logits = core.action_logits + state_action(predicted_state)。底模原生logits在同一前缀完全相同。按“ORIGINAL选中动作−EVENT选中动作”的margin，分别计算动作路径与状态分支的变化；22/23指较大的不利项，不是声称其他分支完全无关。
core.action_logits包含原生logits与当前特征/递归记忆的动作残差；这里的core不是说冻结Qwen发生更新。四位状态用0.5仅作描述，不是部署STOP阈值。分类相同也不代表概率或状态都正确。

进一步分离动作读出与递归记忆：23个退化配对中，16个有完整共同前缀缓存，可做真实CPU递归复算；7个缺少中间因果特征，保留为不可定位，未补未来输入或新跑底模。
所有完成复算的胜负配对最大CPU/现场logit差为 1.43051147e-06，原动作均一致。固定校验界为1e−4，没有按结果放宽。
- 在16个退化复算样本中，保持EVENT记忆与状态分支，只换回ORIGINAL动作读出，7/16恢复该步原动作。
- 保持EVENT动作读出与状态分支，只换回ORIGINAL递归记忆，0/16恢复该步原动作。
- 共同换回ORIGINAL递归记忆与动作读出、保留EVENT状态分支，12/16恢复该步原动作。
这些都是单步诊断替换，混合组件可能偏离各自训练分布，不能写成“救回7/19个episode”或新SR，更不能据此直接上线拼接模型。

具体例子：condition104 / seed1209。第20步，两臂历史概率均1.0，真实任务已满足；EVENT终点概率0.9994，比ORIGINAL的0.9849更高。ORIGINAL选择STOP，EVENT右转。STOP−右转margin从+0.9734变成−0.3815，其中动作路径贡献−1.4294，状态分支贡献+0.0745（反而略有帮助）。只换回原动作读出可在该输入恢复STOP。
另一个限制例子：condition74 / seed1211，两臂都低估真实历史，EVENT低估更重；动作路径与状态贡献共同不利，单独换一个组件不能恢复STOP。因此不能说事件识别已经全部解决，也不能说只修动作头一定能挽回全部损失。

23个退化配对最终表现：16个耗尽预算、6个在终点条件未满足时STOP、1个在历史条件未满足时STOP。原轨迹只存碰撞总数，无法精确确定首次碰撞时刻，不能由汇总数编造时序因果。
退化涉及6个父族、12种首次分歧原始输入；seen/sham与多个种子有关联。21/23来自已经见过锚点的历史，2/23来自缺失历史。不得把这23个当作23个独立场景。

非选择性检查：13个获胜配对也全部进入CASES.json；其中7个可完整缓存重放，6个缺缓存。双方成功/失败的全部配对也保留首分歧及代数分数拆分。不能只用退化子集推算新方法总体收益。

下一步建议：继续保留ORIGINAL。若另行验证修复，优先检验能否在固定原动作/记忆路径的条件下改善事件分支，而不是再次联合重训所有轻量参数或扩大记忆。先明确原有成功不能丢失的检查，再做完整配对；本次没有启动该训练，也没有证据承诺它会提升SR。
因果解释边界：这次唯一训练条件变化是事件BCE的监督测度，因此其整个训练后的政策差异可配对归因于这项干预；但局部组件替换只能定位决策机制，不能证明优化历程中的每条梯度路径或完整闭环修复效果。

初版诊断001把runtime input_key直接当作缓存key，两个命名空间不同，导致缓存可用性被低估。002按冻结运行器实际采用的instruction+RGB哈希窗+执行历史匹配，并额外核对processed输入。001只读保留、由002取代其缓存可用性结论；原实验不受影响。两次均无训练/仿真。
复核入口：diagnose.py、tests.py、CASES.json、LOST_PAIRS.csv、INPUT_MANIFEST.json。六个模型前后指纹不变；5项CPU检查通过。
