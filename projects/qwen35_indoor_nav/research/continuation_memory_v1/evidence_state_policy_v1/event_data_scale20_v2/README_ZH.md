# 20倍数据补量批次 V2

首批17屋父族搜索完成，合格599个，距新增800目标少201；还有终点缺失变体在收尾，这些不会增加父族。首批结果不改。

本批从既有ordinary FIT清单事前排序，排除旧机制数据/语义池屋、当前DEV/TEST/holdout及ordinary CHECK，选16个真实资产屋，每屋最多32父族（新增容量512）。这些房屋已用于普通动作训练，明确属于FIT，不宣称底模未见。候选未看任何方法分数。标签、阈值、原始数组审核、1米父族起点间隔、四历史三真实后缀、500预算与STOP不加帧全部继承。

两批合计达到800时，不再启动新房屋，已启动屋完整完成，余下屋记录NOT_NEEDED_AFTER_TARGET。物理候选不合格仍保留拒绝记录；不存在拿变体或标签填父族数。最终导出两批合并、按完整房屋/父族分组的DATASET.json。尚未训练，不承诺模型改善。

独立systemd任务先使用首批空出的卡，再自动使用其余已完成卡。通过原占位控制器的独立PID租约共享授权资源，仅接受身份已核验的首批服务租约，不停止首批、外部任务或占位控制器。最后一项任务完成后恢复占位。

同一网页127.0.0.1:18770分列两批父族和变体，合计进度指向800。磁盘扫描沿用R2异步版本，不阻塞物理生成。运行入口：pipeline.py --config PROTOCOL.json --run-id supplement_001；恢复用新服务名与 --resume。

