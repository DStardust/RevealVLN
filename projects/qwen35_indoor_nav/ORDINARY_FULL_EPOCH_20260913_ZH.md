# 普通导航：保留较好基座，完成新数据的一整轮覆盖

本页是2026-09-13当前工作的入口。原网页与只读接口不变：`http://127.0.0.1:18766/`、`/api/status`。当前版本 `ordinary_full_epoch_v4`。

## 已确认的结果，不混在一起报

以下均为相同100条、5屋INTERNAL_DEV、batch=1、最多500动作，非完整官方val_unseen。

| 版本 | SR | SPL | nDTW | 决策 |
|---|---:|---:|---:|---|
| 扩产前基座 | 14.00% | 12.67% | 36.74% | 历史对照 |
| 扩产首段4,000步 | 21.00% | 18.02% | 36.59% | 目前保留的较好普通基座 |
| 原学习率8,000步 | 14.00% | 12.32% | 33.29% | 中期开发门槛失败，不部署 |
| 低学习率8,000步 | 13.00% | 11.44% | 38.01% | 不采用 |
| FIT选定停止偏置后的4,000步模型 | 16.00% | 14.87% | 43.47% | 不采用；新增8条成功但失去13条 |
| 完整一轮固定31,059步 | 尚未评测 | 尚未评测 | 尚未评测 | 等待整轮训练及固定终点核验 |

停止偏置1.75在FIT上使SR25%→31.25%，但没有迁移到开发房屋，不能当验证收益。真实DEV的8,090个动作逐步审核通过，和唯一固定参数的因果前缀预测一致。此方向收口，不继续追阈值。此前视觉停滞保护也没有SR/SPL收益，不启用。

## 当前为什么训练完整一轮

新池有2,650,347条指令条件决策，但4,000步只覆盖340,712条（约12.9%）；原8,000步累计682,651条（25.8%）。此前还没有用完整新池训练过一轮。短段收益和退步都保留，不能替代整轮覆盖的检验。

现在沿原学习率分支的8,000步数值状态，补完原定[8,000,31,059)计划，新增1,967,696条未覆盖决策；完整一轮恰好2,650,347条，无丢弃尾批。8,000步只是接续状态，不被重新判为合格模型；4,000步较好权重原样保留。

模型、输入窗口、动作集合、损失、数据顺序、优化器动量、游标、seed1209、原5e-5峰值余弦调度均不改。不启用STOP校准、视觉停滞保护或特殊数据。低LR失败分支重复学习的341,939条另列计算成本，不算新增数据覆盖。

## 边界与验收

- 三卡GPU3/4/5，仅借精确核实的旧占位，成功/失败/中断后恢复；GPU0/1/2/6/7和其他任务不动。训练上限14,400秒，lease上限14,700秒，单次attempt，不自动第二轮。
- 首个8,200步已通过CPU状态审核：新增16,601决策，28个参数张量变化，AdamW step=8,200、LR=4.3157551e-5，参数/动量有限，源锁与旧输入锁不变。检查点SHA256 `20d7ef6fbb3ef882f6369c58343d4e164a095928fd695922a67fcbba6efa3851`。
- 固定最终epoch=1、position=0、updates=31,059；先审核最终状态和占位恢复，再在GPU1空卡进行原100条真实评测。不挑中间checkpoint，不以损失下降代替导航收益。
- 主对照为较好4,000步：SR严格上升、SPL不降、nDTW下降不超过1个百分点。全部报告配对、逐屋、留一屋和描述性五屋bootstrap。失败不替换较好基座，也不自动追加轮数。

## 证据位置

- 当前训练：`sft_acceptance/ordinary_expanded_full_epoch_v4/`，事前范围在PLAN_ZH.md、RESUME_AUDIT.json、PROTOCOL_FILESTORE.json。
- 当前工作流与审核：`reviews/Q35N_ORDINARY_FULL_EPOCH_V4/WORKFLOW_STATUS.json`、FIRST_CHECKPOINT_ACCEPTANCE.json；完成后生成FINAL_CHECKPOINT_ACCEPTANCE.json、RESULT.json、REPORT_ZH.md。
- 固定终点评测：`closed_loop_bench/ordinary_full_epoch_dev_v4/`。
- 停止校准完整失败与正确性修复：`reviews/Q35N_ORDINARY_STOP_CALIBRATION_V1/`。临时目录扫描竞态和float32官方SPL对齐均保留旧失败，用新版本修复；没有放宽评分或科学门槛。

这一轮是普通导航基座的工程建设，不是UAD任务记忆架构创新。即使开发结果改善，也必须与论文独立确认、真实居家机器人效果分开叙述。
