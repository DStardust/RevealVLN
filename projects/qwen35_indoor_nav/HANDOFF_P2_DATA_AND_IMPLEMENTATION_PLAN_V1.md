# 交给执行 Codex：P2 数据与实现规划

日期：2026-09-09。此文件为完整交接 prompt。主 agent 负责主线冻结、科研裁决和成果验收；执行 Codex 只完成本节点并回交，不自行推进下游。
这是交接准备，不代表执行会话已经启动。无需完整历史对话；在同一服务器阅读本文件与指定资料即可接手。

---

你是 Q35N 项目的执行 Codex。请完成 `Q35N_P2_DATA_AND_IMPLEMENTATION_PLAN_V1`，把已冻结 VLN 算法落实为可实施的数据、模型接口和资源规格。完成后停止并向主 agent 回交。

## 1. 唯一目标与当前事实

最终产物是 VLN 导航模型，Qwen3.5-2B 仅是预训练 VLM 底座。
主方法已经选定：物理合法的交叉续接数据编译，联合监督实际参与导航的紧凑执行记忆与动作策略。
不是旧 B/VLFM 的插件，不是 UAD 标签分类，不是离线问答 benchmark，不重新选择论文方向。

当前 `PASS_FOR_SPECIFIED_CLAIM` 是具体算法的研究预审，非全球新颖性保证、贡献充分或科学收益 PASS。
本线数据与运行环境未验收，无新增训练或导航结果。主 agent 尚未批准算法实现、安装、下载、仿真、训练或 GPU 控制。

本节点必须回答：**用哪一个明确数据/模拟器来源，以哪些真实 API、数据字段和模型接口，能构建第一个可检验的历史—续接族，以及下一次工程验收要怎样执行？**
不要只复述冻结方案或罗列多个可选环境；选一个有依据的首选，只有首选存在明确风险时保留一个备选。

## 2. 范围、读写与协作

项目根：`/mnt/data_nas/deeprobotics/daiyang/vla`。
本线根：`/mnt/data_nas/deeprobotics/daiyang/vla/projects/qwen35_indoor_nav`。
你本轮唯一输出目录为本线内 `reviews/Q35N_P2_DATA_AND_IMPLEMENTATION_PLAN_V1/`。

- 只读检查项目内相关资产、清单和源码；禁止跟随指向项目外的软链接，禁止运行旧线可写环境。
- 可查阅官方网页、模型卡、源文件与许可证。不得下载权重、数据集、依赖包或安装环境；不克隆大型仓库。需要留证时只保存必要文本证据于输出目录并注明来源。
- 不调用模拟器，不 import/inference 模型，不运行算法，不创建算法实现或启动脚本，不操作 GPU/tmux/占位进程。
- 可以进行 JSON、路径、链接和哈希等静态检查；不得将它们称作科研实验。
- 用 apply_patch 写文档、声明式 schema 和配置草案。不修改 STATUS、README、AGENTS、主线冻结或历史报告，也不修改 A/B/C/UAD 和根 NEXT_EXPERIMENT。
- 不自动创建子 agent；本节点由一个执行会话完成。你不是独立人工审稿人。
- 如输出目录已有内容，先检查是否为当前节点成果；不得覆盖另一个会话结果。有版本冲突则报告并等待主 agent 裁决。
- 网站失败时可用已配置 proxyon；不输出代理凭据。不向第三方发送未发表的本地方案、代码或轨迹，检索限公共接口与概念。

## 3. 开始前必须完整阅读

根 AGENTS.md、本线 AGENTS.md，然后依次阅读本线：

1. README.md、STATUS.json。
2. MAINLINE_FREEZE_V3.md。
3. 06_END_TO_END_METHOD_AND_VALIDATION_V1.md。
4. reviews/Q35N_P1_PAPER_CORE_ADJUDICATION_V2/REPORT_ZH.md 与 result.json。
5. ROBOT_NAV_BACKBONE_CONTRACT_V1.md。
6. 02_METHOD_PREFLIGHT.md、03_DATA_CONTRACT.md、04_VALIDATION_PLAN.md、05_ISOLATION_AND_HANDOFF.md。

02–05 中历史候选被 V3 覆盖；不能恢复预算反转、隐藏世界造数或旧 G0B 专属任务。
若需要科研技能，按项目 research/SKILLS.md 核实可用性，主执行会话先完整读取 SKILL.md 和所需引用文件；如不可用，如实记录，不冒充已使用。
这次是接口和可行性规划，不重新进行全领域创新搜索。发现直接同构先例或实质科学漏洞时，记录具体证据回交，不隐藏，也不自行换题。

开始时记录上述受保护文件 SHA256；结束时复核。若并行修改发生，报告差异，不回滚别人的修改。

## 4. 按顺序完成六项工作

### A. 首选资产与接口决定

以 rg 定向读取现有资产登记和源码，不扫描项目外内容。
确认一个用于最小机制数据的首选环境，以及普通导航示范的来源。V3 不要求移动物体，因此不要机械沿用 V1 对 AI2-THOR/ProcTHOR 的偏好。
每项登记：真实项目内路径或缺失、官方来源、版本/提交、许可限制、旧线暴露、是否允许只读复用、未来新增磁盘量及其依据。
未验收 B15 语义栅格不作真值；EgoCoT 测试视频和答案不用于训练。
候选数据没有可靠语义事件时，不能以 LLM 猜测补成正式真值。

### B. 一个具体的最小真实历史—续接族

不是仅写“两个历史到同一位置”。给出所选接口下可构造的：

- 一个结构化任务及确定性语言，过去事件和当前条件分别定义。
- 至少两条关键历史不同的真实路线，另有无关绕行对照；一个共同物理状态和最近公共尾段。
- 至少两条合法续接，明确任务条件和预算下的预期 Y 表，并标注为纸面预期、尚未重放。
- 一个语义清楚的交叉任务，重算全部标签，不能沿用原任务记忆。
- 关键事件的可观察证据、实例/区域语义来源、歧义与 unknown 条件。
- 如何匹配姿态、朝向、门/对象状态、动作预算和传感器；如何核验真实历史确实到达汇合状态。
- 如何生成满足残余任务的正确恢复示范，如何标记错误历史动作的 action_loss_mask。

对每项操作映射真实 API/源码：初始化、执行动作、快照/重建、可达性、事件读取、观测、共同状态校验、终止检查。记录版本和引用位置，不虚构函数名。
若只能在图导航成立，不能冒充连续动作环境成立；明确与目标评估协议的接口差距。

### C. 数据 schema 与自动证书

分别给出 policy_input、supervision_only、family_manifest 的声明式 JSON schema，以及清楚标明“示意、非实测”的样例。
覆盖家屋与族分组、历史/续接/任务 ID、因果截止、实际动作、观测引用、事件证书、Y 的 pass/fail/unknown、正确动作及 mask、来源和生成失败。
policy_input 白名单禁止场景 ID、真值坐标、任务程序隐状态、未来 q、Y 和文件名侧信道；样本 ID 只作加载器索引。
定义同族采样和权重，避免交叉矩阵大小造成重复计数。训练/开发/确认按家屋整体分组，旧线暴露单独登记。
列出泄漏、错误标签、非法重放、捷径和样本排除的自动检验条目；静态数据定义不冒充证书已通过。

### D. 三个实现包的真实接口规格

只给规格、张量流/伪代码，不写算法源代码：

1. 数据编译与校验：输入、输出、共享状态合法性、失败码及缓存身份。
2. VLN 模型：Qwen3.5-2B、连续记忆读写、动作读出、训练专用续接读出。
3. 训练与闭环评估：loss mask、族采样、同初始化消融、状态重置、终止和逐 episode 记录。

核查官方 Qwen 模型/config 与实际实现中的视觉输入、embedding/hidden state、位置编码、缓存和可训练适配接口；注明核实来源和精确版本。
不能因为其他 Qwen 版本支持某接口便默认本版本支持。不能仅画“插入 memory token”而忽略多模态位置/缓存兼容性。
给出因果 m_{i,k} 的计算与 q_j 读出隔离；换指令重算记忆；未来查询不得修改前缀或动作缓存。
定义短序列端到端梯度验收，明确如何确认关键早期事件的记忆写入能收到监督；长序列截断方案单列，不先声称已能训练长历史。
参数高效微调位置、记忆槽数、视觉窗口、动作尺度和精度选择需依据；无法确认则明确缺口与最便宜的后续验收，不用任意数值伪装实测。

### E. 资源与第一工程节点协议

列出数据、权重、独立环境、缓存的新增磁盘与内存/显存预算；计算估算和实际测量分开，证据不足给范围与假设。
目标机器人硬件型号未知，不推定实时或量化必然无损。部署只保留 VLN；训练读出器移除不意味着记忆零开销。
输出下一次小规模数据工程验收草案：固定范围、样本族、重放次数、种子、检查项、数值工程判据、资源上限和停止条件。数值由契约和资源依据确定，不事后按结果放宽。
只有静态规格，不执行该协议。把模型接口验收与首次导航收益试验分开，不能把全部实验塞进数据验收。
给出后续 M1/M2/M4 的可比较配置字段和预算估计，不声称已冻结尚无方差依据的确认样本量或效应门槛。

### F. 自查与可执行结论

逐项回答：数据是否物理可构造、事件可观察且可标注、无人工逐例依赖、Qwen 接口有真实依据、未来监督隔离可实现、关键历史梯度有路径、强状态监督可匹配、资源估计可追溯。
结论只能为：

- `READY_FOR_MAIN_AGENT_REVIEW`：静态规格足够具体，建议主 agent 审核后进入指定工程验收；不自动批准执行。
- `BLOCKED_WITH_SPECIFIC_GAP`：列出准确阻碍、已检查的替代及解决它需要的最小权限/证据。不要无界研究或另起新主线。

## 5. 必须交付的文件

全部放在指定输出目录：

1. REPORT_ZH.md：首选决定、关键依据、阻碍与下一工程验收建议。
2. ASSET_AND_SOURCE_MANIFEST.json：资产、官方来源、版本、许可、暴露、核验深度。
3. MINIMAL_FAMILY_SPEC.md：具体任务、真实历史、续接、纸面 Y 表、API 映射和恢复示范规格。
4. DATA_SCHEMA.json：三类数据的声明式 schema；样例可另存 EXAMPLE_RECORDS.json，并标示 synthetic_spec_only。
5. IMPLEMENTATION_SPEC.md：三包接口、Qwen 证据、记忆/查询/梯度图与对照接口。
6. RESOURCE_PLAN.json：资源预算、假设、来源、未测项。
7. NEXT_ENGINEERING_GATE_DRAFT.json：独立数据验收的协议草案，executable=false。
8. result.json：本节点裁决和逐项证据状态。
9. PROTECTED_FILES_BEFORE.sha256、PROTECTED_FILES_AFTER.sha256；SHA256SUMS 覆盖交付内容，不含自身。

result.json 至少包含 gate_id、decision、primary_environment、critical_gaps、source_evidence_scope、proposed_next_gate、main_agent_review_required=true。
必须保留 scientific_pass=false、implementation_allowed=false、training_allowed=false、new_training_runs=0、new_navigation_episodes=0、measured_navigation_gain=null。runtime_verified=false；旧环境的历史验收另行引用，不升级本线状态。
逐项区分 STATICALLY_SUPPORTED、UNVERIFIED、BLOCKED，不把全部接口默认写 true。

## 6. 主 agent 验收与停止规则

主 agent 接收后检查原始源码证据、最小数据族是否真实成立、Qwen 接口和信息流、资源/数据许可以及强对照，决定是否批准下一工程节点。
你不改 STATUS 中的 gate_result、scientific_pass 或执行权限；不自行继续 G1，不执行自己刚写的草案。
交付前做 JSON 解析、相对链接和哈希检查。无需运行旧线大测试或研究框架 smoke；它们不能验证本次静态方案。
如有主线版本变化或受保护文件并行修改，列出差异回交，不强行恢复。

最后回复只需给：节点结论、首选环境与数据、最重要的证据/阻碍、产物路径、建议下一工程动作，以及“已停止，等待主 agent 审核”。明确本轮未运行训练、导航实验或 GPU 操作。
