# RevealVLN — MF3ZP review handoff

This handoff describes the latest versioned state for external code/science review.

## Repository snapshot

- Project: RevealVLN
- Revision: `mf3zp_revealskill_v1` plus the explicitly versioned Qwen-provisional exploratory diagnostic
- Base source commit before this handoff: `4ff282cb3b6ed45a9402903134a1aaf1ffb2a4b6`
- Frozen single-decision learned-gate family: **stopped** (`FROZEN_SINGLE_DECISION_GATE_FAMILY_STOPPED = true`)
- Public split access: `val_seen=false`, `val_unseen=false`, `test=false`, `test_challenge=false`
- No deployment checkpoint was generated by the provisional diagnostic.

## What is included

The commit contains the MF3ZP evidence-graph/schema code, Qwen annotation validators, the explicitly exploratory provisional feature/training runner, tests, versioned method notes, and small protocol/result summaries. Raw API payloads, images, caches, credentials, and the large human-review export are intentionally not part of the source handoff.

The Qwen model identifier used by the provisional path is the fixed string `qwen3.8-max`. The API key remains project-local in `.secret/qwen_api_key` and is never committed.

## Current scientific status

The 300-event MF3ZP pilot is outcome-blind and spans 39 development scenes (R2R 150, RxR 150). Structural Qwen evidence validation completed 538/538 records, but no human verification has been completed, so these are provisional labels rather than gold labels.

Only 99/300 pilot events (R2R 54, RxR 45; 37 scenes) had strict canonical matches to the existing exact utility outcomes. The provisional run therefore reports feasibility diagnostics, not a formal Oracle/REE gate.

Key provisional ITT utility results:

| Domain | Snapshot | Temporal | Snapshot + provisional Qwen |
|---|---:|---:|---:|
| R2R | +0.016408 (95% scene bootstrap [+0.002674, +0.032764]) | +0.006937 ([-0.006928, +0.022358]) | +0.017046 ([+0.003078, +0.033186]) |
| RxR | -0.024260 ([-0.055744, -0.001129]) | -0.022624 ([-0.054621, +0.002153]) | -0.017384 ([-0.053142, +0.016002]) |

The Qwen provisional resolved-fraction probe also had higher (worse) temporal MAE than snapshot (0.146579 vs 0.134078). The evidence is mixed by domain and does not authorize formal training or public evaluation.

## Review request

Please inspect the pushed commit and determine whether the mixed provisional result is primarily attributable to:

1. label/support mismatch (99 strict outcome matches out of 300),
2. provisional Qwen semantic labels,
3. representation/objective mismatch, or
4. a remaining implementation/provenance issue.

Do not rerun CAR, RCSP, DSR, MF3ZN, or MF3ZO; do not search another single-decision gate on the consumed 39-scene universe; do not access any public split. Any proposed next experiment must be a separately versioned, bounded protocol with a predeclared stop rule.

The formal MF3ZP protocol remains distinct from the exploratory provisional result. In particular, provisional Qwen labels must not be described as human-validated gold labels, and the current result must not be described as a TUAD/RevealSkill success or failure.
