# RevealNav Method-Freeze-2 正确性修订 3

**修订编号：** `MF2-CR3`  
**日期：** 2026-08-24  
**状态：** Phase-0C 混合审核协议；混合关卡通过前仍禁止特征生成和训练。  
**替代范围：** 仅替代
`METHOD_FREEZE_2_CORRECTNESS_REVISION_2.md` 中“双人人类审核”的要求。
MF2-CR1 与 MF2-CR2 的其他定义、阈值、数据边界、模块、关卡、非结论和
证据要求全部保持不变。  
**规范边界：** `FROZEN_SPEC.md` 继续保持逐字节不变。

## 1. 审核轨道与真实来源

固定的 35 项私有材料由三个相互隔离的轨道审核：

| 轨道 | 展示别名 | 审核者类型 | 审核系统 |
|---|---|---|---|
| H | 审核者 A | `human` | 项目研究者 |
| M1 | 审核者 B | `model` | Claude-Code 控制的 Qwen3.8-Max |
| M2 | 审核者 C | `model` | 全新且无既有上下文的 Codex 会话 |

图表和内部汇总可以使用 A/B/C 别名，但机器可读记录必须保留
`reviewer_type`、系统名称、精确版本、提示词哈希、审核包哈希和原始
输出。模型轨道不得被描述为人类标注者。本协议的准确名称是
**“单人类审核 + 两个独立 VLM 审计”**，不能称为三人类审核或人类间
一致性审核。

唯一有效的视觉输入为版本化局部地图审核包：

`artifacts/phase0/phase0c_language_review_35_v2_localmap/PHASE0C_LANGUAGE_REVIEW_35_V2_LOCALMAP.json`

当前固定 SHA-256 为
`3c3f650fa26ceb1d948614e3c1eb6800dca85504e1cad7690c52ab1294424c7c`。旧版全局路线
小图继续作为不可变来源证据保留，但已废止用于审核，三个轨道均不得
以旧图替代新版材料。

## 2. 隔离与执行顺序

1. H 轨道必须先独立审核全部 35 项，并且在此期间不能查看 M1/M2 的
   任何结果。完成后立即对 H 表计算 SHA-256 封存；封存后的修改会使
   关卡失效。
2. M1 与 M2 使用完全相同的英文固定提示词、审核包 JSON、中文图例审核
   板和四张因果观察图。两者在独立干净会话中运行，不能访问 H 结果、
   对方结果、既有审核对话或任何 `val_unseen/test` 数据。
3. 任何轨道都不得改变 35 项集合、阈值、事件元数据、图像、指令文本或
   目标语义出口；35 项必须全部审核，禁止只审核看起来有利的子集。
4. 模型轨道必须保留精确模型标识、温度/随机种子（或最接近的确定性
   设置）、执行时间和未经整理的原始输出。
5. Matterport 派生媒体保持私有，只能在本地受控或其他明确获授权的环境
   中处理。

## 3. 单项判定要求

三个轨道都必须填写以下六个布尔字段。字段名保留英文以保证验证器兼容：

1. `branch_dependent_instruction`：完成指令是否确实依赖所标出的定向
   分支，而不是仅经过一个泛化的邻近区域。
2. `target_branch_matches_instruction`：离线地图中绿色 `B→T` 目标出口
   是否就是指令要求的分支。`B` 表示参考路线转向后的目标
   分支入口，与观察前相机位姿 `P` 是两个不同概念，不要求重合。
3. `causal_reveal_confirmed`：目标分支在 `P` 画面中不可见或不明确，
   并在有序的 `D1–D3` 真实画面中变得可行动。此项不得使用下方离线
   地图作为“智能体已经看见”的证据。
4. `semantic_track_confirmed`：`C1–C3` 候选集合是否表示同一个定向、
   可执行出口，而不是两个不同的语义选项。
5. `cost_expiry_interpretation_confirmed`：该分支是否构成有意义的资源
   条件化末次可行/选项损失实例；不得把它解释成在线可观测的首次到达
   时间。
6. `candidate_valid`：只有前五项全部为 `true` 且审核者有把握时才能为
   `true`；任何不确定性必须记为 `false`。

此外必须填写：`reviewed=true`、审核者 ID、精确系统/模型版本、ISO-8601
时间戳、支持判定的最短指令片段 `instruction_clause`。无效或不确定项必须
填写 `rejection_reason`；有效项的拒绝原因留空；`reviewer_notes` 可选。
禁止改动任何核心事件字段或预填来源字段。

## 4. 固定汇总规则

一项事件只有在 H、M1、M2 三个轨道的六个布尔判断全部为 `true` 时才被
接纳。H 的拒绝为最终拒绝。任何 Human–VLM 或 VLM–VLM 分歧均须完整保留
并排除该项，不能通过改提示词、重复抽样或事后覆盖来消除。

混合审核只有同时满足以下条件才通过：

- 三个轨道均完整审核全部 35 项；
- 封存的 H 表和两个原始模型表全部通过结构、来源、审核包哈希和不可变
  事件字段检查；
- 三方一致接纳的集合至少包含 15 个唯一事件并覆盖至少 10 个 MP3D
  场景；
- 接纳事件中未解决语义歧义数和缺失字段数均为 0。

必须报告 Human–Qwen、Human–Codex、Qwen–Codex 对二元最终判定的两两
Cohen's kappa，以及三方完全一致率。这些是跨系统一致性指标，不能称为
人类间一致性指标。

## 5. 授权边界

混合关卡通过后，可以提出新的版本化 canonical method、生成允许训练切分
的特征并启动初始训练实验，但不得声称标签经过多人类审核。在将这些标签
作为论文最终真值前，应至少增加一名独立人类对接纳集合复核；否则必须在
论文中准确披露“单人类 + 双 VLM”协议并分析局限性。

混合结果通过前，整体 Phase-0C 保持 `NO_GO`，禁止特征生成和训练，语言
依赖 Reveal Event 的已验证数量仍为 0。
